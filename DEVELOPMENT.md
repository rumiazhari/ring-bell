# Development Guide

## Requirements
- Godot 4.7.x (tested with 4.7.2 stable, Windows)
  - This workspace uses: `C:/Vibe Code project/Godot Project/Godot_v4.7.2-stable_win64.exe`
  - Canonical project path: `C:/Vibe Code project/Godot Project/ring-bell` (never a OneDrive copy)

## Opening / running

- Open the project folder in Godot (it contains `project.godot`), press F5.
- Or from a terminal:

```powershell
& "C:/Vibe Code project/Godot Project/Godot_v4.7.2-stable_win64.exe" --path "C:/Vibe Code project/Godot Project/ring-bell"
```

## Controls

| Input | Action |
|---|---|
| WASD | Move (camera-relative) |
| Shift | Sprint (drains stamina) |
| LMB / Space | Melee attack |
| E | Interact / talk |
| F | Eat food item |
| G | Use medical item |
| Q / R | Rotate camera (also RMB-drag) |
| Mouse wheel | Zoom |

## Debug hotkeys

| Key | Effect |
|---|---|
| F3 | Toggle debug overlay (stats, quest states, world-event log) |
| F5 | Quicksave |
| F9 | Quickload |
| T | Cycle game time scale x1 / x8 / x30 |

Save file: `%APPDATA%\Godot\app_userdata\Ring Bell\saves\save_01.json`

## Headless validation (no window needed)

Run after any script change. All must finish with `finished with 0 failure(s)` per `tools/run_suite.py`.
A Windows child exit `3221225477` (0xC0000005) with that marker is not a failure — judge only by the marker, not the exit code. Small headless shutdown `ObjectDB !is_inside_tree()` noise is guarded where feasible (see `world/main.gd` + at least one probe harness). `tools/run_suite.py` default timeout is now 400 s for citytest/roadtest/ruraltest (P3.1/P4.1/P4.2 6 inflight + biome+road+rural overhead); explicit per-flag timeouts below reflect that, with `450` guidance for slower CI.

```powershell
$G = "C:/Vibe Code project/Godot Project/Godot_v4.7.2-stable_win64.exe"
$P = "C:/Vibe Code project/Godot Project/ring-bell"

# 1) Reimport + parse check (12 s)
python tools/run_suite.py --import 120

# 2) Settlement + road determinism + road/bridge budgets + streaming (P4.1)
python tools/run_suite.py --roadtest 400
# aliases also accepted (same harness):
python tools/run_suite.py --settlementtest 400
python tools/run_suite.py --roadmaterialtest 400

# 3) Rural building fabric determinism + shell budgets + streaming (P4.2)
python tools/run_suite.py --ruraltest 400
# aliases also accepted (same harness):
python tools/run_suite.py --settlementbuildingtest 400
python tools/run_suite.py --ruralfabrictest 400

# 4) Hydrology determinism + water manifest budgets (hydrology owns river+tributaries)
python tools/run_suite.py --hydrotest 300
# alias also accepted (same harness):
python tools/run_suite.py --hydromaterialtest 300

# 5) Procedural city determinism suite
python tools/run_suite.py --citytest 400

# 6) Terrain manifest + streaming budgets (17x17, 289 verts / 512 tris, 1 collider/chunk)
python tools/run_suite.py --terrainmaterialtest 300

# 7) Rural mosaic biomes/geology + streamed field/forest dressing (9x9, 81/128, ACTIVE-only, 48/12/6)
python tools/run_suite.py --biometest 300
# alias also accepted (same harness):
python tools/run_suite.py --biomematerialtest 300

# 8) Streamed-city integration (physics rays, doors, stairs, camera, persistence)
python tools/run_suite.py --cityruntime 400

# 9) Honest player traversal (no teleport: door -> stairs 5 storeys -> roof -> exit)
python tools/run_suite.py --walkthrough 360

# 10) Havoc physics + firearms integration (doors, explosion, SMG, rocket, glass ladder)
python tools/run_suite.py --havoctest 240

# 11) Functional regression suite (exits 0 on success; runs on the legacy block)
python tools/run_suite.py --smoke 180

# 12) Day/night + sleep AI + zombie wandering soak (legacy block)
& $G --headless --path $P -- --soak
```

The project wrappers `tools/run_suite.py` invoke `godot --headless --path <proj> -- --<flag>` and judge by the `finished with 0 failure(s)` marker printed by each harness (Windows `3221225477` with marker is a pass). Long Godot runs can hang past the shell timeout and lose partial output — `run_suite.py` redirects to a file so timeouts still yield diagnostics. Do not launch a second Godot instance while one may still be alive (`tasklist /FI IMAGENAME eq Godot*`).

Note when launching via `Start-Process`: quote the `--path` argument; prefer plain `&` invocation so output streams live.

## World modes

| Mode | When | Content |
|---|---|---|
| CITY (default) | normal launch | streamed procedural city + terrain + water + biome + road + rural building fabric: ChunkManager loads 64 m chunks (ACTIVE ring <= 1 with physics, WARM <= 2 visuals only, hysteresis UNLOAD=3) from deterministic CityPlan/TerrainPlan/HydrologyPlan/GeologyPlan/BiomePlan/SettlementPlan/RoadNetworkPlan/RuralBuildingPlan via WorldPlan facade; player spawns at the plaza anchor on the `URBAN_INNER_M=350` flat, ambient zombies scatter on streets; river corridor `CX 530-710 m` east with `9x9` water meshes (81 verts / <=128 tris, at most 1 water collider per wet chunk, 9 active water max) and `9x9` biome overlay (81 verts / <=128 tris, at most 1 biome collider per forest/quarry chunk, 9 active biome max) plus MultiMesh dressing (<=48 forest / <=12 field hedgerow / <=6 quarry) plus road ribbons (<=96 verts / <=64 tris typical, bridge deck +0.35) plus rural shells (<=280 verts / <=210 tris typical, dense <=480/360 village+well+forage, at most 1 rural shell+well collider per chunk, 9 active rural max, doors <=6 per village chunk, wells <=2 forage <=4, road setback >=4 m, spacing >=8 m) streams with the same pipeline and F3 stats `t_water_gen`/`t_water_mat`/`t_biome_gen`/`t_biome_mat`/`t_road_gen`/`t_road_mat`/`t_rural_gen`/`t_rural_mat` |
| LEGACY | `--smoke`, `--soak`, `--legacy-block` | hand-built Prototype 0 test block with the full narrative cast |

**Streaming telemetry (F3 overlay and headless logs):** `ChunkManager.debug_lines()` exposes `chunk | active | warm | queued | loads/unloads`, `boxes | colliders | doors | buildings | records`, `gen | materialize | resident`, plus terrain `verts | tris | colliders | t_gen | t_mat_total | active terrain (warm)`, water `verts | tris | colliders | t_water_gen | t_water_mat | active water (warm)` and biome `verts | tris | colliders | instances | t_biome_gen | t_biome_mat | active biome (warm)` and road `verts | tris | colliders | bridges | t_road_gen | t_road_mat | active road (warm)` and rural `verts | tris | colliders | doors | buildings | t_rural_gen | t_rural_mat | active rural (warm)` plus `settlement anchors total`. Per-chunk `terrain_gen_ms`/`terrain_mat_ms`, `water_gen_ms`/`water_mat_ms` and `biome_gen_ms`/`biome_mat_ms` and `road_gen_ms`/`road_mat_ms` and `rural_gen_ms`/`rural_mat_ms` are measured inside the worker (private WorldPlan) and on the main thread respectively and budgeted within `FRAME_BUDGET_MS 12.0`. Water district_hint `urban_basin|rural_plateau|river_valley` derived from radial distance and distance_to_water; bank ribbon remains vertex-color transition without extra geometry as budgeted choice (81/128) deferred until terrain trench carve. Road hierarchy primary 7.0/secondary 5.0/track 3.5 m, lift 0.04 / bridge +0.35, shared-edge centerlines within 0.02 m, bridge only at `crossing_candidates`. Rural building vocab village_house/cottage/barn/farmhouse/stable/shed, footprint 6-10x8-14 (village 8-10x10-12, barn 8-10x10-14), height 4.2+2.9*floors, plaster/brick/timber walls + roof 8a3a2a/5a5a5a, road setback 4 m, spacing 8 m, door faces road/settlement, no building inside floodplain/water/cliff or inside URBAN_INNER_M except gate barn, slope gates `<14` village_house else `<22`, distance_to_water > BANK_W+2.

**Collision budget (ACTIVE-only, intentional optimization — corrects the previous warm+active assumption, now including road and rural, folded C001-C004):** warm chunks retain their merged `MeshInstance3D` visuals (city, terrain, water, biome, road, rural) but their `StaticBody3D`/`WaterBody`/`BiomeBody`/`RoadBody`/`RuralBody` collision is disabled (`collision_layer=0` or `MeshBatcher.disable_collision()`). Only ACTIVE chunks contribute to `colliders`/`terrain_colliders`/`water_colliders`/`biome_colliders`/`road_colliders`/`rural_colliders` and to `active terrain`/`active water`/`active biome`/`active road`/`active rural` counts. This keeps physics at `9 city + 9 terrain + at most 9 water + at most 9 biome + at most 9 road + at most 9 rural` colliders (54 peak at 3x3, typically <=45+rural) while warm visuals stay resident for seamless streaming. Previous docs that stated warm+active physics are corrected here. Per-chunk budgets: city 1 mesh+1 body, terrain 289/512 1 collider, water 81/128 1 collider, biome overlay 81/128 1 MultiMesh <=48 instances 1 collider, road ribbon <=96/64 typical 160/96 junction 1 collider, rural shell <=280/210 typical 480/360 dense 1 collider — all ACTIVE-only. Streaming pacing `MAX_MATERIALIZATIONS_PER_FRAME 1` with early `_collect_finished_jobs(pc)` before recalc and Variant freed-Zombie guard (`is_instance_valid` + `!is_queued_for_deletion()` + `is_inside_tree()` before `global_transform`) keep frame budget stable (2a423d9, documented here).

Extra dev flag: `--shot` captures screenshots (gameplay + top-down overview + street level + far teleport) to `%TEMP%\opencode
b_*.png` and prints F3-style streaming stats, then quits. Note: headless `--shot` uses the dummy renderer and its textures are null — for the archived windowed proof (see below) use a normal **windowed** CITY run, not `--shot`.

### What `--roadtest` / `--settlementtest` / `--roadmaterialtest` verifies (P4.1-SETTLEMENT-ROADS)

Same-seed settlement anchors and road graph identical regardless of query/build order (including negative coords), different seed materially differs (settlement >=3/9 probes differ, road edges >=30% differ), settlement plan produces 12-36 anchors spaced village 700 / hamlet 420 / farmstead 220 + 1.8*radius with floodplain/slope/fertility gates (village slope <14 dist > BANK+FLOOD+14, hamlet not cliff/water, farmstead slope <22), city gates 4-8 on URBAN_OUTER+-60, road graph non-empty connects every village/hamlet to a city gate and crosses water only at `crossing_candidates` with `is_bridge`; geographic validity for canonical +4 alternate seeds using real `WorldConstants` thresholds and macro-cell non-speckling (>8 cells); road materialization budgets and seams: chunk manifests byte-identical shuffled, shared-edge centerlines within 0.02 m at + and - borders, each chunk <=1 road collider (0 if dry), verts <=160 (typical <=96) tris <=96 (typical <=64), 3x3 ACTIVE <=9 road colliders, `t_road_gen`/`t_road_mat` measured, at least 9 resident road chunks around a road+bridge transect; ChunkManager streams road with terrain/water/biome/city without duplication: 3x3 ACTIVE around primary road+bridge corridor claims `active road <=9`, walking 480 m beyond UNLOAD_RADIUS unloads and returning regenerates identical manifests (verts/tris/colliders/hierarchies/widths/colors), debug stats contain `t_road_gen`/`t_road_mat`, generated road excluded from `save_state()`; `GENERATOR_VERSION` stays 2 additive, `WorldPlan` pure, `CityPlan`/`TerrainPlan`/hydrology/biome unchanged.

### What `--ruraltest` / `--settlementbuildingtest` / `--ruralfabrictest` verifies (P4.2-RURAL-FABRIC)

Same-seed rural building manifests and `rural_buildings_in` identical shuffled incl. negative coords, different seed materially differs (`>=3/9` probes differ and `>=30%` placements differ), rural plan produces `1-6` buildings per settlement by kind (village `4-6`, hamlet `2-3`, farmstead `1-2`, isolated_farm `1`) spaced `>=8 m` and road setback `>=4 m`, no building inside floodplain/water/cliff or inside `URBAN_INNER_M` except gate barn, door faces road/settlement and obeys Door contract (closed blocks aperture center, open clears without RID exclusion, swung leaf remains collidable); geographic validity for canonical +4 alternate seeds: villages only where slope `<14` and `distance_to_water > BANK_W+FLOODPLAIN_W+14` outside floodplain/forest, farmsteads only where slope `< BUILDABLE_MAX` and not cliff/water, rural buildings only where `terrain_class != cliff` and slope `< BUILDABLE_MAX` (`<14` for village houses) and `distance_to_water > BANK_W+2` and setback `>=4` from road, never on `is_bridge` deck — using real `WorldConstants` thresholds, macro-cell non-speckling proven by clustering; rural materialization budgets and seams: manifests byte-identical shuffled, building center ownership gives no duplication at `+` and `-` borders, each chunk `<=1` rural shell collider (`0` if none), `verts <=320` (typical `<=192`) `tris <=240` (typical `<=144`), doors `<=6` per village chunk, `3x3` ACTIVE `<=9` rural colliders, `t_rural_gen`/`t_rural_mat` in stats, at least 9 resident rural chunks around a hamlet+road+bridge transect (primary/secondary/track counted); ChunkManager streams rural with city+terrain+water+biome+road without duplication: `3x3` ACTIVE around hamlet near road+bridge claims `active rural <=9`, walking `480 m` beyond `UNLOAD_RADIUS` unloads rural chunks and returning regenerates identical manifests (counts/footprints/yaws/colors/shell verts/tris/colliders/doors), debug stats contain `t_rural_gen`/`t_rural_mat`, generated rural excluded from `save_state()`; determinism and buildability preserved after rural fabric: `GENERATOR_VERSION` stays `2` additive, `WorldPlan` pure, `CityPlan` IDs and `TerrainPlan` `17x17` and hydrology `CX/width 530-710/72/38-50` and biome contiguity `>=192 m` and road seam `0.02` and `9` resident road chunks unchanged.

### What `--hydrotest` / `--hydromaterialtest` verifies (P2.2-HYDRO-ANCHORS)

Same-seed hydrology queries and water manifests identical regardless of query/build order (including negative coords), different seed materially differs, primary centerline continuous across every chunk seam, both tributaries approach monotonically and meet within 600 m of the seeded confluence; geographic validity for canonical +4 alternate seeds: deterministic `CX 530-710 m` + meander `+-72 m`, width `38-50 m`, bank `9 m` / floodplain `26 m` monotonic, no water on cliff slopes/ridge hilltops except lake placeholder, flow unit length with northward `>0.35` primary and `>0.45` tributary convergence dot using real `WorldConstants` thresholds; water materialization budgets and seams: chunk water manifests byte-identical across shuffled builds, shared-edge heights/centerline within `0.02 m` at `+` and `-` boundaries, `<=1` water collider per chunk (`0` if dry), `<=9` active water in `3x3`, bounded `81/128` verts/tris per wet chunk (9x9) and per-chunk `water_mat_ms`; ChunkManager streams water with terrain+city without duplication: `3x3` ACTIVE ring around a hill near the river claims `active water <=9`, walking `480 m` beyond `UNLOAD_RADIUS` unloads river chunks and returning regenerates identical manifests, debug stats contain `t_water_gen`/`t_water_mat`, generated water excluded from `save_state()`.

### What `--citytest` verifies

Same world seed -> identical plan data and chunk geometry manifests regardless of query/build order; a different seed produces a materially different city; spawn anchors exist; multi-storey buildings generate near the center; chunk discovery records survive a save/load round-trip; building overlap, door manifest, stair/landing geometry, facade/balcony/scaffolding, and destruction delta persistence.

### What `--terrainmaterialtest` verifies

Terrain is a 17x17 heightfield per 64 m chunk (4 m spacing, 289 verts / 512 tris, 1 Concave per chunk, 9 active max); shared-edge heights identical within `0.02 m` at `+/-` boundaries; manifest equality across shuffled builds; material vocab, cliff-to-rock, normals, and mesh/collision extents; ACTIVE-only terrain colliders, `t_gen`/`t_mat` timings; ground ownership (`URBAN_INNER_M=350` flat, `URBAN_OUTER_M=600` transition) and no duplicate flat/terrain surfaces.

### What `--biometest` / `--biomematerialtest` verifies (P3.1-RURAL-MOSAIC)
Same-seed geology and biome queries identical shuffled incl. negative coords, different seed materially differs >=3/9, 40 probes show contiguous forest/field clusters run-length >=192 m (3 chunks) with no speckling, vocab subset of `WorldConstants.BIOME_VOCAB`; geographic validity for canonical +4 alternate seeds: river_floodplain/wet_meadow only within `BANK_W+FLOODPLAIN_W+16 m` where slope < BUILDABLE_MAX and not cliff, forest only on slope/elevation-compatible outside water/floodplain (mixed only on upland >=38 m), arable/pasture only on gentle <12-14deg and fertility gate outside floodplain/forest, quarry only where quarry_suitability >0.72 and slope >=28 or cliff/limestone upland — using real `WorldConstants` thresholds; rural materialization budgets and seams: manifests byte-identical shuffled, shared-edge biome agreement >=7/9 samples at + and - boundaries, each chunk <=1 biome collider (0 if no proxies), instances <=48 forest / <=12 field hedgerow / <=6 quarry, 3x3 ACTIVE <=9 biome colliders, each overlay 81 verts / <=128 tris (9x9) with biome_mat_ms measured, at least 9 resident biome chunks around rural transect; ChunkManager streams biome with terrain/water/city without duplication: 3x3 ACTIVE around field/forest edge claims active biome <=9, walking 480 m beyond UNLOAD_RADIUS unloads biome chunks and returning regenerates identical manifests (colors/instances/verts/tris/colliders), debug stats contain t_biome_gen/t_biome_mat, generated biome excluded from save_state().

### What `--cityruntime` verifies

ChunkManager exists, `3x3` ACTIVE ring, player spawned, exterior wall blocks physics ray, zombies and door entities exist, closed leaf blocks doorway center, open doorway clear without RID exclusion and swung leaf remains collidable, far ring activates after teleport and origin chunks unload/return deterministically, stair probe reaches upper floor, camera rig exposes real lens offset and sector/facade math, interior presentation distance `<=9 m` while preserving user zoom, and streamed destruction survives unload/reload.

### What `--walkthrough` verifies

Honest player traversal (no position writes/teleport): closed door blocks, E opens it, walk door->stairwell, climb all 5 storeys to roof deck, camera tracks vertical climb, descend to ground, walk out, close door and verify it blocks again. No waypoint skips.

### What `--havoctest` verifies

Door structural destruction leaves doors group and spawns wood debris, explosion damages/kills zombie and emits noise and ragdoll, player survives blast, weapon system wired, destructible prop placed with clear shot, SMG damages prop, rocket in flight detonates into debris, explosion respects material integrity, glass damage ladder matches `ItemDB` weapons, camera follows vertical movement.

### What `--smoke` verifies

Population spawns; hungry NPC eats carried food; crates feed NPCs; melee damages/kills zombies; quest objective advances if Hana was met BEFORE the quest starts; Hana's independent death fails the quest and is recorded; Kenji's dialogue switches to a grief branch; parkour ledge/cornice/awning grabs, stamina scaling, and HUD flashes; semantic room layouts and zombie flanking; save/load keeps her dead, respawns everyone else, restores the clock.

## Manual test walkthrough (the narrative + hydro + biome + road + rural building fabric proof)

1. Run the game windowed (1200x720 windowed, CITY default). Spawn at plaza, toggle F3.
2. With WASD approach a qualifying building (`int(floors)>=2`, `BuildingBuilder.has_stairs_for` true); closed entrance must stop the capsule. Press E — door visibly swings and remains solid at the swung position, walk through the aperture, cross the entry corridor, climb the visible stairwell to every floor and the roof deck, reverse and leave, close the door from outside and verify it blocks again. F3 overlay must show `active water` and `t_water_gen`/`t_water_mat` plus `active biome` and `t_biome_gen`/`t_biome_mat` plus `active road` and `t_road_gen`/`t_road_mat` plus `active rural` and `t_rural_gen`/`t_rural_mat` (even if zero near spawn) alongside `active terrain`; water `district_hint` + bank-ribbon vertex-color choice remains 81/128 as budgeted; road `primary 7.0 / secondary 5.0 / track 3.5 m` lift `0.04` and bridge deck `+0.35` are vertex-colored grey/gravel/dirt without authored texture; rural shells are vertex-colored plaster/brick/timber walls + roof `8a3a2a`/`5a5a5a` boxes `6-10x8-14` at terrain+0.01 with cardinal yaw, doors face road/settlement, road setback 4 m, spacing 8 m, building center ownership.
3. Then walk east (or toward the deterministic river `CX` at `~620 m`) `600-900 m` over rolling hills following a **readable rural road corridor** (grey paved/gravel/tan dirt ribbon) — the valley should appear as a long teal water surface `4a7a94` with darker banks and lighter floodplain meadow contrast plus continuous biome tint field->wet meadow->floodplain and hedgerow/tree proxies plus **continuous road ribbon** meeting the river at a single bridge/ford deck (`+0.35` over water, bank `9 m` floodplain `26 m`) and continuing toward a visible settlement anchor (hamlet/farmstead clearing) with **continuous rural shells `2-6` low-rise vertex-colored boxes with working doors set back from road ribbon `7.0/5.0/3.5` lift `0.04` plus visible bridge deck `+0.35` over teal water** without 64 m seam cracks or flicker as chunks warm-stream. Stand on a bridge deck hill and on a road/field junction hill and observe (a) road ribbon continuity across streamed chunks (no cracks at 64 m seams, ribbon interpolates, no flicker), (b) water+biome+road+rural continuity alongside road, (c) instanced biome trees/hedgerows plus road ribbon plus rural shells pop in only within ACTIVE ring and remain stable without duplication, and one rural door can be opened/closed from outside with `E` and still blocks when closed. If a failure occurs, retain PNG/log with exact player `global_position`, `WorldSeed` value, `nearest_settlement(p)`/`nearest_rural_building(p)`/`road_hierarchy_at(p)`/`distance_to_road(p)`/`distance_to_water(p)` and river/road centerline X at that Z.
4. Quest proof: Talk to **Kenji Tanaka** inside the apartment building (east door). Accept his task. Cross the east road; find **Hana Tanaka** near the wrecked cars in the southeast corner. Talk to her - objective flips to "Return to Kenji". Return to Kenji -> quest completes. Variation A: talk to Hana FIRST, then Kenji - he skips the "find" objective. Variation B: while the quest is active, let a zombie kill Hana (or use timescale T) - the quest FAILS by itself and Kenji now only speaks the grief branch. Kill Hana, F5, quit, relaunch, F9 -> she stays dead after load.

Do not compensate with teleport or disabled collision. A screenshot built with `--shot` in headless is not sufficient for this proof due to dummy renderer; the archived PNG must be from a real windowed run and stored under `.hermes/autopilot/reports/SPEC-C005-windowed.*` or `junk/` with its run log, referenced in the builder handoff. This single capture satisfies the surviving C001+C002+C003+C004 windowed proof plus the new rural building fabric proof.

## Conventions for AI-assisted changes

- Read ARCHITECTURE.md before refactoring anything.
- One responsibility per file; keep files small and explicitly named.
- Update the signal contract list in `event_bus.gd` when adding signals.
- Never hard-code actor ids outside `population.gd`, `dialogue_data.gd`,
  and quest definitions.
- After changes: run every gate named by the active spec through `tools/run_suite.py` and judge by the `finished with 0 failure(s)` marker (Windows `3221225477` with marker is a pass). Do not weaken assertions, hide errors, accept pending/inflight work as completed behavior, or replace ordinary movement with teleportation when the spec requires player traversal.
- Preserve all unrelated dirty files and user work. Never reset or clean the checkout. Never delete files; move unwanted artifacts into project `junk/`.
- Headless shutdown noise: where `world/main.gd` or smoke/walkthrough probes fetch `global_transform` after `queue_free` during shutdown, guard with `is_instance_valid` + `is_inside_tree()` before reading transforms; ensure `ObjectDB` script warnings are quelled without masking real failures.

## P4.3 Rural Interior & Scavenge — delta from P4.2 (this cycle)

- What --ruraltest now proves: same-seed rural+interior+crate manifests identical shuffled incl. negative coords, different seed differs >=3/9 and >=30% placements+contents, 1-6 buildings per settlement by kind (village 4-6/hamlet 2-3 strict/farmstead 1-2) aabb spacing >=8 and uniform road setback >=4, partition 0-1 inset 0.5 doorway 0.95 not overlapping door swing 1.0, furniture 1-3 against walls >=0.9, crate 0-3 per village chunk 1 per hamlet 1-4 items ItemDB 40/25/20/15, door faces road/settlement and obeys Door contract. Geographic validity for canonical +4 seeds via real WorldConstants thresholds, macro-cell non-speckling clustered not speckled. Budgets: manifests byte-identical shuffled, building center ownership no duplication at + and - and -Z borders shared road centerline 0.02, each chunk <=1 rural shell+interior collider (0 if none) warm collision_layer 0, verts <=400 tris <=300 per chunk (typical <=240/180 hamlet/farmstead), doors <=6 per village crates <=3 per village furniture <=6 per village, 3x3 ACTIVE <=9 rural colliders, t_rural_gen/t_rural_mat in stats within FRAME_BUDGET_MS 12, at least 9 resident rural chunks around hamlet+road+bridge transect with interior wall gap 0.90-1.10 and crate ItemDB vocab. ChunkManager streams rural shells+interiors+crates with city+terrain+water+biome+road without duplication, walking 480m beyond UNLOAD_RADIUS unloads and returning regenerates identical manifests and debug stats contain t_rural_gen/t_rural_mat and crates/furniture, generated rural excluded from save_state() except deltas.crates. Determinism preserved GENERATOR_VERSION stays 2 additive, WorldPlan pure, CityPlan IDs and TerrainPlan 17x17 and hydrology CX/width and biome contiguity and road seam 0.02 and 9 resident road chunks unchanged — proved by --citytest + --terrainmaterialtest + --hydrotest + --biometest + --roadtest each 0 failures with retained seams.
- Streaming pacing: MAX_MATERIALIZATIONS_PER_FRAME 1 with early _collect_finished_jobs(pc) before streaming recalc + Variant freed-Zombie guard (is_instance_valid && !is_queued_for_deletion() && is_inside_tree() before global_transform) — intentional pacing fix from 2a423d9, documented here, not reverted; keep MAX_INFLIGHT 6.
- run_suite timeout: --citytest/--roadtest/--ruraltest 400 (document 450 guidance for slower CI), --hydrotest/--biometest/--terrainmaterialtest 300, --cityruntime 300, --walkthrough 360, --havoctest 240, --smoke 180. Windows exit 3221225477 with marker not failure; ObjectDB inside_tree guards extended to crate path (main.gd + probe harnesses including rural_test crate disable guarded plus new FoodCrate pathway).
- Manual proof: normal windowed CITY run (1200x720) shows WASD/E door closed blocks/open clears without RID exclusion/swung collidable, stair to roof camera following, F3 overlay with active rural + t_rural_gen/t_rural_mat and crates alongside active road/biome/water/terrain, then 600-900m east along rural road corridor to river valley showing continuous rural shells 2-6 low-rise vertex-colored with working doors set back from road ribbon 7.0/5.0/3.5 lift 0.04 plus visible bridge deck +0.35 over teal water 4a7a94 bank 9 floodplain 26 without seam cracks/flicker and through open door partition 0.95 plus furniture silhouette plus crate Search shelves (Canned Food x1) via E takes one item prompt updates. PNG+log under .hermes/autopilot/reports/SPEC-C006-windowed.* referenced; prior synthetic placeholders correctly deferred. Folding C001-C005 deferred docs (ACTIVE-only, water district_hint + bank vertex-color, streaming pacing, timeout, shutdown noise guards, windowed proof).

## P4.4 Rural Homestead Renewables — delta from P4.3 (this cycle)

- What --ruraltest now proves: same-seed rural+interior+crate+well+forage manifests identical shuffled incl. negative coords, different seed differs >=3/9 and >=30% placements+contents, 1-6 buildings per settlement by kind (village 4-6/hamlet 2-3 strict/farmstead 1-2) aabb spacing >=8 uniform road setback >=4, partition 0-1 inset 0.5 gap 0.95 not overlapping door 1.0, furniture 1-3 against walls >=0.9, crate 0-3/village 1/hamlet ItemDB 40/25/20/15, well 1/hamlet 1-2/village 0-1/farmstead >=8 from building >=6 from well road 4.0, forage 2-5 per vicinity allowed biomes >=6 building >=4 well/forage >=2 road, door/well/forage Interactable contracts with prompt updates to dry/picked. Geographic validity for canonical +4 seeds via real WorldConstants thresholds macro-cell non-speckling. Budgets: manifests byte-identical shuffled for buildings/doors/interiors/crates/wells/forage, building/well/forage center ownership no duplication at + and - and -Z borders shared road centerline 0.02, each chunk <=1 rural shell+well collider (0 if none) warm collision_layer 0, verts <=480 tris <=360 per chunk (typical <=280/210 hamlet/farmstead), doors <=6 crates <=3 wells <=2 forage <=4 furniture <=6 per village, 3x3 ACTIVE <=9 rural colliders, t_rural_gen/t_rural_mat in stats within FRAME_BUDGET_MS 12, at least 9 resident rural chunks around hamlet+road+bridge transect with wells/forage ItemDB vocab and non-empty wells/forage. ChunkManager streams rural shells+interiors+crates+wells+forage with city+terrain+water+biome+road without duplication: 3x3 ACTIVE around hamlet near road+bridge claims active rural <=9 and wells/forage active counts, walking 480 m beyond UNLOAD_RADIUS unloads and returning regenerates identical manifests and debug stats contain t_rural_gen/t_rural_mat and crates/furniture/wells/forage, generated rural excluded from save_state() except deltas.crates+wells+forage with depleted flags and depleted_at_day re-applied before materialize, refill well at 04:00 next day and forage after 2 days via GameClock. Determinism and buildability preserved after renewables: GENERATOR_VERSION stays 2 additive, WorldPlan pure, CityPlan IDs and TerrainPlan 17x17 and hydrology CX/width 530-710/72/38-50 and biome contiguity >=192 m and road seam 0.02 and 9 resident road chunks unchanged — proved by --citytest + --terrainmaterialtest + --hydrotest + --biometest + --roadtest each finished with 0 failure(s) with retained seams.
- Streaming pacing: MAX_MATERIALIZATIONS_PER_FRAME 1 with early _collect_finished_jobs(pc) + freed-Zombie Variant guard (is_instance_valid && !is_queued_for_deletion() && is_inside_tree() before global_transform) — intentional pacing fix from 2a423d9 documented here not reverted; keep MAX_INFLIGHT 6. Well/forage ACTIVE-only and forage Area3D monitorable false when warm.
- run_suite timeout: --citytest/--roadtest/--ruraltest 400 (document 450 guidance for slower CI), --hydrotest/--biometest/--terrainmaterialtest 300, --cityruntime 300, --walkthrough 360, --havoctest 240, --smoke 180. Windows exit 3221225477 with marker not failure; ObjectDB inside_tree guards extended to well/forage path. WORLD-CONTRACT §16, ARCHITECTURE streaming notes 280/210 480/360 justification wells<=2 forage<=4 Well/ForagePatch ItemDB 40/25/20/15 regrow well 1 day 04:00 forage 2 days, ACTIVE-only clarified.
- Manual proof: normal windowed CITY run (1200x720) shows WASD/E door closed blocks/open clears without RID exclusion/swung collidable, stair to roof camera following, F3 overlay with active rural + t_rural_gen/t_rural_mat and crates/wells/forage alongside active road/biome/water/terrain, then 600-900 m east along rural road corridor to river valley showing continuous rural shells 2-6 low-rise vertex-colored with working doors set back from road ribbon 7.0/5.0/3.5 lift 0.04 plus visible bridge deck +0.35 over teal water 4a7a94 bank 9 floodplain 26 without seam cracks and beside a barn a stone well ring 1.8m with Draw water prompt and along hedgerow forage bushes with Forage prompt; E draws/picks and prompt updates to dry/picked then depleted disables and state survives 480m unload/return. PNG+log under .hermes/autopilot/reports/SPEC-C007-windowed.* referenced; folding C001-C006 deferred docs.

## P4.5 Rural Hearth Habitation — delta from P4.4 (this cycle)

- What --ruraltest now proves: same-seed rural+interior+crate+well+forage+hearth (stove/bed reusing furniture pos) manifests identical shuffled incl. negative coords, different seed differs >=3/9 and >=30% placements+contents, 1-6 buildings per settlement by kind (village 4-6 hamlet 2-3 strict farmstead 1-2) aabb gap >=8 road setback 4.0 uniform, partition 0-1 inset 0.5 gap 0.95, furniture 1-3 against walls >=0.9, crate 0-3/village 1/hamlet ItemDB 40/25/20/15, well 1/hamlet 1-2/village >=8 building >=6 well road4.0, forage 2-5 per vicinity allowed biomes >=6 building >=4 well/forage road2.0 distinct kind weights 45/30/25 no duplicate, hearth stove/bed 0-1 per building reusing furniture anchor >=0.9/1.0 gates, door/well/forage/stove/bed Interactable contracts with stove Cook meal consuming canned_food and bed Sleep advancing GameClock 480 min. Geographic validity for canonical +4 alternate seeds via real WorldConstants thresholds macro-cell non-speckling clustered not speckled. Budgets: manifests byte-identical shuffled for buildings/doors/interiors/crates/wells/forage/hearth, building/well/forage/hearth center ownership no duplication at + and - and -Z borders shared road centerline 0.02, each chunk <=1 rural shell+well collider (well baked into same Concave, no second WellBody — harness asserts body count == rural_colliders) verts <=480 tris <=360 per chunk (typical <=280/210 hamlet/farmstead with 1 hearth, village core <=480/360 with 6 buildings+2 wells+4 forage+2 stoves+2 beds — hearth shares 24 verts already counted, so no increase), doors <=6 crates <=3 wells <=2 forage <=4 furniture <=6 per village, stoves <=2 beds <=2 hearth <=4 per village, 3x3 ACTIVE <=9 rural colliders unified 54 peak not 63, t_rural_gen/t_rural_mat in stats within FRAME_BUDGET_MS 12, at least 9 resident rural chunks around hamlet+road+bridge transect with hearth/well/forage ItemDB vocab and non-empty hearth and unified collider. ChunkManager streams rural shells+interiors+crates+wells+forage+hearth with city+terrain+water+biome+road without duplication: 3x3 ACTIVE around hamlet near road+bridge claims active rural <=9 and wells/forage/hearth active counts and stove/bed Area3D enabled only ACTIVE, walking 480 m beyond UNLOAD_RADIUS unloads and returning regenerates identical manifests and debug stats contain t_rural_gen/t_rural_mat and crates/furniture/wells/forage/hearth stoves/beds, generated rural excluded from save_state() except deltas.crates+wells+forage (hearth stateless) with depleted flags re-applied before materialize, refill well at 04:00 next day and forage after 2 days proven via deterministic GameClock.advance and patch size check.

- Streaming pacing: MAX_MATERIALIZATIONS_PER_FRAME 1 with early _collect_finished_jobs(pc) + freed-Zombie Variant guard (is_instance_valid && !is_queued_for_deletion() && is_inside_tree() before global_transform) — intentional pacing fix from 2a423d9 documented here not reverted; keep MAX_INFLIGHT 6. Hearth Area3D monitorable ACTIVE-only, unified rural shell+well 1 collider peak 54.

- run_suite timeout: --citytest/--roadtest/--ruraltest 400 (document 450 guidance for slower CI), --hydrotest/--biometest/--terrainmaterialtest 300, --cityruntime 300, --walkthrough 360, --havoctest 240, --smoke 180. Windows exit 3221225477 with marker not failure; ObjectDB inside_tree guards extended to hearth path (Stove/Bed Area3D disable path, Well Area3D). WORLD-CONTRACT §17, ARCHITECTURE streaming notes 280/210 480/360 justification wells<=2 forage<=4 stoves<=2 beds<=2 hearth<=4 Well Area3D + Stove/Bed ItemDB/Needs/GameClock table + regrow 04:00/2 days + 480 min sleep, ACTIVE-only clarified for hearth, unified 54 peak.

- Manual proof: normal windowed CITY run (1200x720) shows WASD/E door closed blocks/open clears without RID exclusion/swung collidable, stair to roof camera following, F3 overlay with active rural + t_rural_gen/t_rural_mat and crates/wells/forage/hearth alongside active road/biome/water/terrain, then 600-900 m east along rural road corridor to river valley showing continuous rural shells 2-6 low-rise vertex-colored with working doors set back from road ribbon 7.0/5.0/3.5 lift 0.04 plus visible bridge deck +0.35 over teal water 4a7a94 bank 9 floodplain 26 without seam cracks and inside open door stove Cook meal (Canned Food x1) consuming inventory and bed Sleep until dawn advancing GameClock 480 min to 06:00 with fatigue reduction and beside barn stone well Draw water and along hedgerow forage Forage bushes; E interacts update to needs Canned Food/dry/picked and depleted survives 480m unload/return with GameClock refill proven. PNG+log under .hermes/autopilot/reports/SPEC-C008-windowed.* referenced; ARCHITECTURE.md/DEVELOPMENT.md updated for ACTIVE-only hearth Area3D, unified collider 54 peak, stove/bed Needs/GameClock table, 480/360 280/210 unchanged, streaming pacing 1-per-frame + freed-Zombie guard, WORLD-CONTRACT §17.



## P5.1 Field-Parcel Cultivation (delta from P4.5)

- Field parcels stream with biome overlay at terrain+0.02: tilled quads 18-64x14-48 with crop vertex colors c2b280/8faa6a/6e635a/6a8a5a plus hedgerow 5a7a3a 2 per parcel capped 8 of 48, at most 4 parcels 4 CropPatch Area3D per chunk 96/64 verts/tris typical 64/32, 1 biome collider 9 active, unified 54 peak not 63, t_biome_gen/mat within FRAME_BUDGET_MS 12, landscape-cell 256 coherent 0-3 with village-adjacent +1 density 0.38, road 3.0 building 8 well/forage 6 spacing 4 height variance 0.8, ARABLE_MAX 12 PASTURE_MAX 14, is_grown planted+2 CropPatch monitorable ACTIVE-only, regrow 2 days via GameClock.advance, deltas.field_crops re-applied before materialize, Variant freed-Zombie guard extended to CropPatch, MAX_MATERIALIZATIONS_PER_FRAME 1 with early _collect_finished_jobs(pc) pacing retained. Run `python tools/run_suite.py --biometest 300` (now also field parcel gate) plus existing 400/300/400/300/400/300/400/360 harnesses, all with 0 failure(s). Windowed proof .hermes/autopilot/reports/SPEC-C009-windowed.* shows continuous tilled parcels 200-600m wheat c2b280 / barley 8faa6a at terrain+0.02 bounded hedgerow 5a7a3a enclosures aligned track/contour not speckles plus CropPatch Harvest wheat prompt.

## P5.2 Orchard Rows — delta from P5.1 (2026-08-31)

- Added orchard parcel cultivation per 256 m landscape cell (0-2 with village-adjacent +1 density 0.42, footprint 20-68x16-52 terrain+0.04 aabb gap >=4 building >=8 well/forage >=6 road >=3 never floodplain/water/cliff/urban 350, is_grown orchard planted+3 regrow 3d via GameClock). Bounded by true-mesh hedgerow 2.0x0.45-0.75x0.4 at terrain+0.04 and canopy rows 3-4 rows 3-5 trees at terrain+0.04 plus one ACTIVE-only FruitPatch per parcel (apple/plum/pear/cherry weighted 40/30/15/15 distinct ItemDB apple/plum hunger 18/16). Gives 200-600 m canopy blocks visible from ridge, orchard-vs-field harvest distinctness, true hedgerow not stretched plus tilled lift 0.02->0.04 exposing furrows.
- WorldConstants: ORCHARD_PARCEL_* and FRUIT_* plus HEDGEROW_TRUE_* and FIELD_PARCEL_LIFT_M corrected 0.02->0.04, COL_TRUNK/CANOPY, MAX_ORCHARD_INSTANCES 12, ORCHARD_HEDGEROW_MAX 6. WorldSeed: ORCHARD_PARCEL_DOMAINS ordered. ItemDB: apple/plum/pear/cherry 18/16/14/12. BiomePlan: orchard parcels per 256 cell with village bias, tree_rows/trees_per_row, fruit growth planted+3. BiomeChunkBuilder: tilled lift 0.04 + hedgerow true-mesh + orchard canopy 12 + FruitPatch ACTIVE-only, budgets 81/128+96/64+12, hedgerow 8/6 of 48, unified 54 peak. ChunkManager: orchard totals, debug_lines orchard_parcels/fruit_patches, ACTIVE-only FruitPatch, save_state deltas.fruit_patches, regrow 3d.
- Docs: WORLD-CONTRACT §19, ARCHITECTURE P5.2 and dedup P5.1 duplicate, streaming pacing 1-per-frame + freed-Zombie guard verbatim.
- Verification: --biometest 0, --ruraltest 0, --roadtest 0, --hydrotest 0, --citytest 0, --terrainmaterialtest 0, --cityruntime 0, --walkthrough 0, --havoctest 0, --smoke 0, GENERATOR_VERSION stays 2, city 9/terrain 9/water <=9/biome <=9/road <=9/rural <=9 shell+well unified <=54 peak field/orchard Area3D not counted t_biome_gen/mat within 12 ms plus true hedgerow 2.0x0.45-0.75x0.4 not stretched.


## P5.3 Workbench Economy — delta from P5.2 (this cycle)

- What --biometest and --ruraltest now prove: same-seed rural manifests including rural_workbenches center/pos/aabb/yaw/building_id/settlement_id + CropPatch distinct wheat->wheat_grain barley->barley_grain vs canned + Workbench recipes Mill flour/Bake bread/Press cider byte-identical shuffled incl. negative coords, different seed differs >=3/9 and >=30% placements, 1 workbench per village barn 0-1 per hamlet 50% via unit_float, footprint 1.2x0.9x0.6 terrain+0.04 aabb gap >=8 between workbenches >=6 well/forage >=8 building >=3 road never floodplain/water/cliff/urban 350, CropPatch is_grown planted+2 yields wheat_grain/barley_grain not canned, Workbench monitorable==ACTIVE stateless and prompt priority Bake>Cider>Mill vs needs, ItemDB wheat_grain 12 barley_grain 10 flour 14 bread 42 cider thirst 38. Geographic validity holds for canonical +4 seeds via real WorldConstants thresholds, parcel contiguity 200-600m hedged tilled+canopy blocks not speckle with at least 5 village workbenches in 5-seed belts and barn aabb ownership no duplication at + and - and -Z. ChunkManager streams rural shells+interiors+wells+forage+hearth+workbench plus distinct CropPatch wheat_grain/barley_grain with city+terrain+water+biome+road without duplication: 3x3 ACTIVE around village barn claims active rural <=9 and workbenches active counts monitorable only ACTIVE, walking 480m beyond UNLOAD_RADIUS unloads and returning regenerates identical rural+workbench manifests and debug stats contain t_rural_gen/mat and workbenches, generated rural/workbench geometry not in save_state except deltas.field_crops with wheat_grain contents re-applied before materialize, regrow 2d plus is_grown planted+2 proven via GameClock.advance mock; Workbench stateless survives 480m walk. Determinism preserved GENERATOR_VERSION stays 2 additive, WorldPlan pure, CityPlan IDs and TerrainPlan 17x17 and hydrology CX/width 530-710/72/38-50 and settlement roads 700/420/220+1.8r 4-8 gates MST is_bridge and rural 1-6 >=8 aabb >=4.0 unchanged — proved by --citytest + --terrainmaterialtest + --hydrotest + --roadtest + --biometest each 0 failures with retained seams. Existing budgets not weakened: --citytest, --terrainmaterialtest, --hydrotest, --biometest, --roadtest, --ruraltest, --cityruntime, --walkthrough, --havoctest, --smoke each 0 failures (3221225477 with marker not failure; ObjectDB inside_tree guards extended to Workbench); closed leaf blocks, FoodCrate/Well/ForagePatch/Stove/Bed/CropPatch/FruitPatch/Workbench prompts update and depleted/monitorable ACTIVE-only, CropPatch Harvest wheat now yields wheat_grain 12/barley_grain 10 not canned, FruitPatch apple/plum gated planted+3, Workbench Mill flour Wheat Grain x2->Flour Bake bread Flour->Bread 42 Press cider Apple x2->Cider thirst 38 via InventoryComponent remove/add stateless and monitorable false when warm, walkthrough climbs 5 storeys, havoctest honest, 54 peak field/orchard/workbench Area3D not counted t_* within 12ms.

- Streaming pacing: MAX_MATERIALIZATIONS_PER_FRAME 1 with early _collect_finished_jobs(pc) + freed-Zombie Variant guard (is_instance_valid && !is_queued_for_deletion() && is_inside_tree() before global_transform) — intentional pacing fix from 2a423d9 documented here not reverted; keep MAX_INFLIGHT 6. Workbench Area3D monitorable ACTIVE-only, unified rural shell+well 1 collider peak 54, forage/hearth/workbench Area3D monitorable false when warm.

- run_suite timeout: --biometest 300 / --ruraltest 400 / --roadtest 400 / --hydrotest 300 / --citytest 400 / --terrainmaterialtest 300 / --cityruntime 300 / --walkthrough 360 / --havoctest 240 / --smoke 180 (document 450 guidance for slower CI for citytest/roadtest/ruraltest). Windows exit 3221225477 with marker not failure; ObjectDB inside_tree guards extended to Workbench path (main.gd + probe harnesses including rural_test Workbench disable guarded plus Well/Forage/Stove/Bed/CropPatch/FruitPatch/Workbench pathway).

- Manual proof: normal windowed CITY run (1200x720) shows WASD/E door closed blocks/open clears without RID exclusion/swung collidable, stair to roof camera following, F3 overlay with active rural 280/210 480/360 plus t_rural_gen/mat and rural_workbenches alongside active biome 81/128+96/64+12 plus t_biome_gen/mat and field_parcels/field_crops/orchard_parcels/fruit_patches active road/water/terrain, then 600-900m east along rural road corridor to river valley showing continuous tilled parcels 0.04 wheat/barley furrow bounded true hedgerow 0.45-0.75m plus orchard canopy rows 3-4 rows 3-5 trees trunk 0.35x1.8+canopy at 0.04 200-600m blocks plus village barn workbench Box 1.2x0.9x0.6 7a6a5a at 0.04 inside barn with CropPatch Harvest wheat (Wheat Grain x1) and FruitPatch Harvest apples (Apple x1) and Workbench Mill flour/Bake bread/Press cider via E Area3D; E at field yields wheat_grain not canned, E at workbench with wheat_grain x2 yields flour and Bake->bread 42 and apple x2->cider thirst 38; Rural shells 2-6 low-rise plus Stove/Bed hearth plus Well/Forage true hedgerow without seam cracks at + and - and -Z. PNG+log under .hermes/autopilot/reports/SPEC-C011-windowed.* referenced; ARCHITECTURE.md/DEVELOPMENT.md updated for ACTIVE-only Workbench, wheat_grain distinct, streaming pacing 1-per-frame + freed-Zombie guard, WORLD-CONTRACT §20, hedgerow dedup. Folding C001-C010 deferred docs.

## P5.4 Rural Granary & Provisioning Cache — delta from P5.3 (this cycle)

- What --biometest and --ruraltest now prove: same-seed rural manifests including rural_granaries center/pos/aabb/yaw/building_id/settlement_id/capacity 8 + hardened rural_workbenches aabb_gap >=8.0 explicit and pos.length()>=350 + CropPatch distinct wheat->wheat_grain barley->barley_grain + FruitPatch + GranaryChest persistent capacity 8 Store/Take vs Workbench Mill/Bake/Press byte-identical shuffled incl. negative coords, different seed differs >=3/9 and >=30%, 1 granary per village barn 0-1 per hamlet 30% via unit_float, footprint 1.2x0.6x0.8 terrain+0.04 aabb gap >=8 between granaries >=1 workbench >=6 well/forage >=8 building >=3 road never floodplain/water/cliff/urban 350, CropPatch is_grown planted+2 distinct, Granary monitorable==ACTIVE persistent Take>Store capacity 8, Workbench hardened, ItemDB wheat_grain 12 barley_grain 10 flour 14 bread 42 cider thirst 38. Geographic validity holds for canonical +4 seeds via real WorldConstants thresholds, parcel contiguity 200-600m hedged tilled+canopy blocks not speckle with at least 2 distinct granary settlements plus 5 village workbenches hardened in 5-seed matrix and barn aabb ownership no duplication at + and - and -Z. ChunkManager streams rural shells+interiors+wells+forage+hearth+workbench hardened + granary plus distinct CropPatch wheat_grain/barley_grain with city+terrain+water+biome+road without duplication: 3x3 ACTIVE around village barn claims active rural <=9 and granaries/workbenches active counts monitorable only ACTIVE, walking 480m beyond UNLOAD_RADIUS unloads and returning regenerates identical rural+granary+hardened workbench manifests and debug stats contain t_rural_gen/mat and granaries/workbenches, generated rural/granary/workbench geometry not in save_state except deltas.granaries capacity 8 with items and deltas.field_crops with wheat_grain contents re-applied before materialize, regrow 2d plus is_grown planted+2 proven via GameClock.advance mock; Granary persistent Store wheat_grain x1 -> Chest 1/8 Take bread x1 <- Chest survives 480m walk plus save_state patch size check and Workbench hardened aabb_gap explicit. Determinism preserved GENERATOR_VERSION stays 2 additive, WorldPlan pure, CityPlan IDs and TerrainPlan 17x17 and hydrology CX/width 530-710/72/38-50 and settlement roads 700/420/220+1.8r 4-8 gates MST is_bridge and rural 1-6 >=8 aabb >=4.0 unchanged harden proven — proved by --citytest + --terrainmaterialtest + --hydrotest + --roadtest + --biometest each 0 failures with retained seams including hardened aabb_gap pos.length(). Existing budgets not weakened: --citytest, --terrainmaterialtest, --hydrotest, --biometest, --roadtest, --ruraltest, --cityruntime, --walkthrough, --havoctest, --smoke each 0 failures (3221225477 with marker not failure; ObjectDB inside_tree guards extended to GranaryChest/Workbench hardened); closed leaf blocks, FoodCrate/Well/ForagePatch/Stove/Bed/CropPatch/FruitPatch/Workbench hardened/GranaryChest prompts update and depleted/monitorable ACTIVE-only, CropPatch Harvest wheat distinct, FruitPatch apple/plum gated planted+3, Workbench Mill/Bake/Press hardened, Granary Store/Take capacity 8 persistent via deltas.granaries and monitorable false when warm, walkthrough climbs 5 storeys, havoctest honest, 54 peak field/orchard/granary/workbench Area3D not counted t_* within 12ms plus hardened aabb_gap pos.length().

- Streaming pacing: MAX_MATERIALIZATIONS_PER_FRAME 1 with early _collect_finished_jobs(pc) + freed-Zombie Variant guard (is_instance_valid && !is_queued_for_deletion() && is_inside_tree() before global_transform) — intentional pacing fix from 2a423d9 documented here not reverted; keep MAX_INFLIGHT 6. GranaryChest Area3D monitorable ACTIVE-only persistent 8 and hardened workbench aabb_gap 8.0 pos.length>=350, unified rural shell+well 1 collider peak 54, forage/hearth/granary/workbench Area3D monitorable false when warm.

- run_suite timeout: --biometest 300 / --ruraltest 400 / --roadtest 400 / --hydrotest 300 / --citytest 400 / --terrainmaterialtest 300 / --cityruntime 300 / --walkthrough 360 / --havoctest 240 / --smoke 180 (document 450 guidance for slower CI for citytest/roadtest/ruraltest). Windows exit 3221225477 with marker not failure; ObjectDB inside_tree guards extended to GranaryChest/Workbench hardened path (main.gd + probe harnesses including rural_test GranaryChest disable guarded plus Well/Forage/Stove/Bed/CropPatch/FruitPatch/Workbench hardened pathway).

- Manual proof: normal windowed CITY run (1200x720) shows WASD/E door closed blocks/open clears without RID exclusion/swung collidable, stair to roof camera following, F3 overlay with active rural 280/210 480/360 plus t_rural_gen/mat and rural_workbenches/granaries alongside active biome 81/128+96/64+12 plus t_biome_gen/mat and field_parcels/field_crops/orchard_parcels/fruit_patches active road/water/terrain, then 600-900m east along rural road corridor to river valley showing continuous tilled parcels 0.04 wheat/barley furrow bounded true hedgerow 0.45-0.75m plus orchard canopy rows 3-4 rows 3-5 trees trunk 0.35x1.8+canopy at 0.04 200-600m blocks plus village barn workbench Box 1.2x0.9x0.6 7a6a5a at 0.04 inside barn with CropPatch Harvest wheat (Wheat Grain x1) and FruitPatch Harvest apples (Apple x1) and Workbench Mill flour/Bake bread/Press cider via E Area3D and Granary Store grain/Take bread via E Area3D; E at field yields wheat_grain not canned, E at workbench with wheat_grain x2 yields flour and Bake->bread 42 and apple x2->cider thirst 38, E at granary Store bread -> Chest 1/8 persists after 480m unload/return Take bread gives bread hunger 42; Rural shells 2-6 low-rise plus Stove/Bed hearth plus Well/Forage true hedgerow without seam cracks at + and - and -Z plus hardened aabb_gap 8.0 pos.length>=350. PNG+log under .hermes/autopilot/reports/SPEC-C012-windowed.* referenced; ARCHITECTURE.md/DEVELOPMENT.md updated for ACTIVE-only GranaryChest persistent 8 and hardened workbench and streaming pacing 1-per-frame + freed-Zombie guard, WORLD-CONTRACT §21, hedgerow dedup. Folding C001-C011 deferred docs.

## G8 M1 Underground Entrance Foundation — delta from P5.4 (this cycle)

- What --cavetest now proves: same-seed cave_entrances + cave_entrances_in byte-identical shuffled including negative coords, different seed materially differs (≥3/9 probes differ + ≥30% placements differ), 0-1 per 256 m cell where quarry-suitable limestone/slope≥28/cliff, spacing ≥32, road ≥4 not bridge, water/floodplain/cliff gates via real WorldConstants for canonical +4 seeds, center ownership no duplication at +/-/-Z, at least 3 entrances in 5-seed world transect, no entrance inside URBAN_INNER_M 350. Materialization budgets: manifests byte-identical shuffled, each chunk ≤1 entrance ≤24 verts/12 tris 0 collider (Area3D only), no duplication at shared borders, at least 1 resident cave chunk around quarry transect with entrance vocab cave_entrance, unified 54 peak not 63 (cave Area3D not counted, verifier scans get_nodes_in_group("cave_chunk") body count vs rural_colliders). Streaming & telemetry: ChunkManager streams cave entrances with city+terrain+water+biome+road+rural without duplication: 3x3 ACTIVE around quarry claims active cave ≤3 (sparse), walking 480 m beyond UNLOAD_RADIUS unloads cave chunks and returning regenerates identical manifests (center/pos/yaw/kind), debug_lines() contains t_cave_gen|t_cave_mat and cave verts|tris|colliders, t_cave_gen/mat within FRAME_BUDGET_MS 12 (cave slice ≤3 ms), ChunkManager pacing 1-per-frame + freed-Zombie guard remains 0 failures. Persistence & compatibility: save_state() excludes generated cave geometry, only deltas.cave_discovered persists, deterministic re-derive on load with per-chunk deltas re-applied before materialize keeps discovered flag, GENERATOR_VERSION stays 2 additive, WorldPlan pure, CityPlan IDs / Terrain 17x17 / hydrology CX etc unchanged proved by --citytest + --terrainmaterialtest + --hydrotest + --biometest + --roadtest + --ruraltest each 0 failures with retained seams. Existing budgets not weakened: all gates finished with 0 failures, guards extended to CavePortal, walkthrough climbs 5 storeys, cave portal "Enter cave" prompt monitorable ACTIVE-only.

- WorldConstants: added cave constants as above, vocab cave_entrance, footprint 3.6×3.6 height 2.2 color 5a4a3a, caps 1/24/12, spacing 32, road 4, water 11, urban 350, slope 28, quarry 0.72. WorldSeed: added CAVE_ENTRANCE_DOMAINS ordered.

- CavePlan pure: deterministic 0-1 per 256 cell as above, 6 attempts, yaw via cave_entrance_yaw, spacing greedy by id, byte-identical.

- UndergroundChunkBuilder: per-chunk manifest as above, materialize CaveMesh + CavePortal Area3D.

- ChunkManager: added cave totals/stats/debug_lines as above, ACTIVE-only portal, save_state deltas.cave_discovered, pacing 1-per-frame + freed-Zombie guard extended to CavePortal.

- Tests: python tools/run_suite.py --cavetest 400 (or --ruraltest 400 extended) plus other gates each 0 failures. Windowed proof .hermes/autopilot/reports/SPEC-CAVE-windowed.* shows F3 overlay cave verts etc and entrance box 5a4a3a at terrain+0.01 portal prompt visible. Hedgerow true-mesh 2.0x0.45-0.75x0.4 not stretched, field/orchard hedgerow+canopy share 48, tilled 0.04, cave 0.01, unified 54.

- Streaming pacing retained: MAX_MATERIALIZATIONS_PER_FRAME 1 with early _collect_finished_jobs(pc) before streaming recalc + Variant freed-Zombie guard is_instance_valid && !is_queued_for_deletion() && is_inside_tree() before global_transform documented verbatim in streaming section, not just commit message.

- run_suite timeout: --cavetest 400 / --citytest 400 / --roadtest 400 / --ruraltest 400 / --hydrotest 300 / --biometest 300 / --terrainmaterialtest 300 / --cityruntime 300 / --walkthrough 360 / --havoctest 240 / --smoke 180 (document 450 guidance for slower CI). Windows exit 3221225477 with marker not failure; ObjectDB inside_tree guards extended to CavePortal path (main.gd + probe harnesses).

## G8 M2 Industrial Corridor — delta from G8 M1 (this cycle)

- What --biometest now proves: same-seed biome_at(p) and biome_at via BiomePlan/WorldPlan byte-identical shuffled including negative coords, different seed materially differs (≥3/9 probes differ + ≥30% placements differ OR biome vocab differs), industrial_corridor occurs only where quarry_suitability>0.52 and strata in {limestone,sandstone,granite_like} and distance_to_road<80 and slope<22 not cliff/water/floodplain/urban 350 and industrial_density>0.48, at least 2 distinct industrial belts (200-600m) in 5-seed world transect, no entrance inside URBAN_INNER_M 350, industrial not inside water/floodplain/cliff. Materialization budgets & seams: manifests byte-identical shuffled, each chunk 81 verts / <=128 tris 0/1 collider (industrial ground 0 collider like field, forest keeps 1), no duplication at shared borders (shared-edge biome agreement >=7/9 at + and - and -Z), at least 9 resident biome chunks around industrial+road transect with at least 3 industrial_corridor chunks, unified 54 peak not 63 (industrial Area3D/MultiMesh not counted, verifier scans get_nodes_in_group("biome_chunk") body count vs biome_colliders), biome_instances <=48 per chunk (industrial slag <=6 of 48, field hedgerow 8 orchard 6 canopy 12 share). Streaming & telemetry: ChunkManager streams biome overlay with industrial palette alongside city+terrain+water+road+rural+cave without duplication: 3x3 ACTIVE around industrial corridor claims active biome <=9 (industrial included), walking 480m beyond UNLOAD_RADIUS unloads biome chunks and returning regenerates identical manifests (center/pos/biome_ids/colors/verts/tris/colliders/instances), debug_lines() contains t_biome_gen|t_biome_mat and biome verts|tris|colliders|instances, t_biome_gen/mat within FRAME_BUDGET_MS 12 (industrial slice <=2ms added), pacing 1-per-frame + freed-Zombie guard remains 0 failures. Persistence & compatibility: save_state() excludes generated biome/industrial geometry (only deltas sibling pattern), deterministic re-derive on load, GENERATOR_VERSION stays 2 additive, WorldPlan pure, CityPlan IDs / Terrain 17x17 / hydrology CX etc unchanged proved by --citytest + --terrainmaterialtest + --hydrotest + --biometest + --roadtest + --ruraltest + --cavetest each 0 failures with retained seams, cave portal "Enter cave" still monitorable. Existing budgets not weakened: --citytest, --terrainmaterialtest, --hydrotest, --biometest, --roadtest, --ruraltest, --cavetest, --cityruntime, --walkthrough, --havoctest, --smoke each finished with 0 failure(s) (3221225477 with marker not failure; guards extended), closed door leaf blocks/open clears without RID exclusion, walkthrough climbs 5 storeys no teleports, cave portal prompt still ACTIVE-only. Player-facing proof archived: one normal windowed CITY run log+PNG under .hermes/autopilot/reports/SPEC-INDUSTRIAL-windowed.* shows F3 overlay biome verts|tris|colliders|instances|field_parcels|...|industrial_corridor alongside active road/water/terrain/rural/cave, then 600-900m to industrial belt near road intersection (x~800-1400, road distance <80, quarry_suitability >0.52, slope <22) with contaminated ground palette 7a6a6a visible at terrain+0.03 and slag tint 5e5850, no seam cracks, referenced in WORLD-CONTRACT §23, ARCHITECTURE.md, DEVELOPMENT.md updated.

- WorldConstants: BIOME_VOCAB extended with &"industrial_corridor", INDUSTRIAL_CORRIDOR_VOCAB, INDUSTRIAL_ROAD_DISTANCE_MAX 80.0, INDUSTRIAL_QUARRY_SUITABILITY_MIN 0.52, INDUSTRIAL_SLOPE_MAX_DEG 22.0, INDUSTRIAL_MIN_PARCEL_M 48.0, INDUSTRIAL_CORRIDOR_LIFT_M 0.03, COL_INDUSTRIAL_CORRIDOR 7a6a6a COL_INDUSTRIAL_DARK 5e5850, INDUSTRIAL_PALETTE_VARIANT 0.08, INDUSTRIAL_ROAD_SETBACK 0.0, INDUSTRIAL_BUILDING_GAP 4.0, MAX_INDUSTRIAL_INSTANCES 6, INDUSTRIAL_CORRIDOR_DENSITY_CELL 480, INDUSTRIAL_CORRIDOR_DENSITY_THRESHOLD 0.48. WorldSeed: INDUSTRIAL_CORRIDOR_DOMAINS ordered.

- BiomePlan pure: deterministic 0-1 per 256? No, per-sample predicate as above, 200-600m belts not speckles, is_industrial helper, surface_tint_at industrial lerp.

- BiomeChunkBuilder: per-chunk manifest as above, materialize CaveMesh + etc. ChunkManager: added industrial handling via biome overlay, ACTIVE-only, save_state deltas, pacing 1-per-frame + freed-Zombie guard extended to industrial via biome.

- Tests: python tools/run_suite.py --biometest 300 (extended to prove industrial gates, vocab, contiguity 200-600m belts not speckles, 9 resident biome chunks with industrial) plus --citytest 400, --terrainmaterialtest 300, --hydrotest 300, --roadtest 400, --ruraltest 400, --cavetest 400, --cityruntime 300, --walkthrough 360, --havoctest 240, --smoke 180 each 0 failures. Windowed proof .hermes/autopilot/reports/SPEC-INDUSTRIAL-windowed.* shows F3 overlay industrial etc. Hedgerow true-mesh 2.0x0.45-0.75x0.4 not stretched, field/orchard hedgerow+canopy share 48, tilled 0.04, industrial 0.03, unified 54.

- Streaming pacing retained: MAX_MATERIALIZATIONS_PER_FRAME 1 with early _collect_finished_jobs(pc) before streaming recalc + Variant freed-Zombie guard is_instance_valid && !is_queued_for_deletion() && is_inside_tree() before global_transform documented verbatim in streaming section, not just commit message.

- run_suite timeout: --biometest 300 / --citytest 400 / --roadtest 400 / --ruraltest 400 / --cavetest 400 / --hydrotest 300 / --terrainmaterialtest 300 / --cityruntime 300 / --walkthrough 360 / --havoctest 240 / --smoke 180 (document 450-500 guidance for slower CI due to biometest 500-540 actual on this hardware, 3221225477 with marker not failure; ObjectDB inside_tree guards extended to industrial via biome).


## G8 M4 Vertical Network Prototype — delta from G8 M2 (this cycle)

- What --verticaltest now proves: same-seed vertical_bridges + vertical_bridges_in byte-identical shuffled including negative coords, different seed materially differs (≥3/9 probes differ + ≥30% placements differ), 0-1 per 256 m cell where 2 barn/stable within 8-14, spacing ≥16, road ≥2 not bridge, water/floodplain/cliff gates via real WorldConstants for canonical +4 seeds, center ownership no duplication at +/-/-Z, at least 3 bridges in 5-seed rural transect, no bridge inside URBAN_INNER_M 350, no bridge inside water/floodplain/cliff, ledge_y = building_ground_y + height +1.2 within 0.1. Materialization budgets & seams: manifests byte-identical shuffled, each chunk ≤1 bridge ≤24 verts/12 tris 0 collider (Area3D only), no duplication at shared borders, at least 1 resident vertical chunks around hamlet barn pair transect with bridge vocab roof_bridge, unified 54 peak not 63 (vertical Area3D not counted, verifier scans get_nodes_in_group("vertical_chunk") body count 0), t_vertical_gen/mat within FRAME_BUDGET_MS 12 (slice ≤3 ms). Streaming & telemetry: ChunkManager streams vertical bridges with city+terrain+water+biome+road+rural+cave without duplication: 3×3 ACTIVE around barn pair claims active vertical ≤3 (sparse), walking 480 m beyond UNLOAD_RADIUS unloads vertical chunks and returning regenerates identical manifests (center/pos/yaw/span/ledge_y), debug_lines() contains t_vertical_gen|t_vertical_mat and vertical verts|tris|colliders|bridges, pacing 1-per-frame + freed-Zombie guard remains 0 failures. Persistence & compatibility: save_state() excludes generated vertical geometry (only deltas.vertical_discovered if any, or stateless), deterministic re-derive on load with per-chunk deltas re-applied before materialize, GENERATOR_VERSION stays 2 additive, WorldPlan pure, CityPlan IDs / Terrain 17×17 / hydrology CX etc unchanged proved by --citytest + --terrainmaterialtest + --hydrotest + --biometest + --roadtest + --ruraltest + --cavetest each 0 failures with retained seams, cave portal "Enter cave" and industrial 7a6a6a still monitorable. Existing budgets not weakened: all gates finished with 0 failures, guards extended to VerticalBridge, walkthrough climbs 5 storeys, vertical bridge Area3D prompt monitorable ACTIVE-only. Player-facing proof archived: .hermes/autopilot/reports/SPEC-VERTICAL-windowed.* shows F3 overlay vertical verts etc and bridge 8b7f6e at ledge_y visible, prompt "Cross bridge" at 1.8 m, no seam cracks at 64 m borders, referenced in WORLD-CONTRACT §24, ARCHITECTURE.md G8 M4, DEVELOPMENT.md G8 M4.

- WorldConstants: added vertical constants as above, vocab roof_bridge, 8-14 span 1.2 width 0.18 thick 1.2 lift, caps 1/24/12, spacing 16, building gap 1.0 road 2 water 11 urban 350 slope 22 color 8b7f6e/6b5a4a, MAX_VERTICAL_VERTS/TRIS. WorldSeed: added VERTICAL_BRIDGE_DOMAINS ordered.

- VerticalNetworkPlan pure: deterministic 0-1 per 256 cell as above, 6 attempts, yaw via vertical_bridge_yaw, spacing greedy by id, byte-identical.

- VerticalChunkBuilder: per-chunk manifest as above, materialize VerticalMesh + VerticalBridge Area3D.

- ChunkManager: added vertical totals/stats/debug_lines as above, ACTIVE-only bridge, save_state deltas.vertical_discovered, pacing 1-per-frame + freed-Zombie guard extended to VerticalBridge.

- Tests: python tools/run_suite.py --verticaltest 400 (or --ruraltest 400 extended) plus other gates each 0 failures. Windowed proof .hermes/autopilot/reports/SPEC-VERTICAL-windowed.* shows F3 overlay vertical etc and bridge 8b7f6e at terrain+height+1.2 portal prompt visible. Hedgerow true-mesh 2.0x0.45-0.75x0.4 not stretched, field/orchard hedgerow+canopy share 48, tilled 0.04, vertical 0.01? Actually vertical at ledge_y, unified 54.

- Streaming pacing retained: MAX_MATERIALIZATIONS_PER_FRAME 1 with early _collect_finished_jobs(pc) before streaming recalc + Variant freed-Zombie guard is_instance_valid && !is_queued_for_deletion() && is_inside_tree() before global_transform documented verbatim in streaming section, not just commit message.

- run_suite timeout: --verticaltest 400 / --citytest 400 / --roadtest 400 / --ruraltest 400 / --cavetest 400 / --hydrotest 300 / --biometest 300 / --terrainmaterialtest 300 / --cityruntime 300 / --walkthrough 360 / --havoctest 240 / --smoke 180 (document 450 guidance for slower CI). Windows exit 3221225477 with marker not failure; ObjectDB inside_tree guards extended to VerticalBridge path (main.gd + probe harnesses).



## G9 M1 City Interior Program — residential ground floor 3-room semantics (bounded)

City interior program G9 M1 residential ground floor 3-4 rooms 0.18 wall 0.95 opening furniture bed/shelf/table stations ACTIVE-only 1500/2480 capped. See WORLD-CONTRACT 25 and ARCHITECTURE G9 M1 for full contract. Tests: --citytest 400 extended proves interior determinism shuffled incl negative different seed budgets 1500/2480 1 collider 9 resident city chunks with interiors unified 54 peak. Streaming pacing 1-per-frame + freed-Zombie guard. Run suite timeouts 400/300/550 etc. Windowed proof SPEC-CITY-INTERIOR-windowed PNG+log.
