# World Coordinate Contract

Authoritative contract for Ring Bell macro-world coordinates. The
single source of numeric constants is `world/generation/world_constants.gd`.

## 1. Axes and units
- Units are meters.
- X and Z are horizontal world coordinates; Y is elevation.
- `Vector2` plan points are always `(x, z)` in **world space**, never chunk-local.
  Example: `Vector2(128, -64)` means world X=128 m, world Z=-64 m.
- Chunk-local coordinates never appear in plan APIs; builders convert via `chunk_rect`.

## 2. Partition scales

| Partition | Size | Purpose |
|---|---|---|
| Simulation/render chunk | 64 m (`CHUNK_SIZE_M`) | streaming, physics, mesh batching |
| Landscape cell | 256 m (`LANDSCAPE_CELL_M`) | terrain field lattice |
| Macro cell | 1024 m (`MACRO_CELL_M`) | future settlement/biome regions |

These are coordinate partitions, not independent random worlds.

## 3. Origin and bounds
- World origin `(0,0)` is the Prague-basin reference origin (city center).
- Initial bounded playable world: 16 km x 16 km = `[-8192, 8192)` on both X and Z.
- Queries outside the boundary are legal (future expansion) but `is_buildable` rejects them and materialization outside is not part of the initial boundary.
- Edge handling is deterministic; no sea is generated in this slice.
- Example inside: `Vector2(8191, 0)` is inside; `Vector2(8192, 0)` is outside.

## 4. Vertical datums (Y, meters)

| Datum | Value | Notes |
|---|---|---|
| `terrain_y` | sampled per `(x,z)` in `[-12, 120]` | `TerrainPlan.height_at` |
| `water_level` | `-1.2 +-0.6` mean/variation along flow axis | `HydrologyPlan.water_level_at`; datum `WATER_LEVEL_Y=-2.0` retained as legacy reference |
| `building_ground_y` | `terrain_y` at footprint center | per-building derived |
| `lower_city_y` | -15.0 | future underground network top |
| `upper_civilization_y` | 120.0 | future upper network floor |
| `sky_dressing` | 500.0 | visual only |

Water surface Y is deterministic per world `z` via `WATER_LEVEL_MEAN + WATER_LEVEL_VAR * sample_coherent_signed(Vector2(0,z), hydro_level, 800)` and is constant across width within a chunk; tributaries subtract `TRIB_FALL_SLOPE * (Cz - z)` northward fall.

## 5. Determinism and ownership
- Plans (`CityPlan`, `TerrainPlan`, `HydrologyPlan`, `GeologyPlan`, `BiomePlan`, `SettlementPlan`, `RoadNetworkPlan`, `WorldPlan`) are **pure**: no Node/scene access, no unseeded randomness, no chunk-local state.
- Chunks (`ChunkBuilder`, `ChunkManager`, `MeshBatcher`, `TerrainChunkBuilder`, `WaterChunkBuilder`, `BiomeChunkBuilder`, `RoadChunkBuilder`) materialize plan data; only they create nodes/meshes/collision.
- Every terrain, hydrology, biome, settlement and road query is a function of explicit seed + world coordinates via `WorldSeed.sample_coherent*` or `unit_float`; chunk build order, query order, and worker scheduling cannot change results.
- Stable IDs use domain + world coordinates/region identity; `GENERATOR_VERSION` remains 2 in P2, P2.2, P3.1 and P4.1 (settlement/road additive, outside dense core).
- `WorldSeed` domain separation: terrain/ridge/valley/soil/moisture/temperature/geology/settlement/hydro/hydro_cx/hydro_phi/hydro_meander2/hydro_width/hydro_level/hydro_trib_*/biome_*/geology_*/road/settlement_*/hydro_*.

## 6. Stable IDs and versions
- Building IDs: `b_<cell>_<edge><k>` (existing CityPlan, unchanged).
- Terrain has no per-sample persistent ID; cells are identified by world coordinate.
- Hydrology bodies: `river_main` and `trib_0`, `trib_1` via `water_body_id_at`; vocabulary subset of `WorldConstants.WATER_BODIES` (`sea|lake|river|reservoir`) plus empty dry.
- Settlement anchors: `settlement_<cx>_<cy>_<k>` plus `gate_<k>` plus `crossing_<id>` for road crossing nodes; road edges `edge_<a>_<b>` (lexicographic), villages 700/hamlet 420/farmstead 220 spacing.
- Road edges: `edge_<a>_<b>` with `hierarchy` primary/secondary/track, `is_bridge` true only at `crossing_candidates`.
- `WORLD_SCHEMA_VERSION = 1`; `GENERATOR_VERSION = 2` (unchanged — terrain, hydrology, biome, settlement, road are additive, not serialized).

## 7. Setting and continental decision
- Setting: continental Prague/Czech-inspired basin. Schema may name `sea|lake|river|reservoir`, but **no sea is generated** in the initial 16 km world — the Czech basin is landlocked. P2.2 hydrology places one deterministic Vltava-like primary river plus 2 tributaries east of the historic core; sea remains explicitly excluded. Future hydrology may add lakes/reservoirs only as placeholder vocabulary in this slice.
- The river is the first landscape-scale landmark beyond buildings and constrains future farms/roads/settlements and crossing choices.

## 8. Slope, class, and buildability
- Slope is in **degrees** derived from `normal_at` / finite differences.
- `CLIFF_SLOPE_DEG = 35°` → `terrain_class = cliff`; `BUILDABLE_MAX_SLOPE_DEG = 22°`.
- Classes: `basin | rolling_hill | upland | cliff`; materials: `alluvial_soil | meadow_soil | upland_grass | rock`.
- `is_buildable` rejects out-of-bounds origins/footprints and footprints whose sampled slope exceeds the threshold (configurable via `constraints`).
- Class height thresholds: `TERRAIN_ROLLING_HEIGHT_M = 10.0` -> rolling_hill, `TERRAIN_UPLAND_HEIGHT_M = 38.0` -> upland (below 10 = basin; cliff overrides height via slope). Authoritative values live in `WorldConstants`.
- No water is placed on cliff-class slopes or ridge hilltops except an explicit lake placeholder; `HydrologyPlan` floodplain check excludes cliff samples (sample 200 random points: any `slope >= CLIFF_SLOPE_DEG` and `terrain_class==cliff` is dry).

## 9. Sampling contract
- Stateless lattice sampling with floor-based indexing and smoothstep interpolation; continuous at lattice and chunk boundaries within `SEAM_CONTINUITY_TOL_M=0.02`.
- Domain-separated (terrain, ridge, valley, soil, moisture, temperature, geology, settlement, hydro, hydro_meander2, hydro_width, hydro_level, hydro_trib_*, biome_*, geology_*, road_*, settlement_*); no RNG sharing between queries; never uses `String.hash()`, time, scene state, or visit order.
- Negative world coordinates are fully supported via `floori` lattice indexing.
- Settlement/road domains: `settlement`, `settlement_field`, `settlement_jitter`, `settlement_gate_phi`, `road`, `road_mid`, `road_width`, `road_hierarchy` etc., seed-separated from terrain/hydro/geology/biome.

## 10. Terrain materialization (P2-TERRAIN-MATERIALIZATION)
- Chunk terrain manifest is 17x17 height samples per 64 m chunk (4 m spacing), world-space shared edges, deterministic for `(seed, coord)` via `TerrainChunkBuilder.build_manifest(WorldPlan, coord)`.
- Heights sampled from `TerrainPlan.height_at` then urban-compatibility masked; normals finite normalized; class/material per sample from masked height+slope (`basin/rolling_hill/upland/cliff` -> `alluvial_soil/meadow_soil/upland_grass/rock`, cliff forces rock).
- Manifest keys: `coord, origin, size, resolution=17, heights, normals, material_ids, class_ids, colors, indices, compatibility_mode, terrain_vertices/triangles/colliders/material_samples/gen_ms`.
- Materialization (`materialize(parent, manifest)`) creates one `TerrainMesh` + one `TerrainBody/ConcavePolygonShape` per chunk under `Terrain_X_Y`; mesh and collision share same X/Z extent.
- Collision budget: at most 1 collider per chunk; active 3x3 ring = 9 terrain colliders max. Vertices 289 / chunk, triangles 512 / chunk.
- Urban compatibility rule: deterministic radial mask centered at world origin — `URBAN_INNER_M=350` flat (height 0), transition `URBAN_OUTER_M=600` smoothstep to real terrain; owned by `TerrainChunkBuilder`; does not mutate `CityPlan`. Terrain height at any world point is `lerp(0, raw_height, smoothstep((d-350)/250))`.
- Ground ownership: flat city ground (`ChunkBuilder._ground`) is emitted only where the chunk rect is wholly inside `URBAN_INNER_M` (every corner distance < 350); any chunk that straddles the radius or lies beyond 350 emits no city flat box and is terrain-only, preventing any competing flat/terrain surfaces at the boundary. Beyond 600 m terrain is at full height.
- Terrain generation runs in the worker-safe data phase (`ChunkManager._thread_build`) with private `WorldPlan`; `terrain_gen_ms` measured inside worker, `terrain_mat_ms` on main thread; stats exposed via `ChunkManager` debug lines (`t_gen`, `t_mat`, `active terrain` filtered to ACTIVE state).
- Generator version remains 2; terrain is additive, not serialized in saves.

## 11. Hydrology slice P2.2 (Vltava-anchored river corridor)

### 11.1 Corridor, meander, width, level
- Primary corridor is deterministic from seed: mean center `CX = HYDRO_CORRIDOR_CX_MEAN 620 + S*HYDRO_CORRIDOR_JITTER 90` where `S = unit_float("hydro_cx")*2-1` so `CX 530-710 m` east of origin, clearly outside `URBAN_OUTER_M=600` for most length and never revisiting the spawn plaza.
- Meander `MX(z) = HYDRO_MEANDER_AMPL 72 * sin(z/HYDRO_MEANDER_WAVELENGTH 1350 + phi) + HYDRO_MEANDER2_AMPL 18 * sample_coherent_signed(Vector2(0,z), hydro_meander2, HYDRO_MEANDER2_CELL 600)`. `phi` seeded via `unit_float("hydro_phi") * TAU`. Keeps the river readable, out of the historic core, continuous across every 64 m chunk seam within `SEAM_CONTINUITY_TOL_M`.
- Width varies smoothly: `38 + 12 * sample_coherent(Vector2(0,z), hydro_width, HYDRO_WIDTH_CELL 900)` → `RIVER_WIDTH_MIN 38` to `RIVER_WIDTH_MAX 50` full width (19-25 half). Entrusted to `WorldConstants`.
- Water level `WATER_LEVEL_MEAN -1.2 +- WATER_LEVEL_VAR 0.6` along flow axis via `sample_coherent_signed(Vector2(0,z), hydro_level, WATER_LEVEL_CELL 800)`; constant across width within a chunk.
- Valley alignment: `HydrologyPlan` owns the river; `TerrainPlan` remains unchanged this cycle (valley depression at `x=-180` stays as small decorative bias, not forced to match the new `CX~620` corridor). `WaterChunkBuilder` visually spans banks/floodplain over existing terrain height; a full terrain trench carve through the basin is explicitly deferred.

### 11.2 Tributaries, banks, floodplain, flow
- `TRIB_COUNT = 2` seeded streams. Each anchor `Ax` outside corridor `CX ± (TRIB_ANCHOR_BASE_OFFSET 260 ± TRIB_ANCHOR_JITTER 80)`, upstream `Az ~ TRIB_UPSTREAM_BASE_Z -2200 + k*TRIB_UPSTREAM_STEP_Z 1400` plus seeded jitter `TRIB_UPSTREAM_JITTER_Z 320`, confluence `Cz` deterministically where the anchor line approaches the primary within 180 m (`Az + 900 +-200` and `cx = river_center_x_at(Cz)`). Centerline is a quadratic bezier anchor→mid→confluence, width `TRIBUTARY_WIDTH_MIN 14` to `TRIBUTARY_WIDTH_MAX 22`, mid jittered `+-30`. Water level inherits primary level minus `TRIB_FALL_SLOPE 0.015 * (Cz - z)` northward fall.
- Banks `BANK_W 9.0`, floodplain `FLOODPLAIN_W 26.0` (authoritative in `WorldConstants`). `is_floodplain(p)` when `abs(dist_to_center) in (half+BANK_W, half+BANK_W+FLOOD_W]` and `terrain_class != cliff`. Bank/floodplain distances are monotonic from center.
- Flow `flow_direction_at(p)` is unit length, northward `Vector2.y > 0.35` on the primary and `dot(tributary_flow, direction_to_confluence) > 0.45` for tributaries, using real `WorldConstants` thresholds.
- Sea is explicitly not generated in this continental basin slice; `WATER_BODIES` vocabulary remains `sea|lake|river|reservoir` but only `river`, `tributary`, and empty are materialized (lake placeholder allowed as vocabulary check only).
- No in-city river carving, no road bridges, no lakes/reservoirs beyond placeholder, no underground water, and no terrain-trench rework inside the 350 m historic core in this slice — all deferred.

### 11.3 Water chunk manifest and materialization
- Per-chunk manifest via `WaterChunkBuilder.build_manifest(WorldPlan, coord)` at **9x9 resolution** (81 samples at 8 m spacing over the 64 m rect; justified low-cost choice documented versus alternative 17x17 289/512). Keys: `coord, origin, size, resolution=9, heights (water Y per sample, NAN if dry), normals, material_ids (river|tributary|""), class_ids (water|dry), colors, indices, is_wet, water_vertices, water_triangles, water_colliders, has_water, water_gen_ms`. Deterministic for `(seed, coord)` and byte-identical across shuffled builds.
- Sampling uses world-space shared edges matching terrain's world-space edge handling so adjacent chunks agree on the centerline sampling within `SEAM_CONTINUITY_TOL_M 0.02` at both positive and negative boundaries.
- Visual `ArrayMesh` per wet chunk: vertices at `(x, water_y, z)` interpolated bilinearly across the wet polygon, clipped to chunk rect (water outside rect omitted). Vertex colors via `WorldConstants` water palette (muted Vltava teal `4a7a94`, tributary dark `3a5a74`). Indices share the same winding rule as terrain; bank ribbon along water edge is a 1.5 m visual color transition (no extra collider) to hide Z-fighting.
- Collision budget: at most **one `ConcavePolygonShape3D` per wet chunk** (`backface_collision=true`, on `environment` layer 1, `collision_mask 0`), `0` if dry. No per-sample or per-triangle bodies, no per-waypoint volumes. Wet chunk budget: **81 verts / <=128 tris / 1 collider**; dry chunk `0/0/0`. `water_mat_ms` measured per chunk on the main thread. ACTIVE 3x3 ring carries at most **9 active water colliders**.
- Urban compatibility: water manifests are generated for all chunks, but the primary corridor at `>530 m` keeps the spawn chunk `0,0` dry; no building footprint overlap check is required this slice. If any future in-city water would intersect a city block, that handling is explicitly deferred.

### 11.4 Streaming, telemetry, persistence, compatibility
- `ChunkManager` mirrors the terrain pipeline: new counters `_water_vertices_total/_water_triangles_total/_water_colliders_total/_water_mat_ms_total`, per-chunk record fields `water_vertices/water_triangles/water_colliders/water_manifest/layers_water`, stats keys `water_vertices/water_triangles/water_colliders` and timings `t_water_gen/t_water_mat` (measured inside worker via private `WorldPlan` and on main thread respectively). `holder["water"]` and `water_gen_ms` are measured inside `_thread_build`; `materialize` creates `Water_X_Y/WaterMesh/WaterBody` under `Chunk_X_Y` and measures `water_mat_ms`.
- ACTIVE-ring rule: water collider counted toward `active water` only when chunk state ACTIVE, analogous to terrain; warm retains visual `WaterMesh` but collision is disabled (`collision_layer=0`) if builder chooses active-only (documented as intentional budgeted optimization). At most 9 active water colliders (3x3).
- `ChunkManager.save_state()` persists only `seed/version/discovery/deltas` — never generated water geometry, vertices, or collision shapes. Hydrology manifests are deterministic from `seed+coord` and survive unload/reload of water chunks (same vertex/triangle/collider counts after regeneration). `WorldPlan` remains pure, `CityPlan` deterministic IDs unchanged, `GENERATOR_VERSION` stays 2; old saves follow the existing mismatch warning + regeneration path and never reinterpret water delta keys.
- `compatibility_mode` string is present on terrain manifests; water manifests carry `resolution` and `has_water` for forward compatibility. `WORLD_SCHEMA_VERSION` may be noted but `GENERATOR_VERSION` stays 2. No new autoload, project setting, or large asset import is authorized beyond the hydrology constants and water streaming stats already described.
- Telemetry: `ChunkManager.debug_lines()` exposes `water verts | tris | colliders | t_water_gen | t_water_mat | active water (warm)` alongside terrain and city lines; per-chunk `water_gen_ms`/`water_mat_ms` are reported and budgeted within `FRAME_BUDGET_MS 12.0` combined with city/terrain.
- Bank ribbon choice: 1.5 m earth bank ribbon remains a vertex-color transition without extra geometry as the budgeted choice for this slice, keeping 81 verts / <=128 tris / 1 collider wet-chunk budget. A narrow earth-geometry strip will be evaluated when/if terrain trench carve through the historic basin is designed (deferred truthfully, not silently omitted).

## 12. Biome & Geology slice P3.1 (Czech rural mosaic)

### 12.1 Vocab and constants (authoritative in `WorldConstants`)
- Biome vocab `BIOME_VOCAB`: `urban_basin`, `river_floodplain`, `wet_meadow`, `arable_field`, `pasture_orchard`, `pasture`, `orchard`, `deciduous_forest`, `mixed_upland_forest`, `rocky_quarry` (subset enforced).
- Geology strata vocab `GEOLOGY_STRATA_VOCAB`: `alluvial`, `loess`, `limestone`, `sandstone`, `granite_like`; soil vocab `GEOLOGY_SOIL_VOCAB`: `alluvial_soil`, `loess_soil`, `limestone_soil`, `sandstone_soil`, `granite_soil`.
- Cell sizes: `GEOLOGY_CELL 700`, `GEOLOGY_RIDGE_CELL 380`, `SOIL_CELL 220`, `BIOME_MOISTURE_CELL 360`, `BIOME_TEMP_CELL 520`, `BIOME_FOREST_FIELD_CELL 420`, `BIOME_FIELD_EDGE_CELL 180`, `BIOME_ORCHARD_CELL 300`, `BIOME_DENSITY_CELL 180`, `LANDSCAPE_CELL_M 256`, `MACRO_CELL_M 1024`.
- Thresholds: `BIOME_MOISTURE_WET_MEADOW_THRESHOLD 0.62`, `BIOME_FOREST_FIELD_THRESHOLD 0.52`, `BIOME_FERTILITY_ARABLE_MIN 0.55`, `BIOME_FERTILITY_PASTURE_MIN 0.42`, `BIOME_DENSITY_FOREST_MIN 0.48`, `BIOME_ORCHARD_THRESHOLD 0.55`, `QUARRY_SUITABILITY_THRESHOLD 0.72`, `QUARRY_SLOPE_MIN_DEG 28`, `ARABLE_MAX_SLOPE_DEG 12`, `PASTURE_MAX_SLOPE_DEG 14`, `BUILDABLE_MAX_SLOPE_DEG 22`, `CLIFF_SLOPE_DEG 35`, `TERRAIN_UPLAND_HEIGHT_M 38`.
- Budgets: overlay `BIOME_OVERLAY_RESOLUTION 9` -> `81 verts / <=128 tris` per chunk (9x9 at 8 m), lift `BIOME_OVERLAY_LIFT_M 0.03`, instance caps `BIOME_INSTANCE_CAP_FOREST 48` / `FIELD_EDGE 12` / `QUARRY 6` with global `MAX_BIOME_INSTANCES_PER_CHUNK 48` and at most 1 biome collider per chunk (0 if no forest/quarry), at most 9 active biome colliders (3x3), `FRAME_BUDGET_MS 12`.

### 12.2 Generation contract (macro 256 m contiguity, hydrology/slope/geology gates)
- GeologyPlan pure: strata, soil, quarry_suitability high on limestone/sandstone uplands and steep/cliff masks via high-frequency 48 m geology noise, fertility high on loess/alluvial, cave_potential deterministic. Bands are sample_coherent-driven not hard-coded per-chunk art; handles negative coords via floori; GENERATOR_VERSION stays 2.
- BiomePlan pure Czech mosaic reading TerrainPlan + HydrologyPlan + GeologyPlan plus moisture/temperature/density fields. Micro rules override macro: water/bank/floodplain -> river_floodplain, 0-16 m outside floodplain where moisture>0.62 and slope<22 and not cliff -> wet_meadow, within URBAN_INNER_M 350 -> urban_basin, quarry_suitability>0.72 and (slope>=28 or cliff or limestone upland >=15) -> rocky_quarry. Macro: forest_field blended with landscape tendency 256 m for contiguity >=256 m; forest_field>0.52 on suitable slope<35 biases forest belts, else fields/pasture. Mixed only on upland >=38. Contiguity rule ensures parcels >=1 landscape cell, transitions at most one-sample wide at 4-8 m; 40 probes along 512 m show run-length >=192 m with no speckling.

### 12.3 Biome chunk manifest and materialization (9x9 vs 17x17 justification)
- Per-chunk manifest via BiomeChunkBuilder.build_manifest at 9x9 resolution (81 samples at 8 m). Keys: coord, origin, size, resolution=9, biome_ids, material_ids, class_ids, colors, heights (terrain+0.03), indices, biome_gen_ms, density, instance_count, instances, has_forest, has_field, has_quarry, is_wet_margin, biome_vertices, biome_triangles, biome_colliders, biome_nodes. Deterministic byte-identical across shuffled builds. World-space shared edges, vocab agreement >=7/9.
- Visual ground overlay: vertices at (x, terrain_height+0.03, z), vertex colors = surface_tint_at, StandardMaterial vertex_color_use_as_albedo. 81/128 budgets. Instanced dressing: forest up to 1 tree per cell where density>0.48 capped 48/12, field hedgerow 1-2 Box proxies max 12, quarry 2-6 Box proxies per quarry chunk. Total <=48. At most one biome collider per chunk, ACTIVE-only, warm retains visuals but disables BiomeBody collision_layer 0. Clipping via footprint center, no duplication.

### 12.4 Streaming, telemetry, persistence, compatibility
- ChunkManager mirrors terrain/water pipeline: new counters biome verts/tris/colliders/instances and biome_gen/mat ms, holder biome and biome_gen_ms in thread_build, materialize creates Biome_X_Y/BiomeMesh/BiomeBody/BiomeMultimesh. ACTIVE-only biome physics, at most 9 active biome colliders (3x3). Per-chunk biome_gen_ms worker + biome_mat_ms main within FRAME_BUDGET_MS 12. Debug lines expose biome verts/tris/colliders/instances t_biome_gen/t_biome_mat active biome (warm). Save_state persists only seed/version/discovery/deltas never biome geometry. Generator version stays 2 additive outside dense core, urban_basin 0 instances, no trench carve, design folding C001/C002 deferred docs.

## 13. Settlement & Road slice P4.1 (deterministic anchors and hierarchical road/bridge corridors)

### 13.1 Vocab and constants (authoritative in `WorldConstants`)
- Settlement vocab `SETTLEMENT_VOCAB`: `village`, `hamlet`, `farmstead`, `isolated_farm`, `town` (town sparingly at arterial intersections, never inside `URBAN_INNER_M`).
- Road hierarchy vocab `ROAD_HIERARCHY_VOCAB`: `primary`, `secondary`, `track`.
- Site radii: village `48-90`, hamlet `26-46`, farmstead `16-28` (deterministic via `unit_float("settlement_jitter")`).
- Spacing: `SETTLEMENT_SPACING_VILLAGE 700`, `HAMLET 420`, `FARMSTEAD 220` plus `1.8 * radius` to any stronger accepted anchor, yielding `12-36` total anchors in `[-3072,3072)` playable annulus plus scattered outliers, deterministic and not per-chunk speckles (macro-cell `1024` / landscape `256` spacing, verified via >8 macro cells).
- Gates: `4-8` deterministic `city_gate` nodes on ring `URBAN_OUTER_M +-60` plus inner ring `URBAN_INNER_M+80` optionally, scored as hamlet, kept 4-8 strongest; each gate center `Vector2(cos(phi),sin(phi))*radius`, `radius 18`, `id gate_<k>`.
- Road widths: `ROAD_WIDTH_PRIMARY 7.0`, `SECONDARY 5.0`, `TRACK 3.5`; lifts: `ROAD_LIFT_M 0.04`, `BRIDGE_DECK_LIFT_M 0.35`, `BRIDGE_WIDTH_EXTRA 0.6`; sampling every `12 m`; budgets: road ribbon `<=96 verts / <=64 tris` typical, junction `<=160/96`, `MAX_ROAD_VERTS/TRIS_PER_CHUNK 160/96`, `MAX_ROAD_SEGMENTS_PER_CHUNK 8`, `MAX_ACTIVE_ROAD_COLLIDERS 9`; `FRAME_BUDGET_MS 12`.
- Colors (vertex-colored, no texture import): primary `7a7a78` paved grey, secondary `8b7f6e` gravel tan, track `6e5d4b` dirt brown, bridge deck `6a6a6a` + abutment `5a4a3a`; `StandardMaterial3D vertex_color_use_as_albedo=true`.

### 13.2 Generation contract (layered macro-cell contiguous, hydrology-constrained)
- Settlement scoring uses `WorldSeed.sample_coherent` / `sample_coherent_signed` / `unit_float` with explicit settlement/road domains (`settlement`, `settlement_field`, `settlement_jitter`, `settlement_gate_phi`, `settlement_gate_radius`, `road`, `road_mid`, `road_width`, `road_hierarchy`, etc.) seed-separated from terrain/hydro/geology/biome, `floori` lattice for negative coords, `MACRO_CELL_M 1024` / `LANDSCAPE_CELL_M 256`.
- Per-macro-cell candidate generation: each `1024 m` cell places `0-2` candidates with seeded jitter via `unit_float("settlement_jitter", [cx,cy,k])`, then scored: base `sample_coherent(p, settlement_field, 520)`, slope gate `<22 hamlet/farmstead` and `<14 village` and `cliff` reject, flood gate `> BANK+FLOOD+14 village`, `> BANK+8 hamlet`, `>2 farmstead` plus `is_floodplain` rejects villages and `water_body_at != ""` rejects all, soil/fertility/strata bias (`loess/alluvial` + `fertility >0.48` farmstead, `>0.42` hamlet, `rocky_quarry` biome rejects except deferred quarry outpost), water access peak `dist 80-520` villages, spacing inhibition greedily by scored suitability with minimum separations above and `1.8*radius`, urban exclusion no anchor inside `URBAN_OUTER+180` unless gate satellite `520-880` (rare). Pure, additive, `GENERATOR_VERSION` stays 2.
- Gates: `phi_k = unit_float("settlement_gate_phi", [k]) * TAU` for `k 0..7` filtered by hamlet suitability at ring radius; keep 4-8 strongest.
- Road graph: nodes = settlement anchors + city_gates + hydrology `crossing_candidates` that roads elect to use (each `crossing_candidates` within `480` of any settlement/gate kept if edge would need to cross its water, at most 9 strongest; each becomes `kind crossing` node). Edges = deterministic MST over all non-crossing nodes using world distance weighted by `1 + 0.25*slope_penalty + 0.30*water_penalty + 0.10*forest_penalty` (slope sampled every 32 m, water large unless through kept crossing corridor, forest biases to village rims), seeded Kruskal stable sort by weight then id, then `1-3` extra seeded loops where second shortest alternative shortens detour `>22%`. Hierarchy: `city_gate` + `village`/primary-crossing => `primary 7.0`; `village--hamlet`/`village--village` non-primary => `secondary 5.0`; remaining `track 3.5`. Geometry: 3-point bezier `a -> mid -> b` where `mid = lerp(a,b,0.5) + perpendicular * offset`, `offset = sample_coherent_signed((a+b)*0.5, road_mid, 300) * clamp(length*0.08,6,28)` plus second nudge, sampled every 12 m, filtered to reject subsegments with avg slope `>= CLIFF_SLOPE_DEG` or hydrology water crossing outside kept crossing corridor (reroute via nearby crossing within 420 else drop edge). Bridges: wherever polyline crosses `distance_to_water < bank+3` and `water_body_at != ""`, crossing subpolyline promoted to `is_bridge=true`, deck length = water width + `2*4` abutments, deck width = road width + `0.6`. Pure, deterministic for `(seed, coord)` via `WorldSeed`, inclusive of negative coords, world-space shared edges.

### 13.3 Road chunk manifest and materialization (flat ribbon + primitive bridge deck, 96/64 vs 17x17 terrain)
- Per-chunk manifest via `RoadChunkBuilder.build_manifest(WorldPlan, coord)` clipped to `64 m` rect inflated by `half_width+2`. Keys: `coord, origin, size, road_segments (clipped), bridge_segments (clipped), road_vertices, road_triangles, road_colliders, bridge_vertices, bridge_triangles, bridge_colliders, has_road, has_bridge, road_widths, hierarchies, colors, road_gen_ms`. Deterministic for `(seed, coord)`.
- Sampling: `road_network.road_segments_in(rect)` already clipped; for each clipped polyline `n>=2`, tessellate flat ribbon: per segment `(p0,p1)` compute left/right `perp * width*0.5`, vertices at `terrain.height_at(p)+ROAD_LIFT_M 0.04` (bridge decks at `water_level_at(p0)+BRIDGE_DECK_LIFT_M 0.35` over water tapering to terrain+lift at abutments), normals `Vector3.UP`, indices two triangles per quad. Bridge decks are simple Box-like flat strip, not truss. Palette as above, vertex colors via `StandardMaterial3D`.
- Collision budget: at most **one road collider per chunk** (0 if dry), one aggregated `ConcavePolygonShape3D` (or two `BoxShape3D` strips) under `StaticBody3D RoadBody` (`collision_layer 1`, `mask 0`, `backface_collision=true`). Bridge decks share same body. Warm retains ribbon visuals but collision disabled (`collision_layer=0`) — ACTIVE-only road physics, analogous to terrain/water/biome.
- Clipping: only segments whose centerline lies inside inflated rect are emitted; shared edges produce no duplication because each ribbon segment belongs to chunks it actually crosses — but each chunk's mesh is clipped to its own rect so adjacent chunks agree within `SEAM_CONTINUITY_TOL_M 0.02` at shared border (world-space polyline at same world param). Urban compatibility: rural ribbons suppressed inside `URBAN_INNER_M 350` unless gate within `90 m` (short primary stub into city). Hydrology compatibility: over-water ribbon is `is_bridge` only; no dirt/road collider placed with `water_body_at != ""` unless `is_bridge` true and `water_id` matches.
- Budgets: each road chunk bounded at `<=96 verts / <=64 tris / 1 collider` typical, multi-segment junction `<=160/96` still 1 collider within `FRAME_BUDGET_MS 12` combined with city/terrain/water/biome. `road_gen_ms` worker + `road_mat_ms` main measured within 12 ms combined (road target `<=2.5 ms`). ChunkManager stats show `t_road_gen/t_road_mat` per ring and not inflate `t_water_*`/`t_terrain_*`/`t_biome_*`.

### 13.4 Streaming, telemetry, persistence, compatibility
- `WorldConstants` is single source for settlement/road numerics (vocab, cells, widths, thresholds, caps, 96/64, lift 0.04/0.35). Builders/tests import, never duplicate inline numbers. `WorldSeed` domain separation includes settlement/road domains.
- `WorldPlan` facade owns `SettlementPlan` + `RoadNetworkPlan` and forwards `settlement_anchors`, `settlements_in`, `nearest_settlement`, `city_gates`, `road_graph`, `road_segments_in`, `road_hierarchy_at`, `distance_to_road`.
- `ChunkManager` mirrors terrain/water/biome pipeline: counters `_road_vertices_total/_road_triangles_total/_road_colliders_total/_road_bridges_total/_road_mat_ms_total`, per-chunk `road_vertices/road_triangles/road_colliders/road_bridges/road_manifest/road_gen_ms/road_mat_ms/layers_road`, stats keys and timings `road_gen_ms/road_mat_ms`. Thread-safe build holds `holder["road"]` and `road_gen_ms` measured inside worker (private `WorldPlan`), exactly as terrain/water/biome. Active-ring rule: road collider counted toward `active road` only when chunk state ACTIVE; at most 9 active road colliders (3x3). Warm retains visual `RoadMesh` but disables `RoadBody` collision (`collision_layer=0`). `debug_lines()` as `road verts|tris|colliders|bridges t_road_gen|t_road_mat active road (warm)`. Also exposes settlement anchors total. Early `_collect_finished_jobs(pc)` before streaming recalc plus `MAX_MATERIALIZATIONS_PER_FRAME 1` and Variant freed-Zombie guard (`is_instance_valid` + `!is_queued_for_deletion()` + `is_inside_tree()` before `global_transform`) keep pacing stable.
- Save state: `ChunkManager.save_state()` persists `seed/version/discovery/deltas/facts` only — never generated settlement anchors, road polylines, ribbon vertices/indices/collision shapes nor water/biome geometry. Settlement/road graph deterministic from `seed` and re-derived on load; per-chunk road manifests deterministic from `seed+coord`. `WORLD_SCHEMA_VERSION` may be noted but `GENERATOR_VERSION` stays 2 additive outside dense core. No new autoload, project setting, or large asset import beyond settlement/road constants and road streaming stats.
- Compatibility/migration: `GENERATOR_VERSION` stays 2, additive outside dense historic core (`urban_basin` inside 350 m has no rural anchor except gate stubs, rural roads carry `has_road=false` inside 350) and does not carve heightfields or river CX/width/level or building footprints. Old saves follow existing mismatch path (warning + regeneration). Never silently reinterpret old destruction/door delta keys. No new large asset import; probe roads with vertex-colored ribbon + primitive bridge deck Box; textured road or village building meshes remain later milestones. Procedural placement stays GDScript with deterministic seeds.
- Telemetry and docs: `docs/world/WORLD-CONTRACT.md` §13 documents vocab, scoring gates, graph construction, hierarchy widths, polyline smoothing, sampling manifest 1 ribbon + 1 collider 96/64, ACTIVE-only rule, save exclusion, compatibility/migration. `ARCHITECTURE.md`/`DEVELOPMENT.md` document settlement/road module map, streaming telemetry (`t_gen/t_mat/.../t_road_gen/t_road_mat`, `active terrain|water|biome|road`), ACTIVE-only city/terrain/water/biome/road collision, new 9th gate `--roadtest`/`--settlementtest` alias, plus folded HEAD streaming pacing `MAX_MATERIALIZATIONS_PER_FRAME 1` + early `_collect_finished_jobs` + freed-Zombie Variant guard, `tools/run_suite.py` 400 timeout for citytest/roadtest, and folding C001-C003 deferred docs (windowed proof, ACTIVE-only, water district_hint + bank vertex-color, streaming pacing, timeout, shutdown noise guards).

## 14. Rural Building Fabric slice P4.2 (deterministic low-rise shelters along settlement and road corridors)

### 14.1 Vocab and constants (authoritative in `WorldConstants`)

- Rural building vocab `RURAL_BUILDING_VOCAB`: `village_house`, `cottage`, `barn`, `farmhouse`, `stable`, `shed` (subset enforced; `shed` appears only for isolated farm).
- Footprints (deterministic per kind via `unit_float(\"rural_building_fp_x/y\")`): village_house `8-10 x 10-12`, cottage/farmhouse `7-9 x 8-11`, barn/stable `8-10 x 10-14`, shed `6-8 x 8-10`; generic min `6,8` max `10,14` in `RURAL_BUILDING_FOOTPRINT_*`.
- Heights: `RURAL_BUILDING_HEIGHT_SINGLE 4.2` plus `RURAL_BUILDING_HEIGHT_VILLAGE_TWO_STOREY_EXTRA 2.9` per extra floor; `floors 1` for hamlet/farmstead, `1-2` for village houses (hamlet houses `1` only this slice).
- Spacing/budget constants: `RURAL_BUILDING_SPACING_MIN 8.0`, `RURAL_BUILDING_ROAD_SETBACK 4.0`, `RURAL_BUILDING_SETTLEMENT_INNER_CLEARANCE 6.0`, `MAX_RURAL_BUILDINGS_PER_CHUNK 6`, `MAX_RURAL_VERTS_PER_CHUNK 320`, `MAX_RURAL_VERTS_TYPICAL 192`, `MAX_RURAL_TRIS_PER_CHUNK 240`, `MAX_RURAL_TRIS_TYPICAL 144`, `MAX_RURAL_COLLIDERS_PER_CHUNK 1`, `MAX_ACTIVE_RURAL_COLLIDERS 9`, `RURAL_DOOR_COUNT_MAX_PER_CHUNK 6`, `RURAL_OVERLAY_LIFT_M 0.04`, `RURAL_BUILDING_COUNTS` per settlement kind `village 4-6 / hamlet 2-3 / farmstead 1-2 / isolated_farm 1`.
- Colors (vertex-colored, no texture import): wall plaster `ddd0c0`, brick `b07a5a`, timber `7a5a3a`, roof `8a3a2a` / `5a5a5a` deterministically per kind via `unit_float(\"rural_building_palette\")`; `StandardMaterial3D vertex_color_use_as_albedo=true`.

### 14.2 Generation contract (settlement-anchored, road/slope/flood/water/city-flat gates, cardinal yaw)

- Settlement-anchored siting uses `WorldSeed.sample_coherent` / `sample_coherent_signed` / `unit_float` with explicit rural building domains (`rural_building`, `rural_building_count`, `rural_building_radius`, `rural_building_angle`, `rural_building_fp_x`, `rural_building_fp_y`, `rural_building_yaw`, `rural_building_palette`, `rural_building_nudge`) seed-separated from terrain/hydro/geology/biome/settlement/road, `floori` lattice for negative coords, `MACRO_CELL_M 1024` / `LANDSCAPE_CELL_M 256` for tile.
- Per-settlement candidate generation: for each `settlement_anchors()` anchor `s`, target building count deterministic via `unit_float(\"rural_building_count\", [hash(s.id), k])`: village `4-6` (mixed `village_house 2-3`, `barn 1`, `cottage 1-2`), hamlet `2-3` (`village_house/cottage 1-2`, `barn/stable 0-1`), farmstead `1-2` (`farmhouse 1`, `barn/stable 0-1`), isolated_farm `1` (`farmhouse` or `barn`). Count ordered by building index `k`.
- For each building `k`, place candidate center `c_k = s.center + polar_offset` where `radius = lerp(0.28,0.82, unit_float(\"rural_building_radius\"))*s.radius` plus angular jitter `unit_float(\"rural_building_angle\")*TAU`, then respect `RURAL_BUILDING_ROAD_SETBACK 4.0` from any road centerline (`distance_to_road(c_k) >= 4.0`, first building `k==0` may be road-facing with `2.5` setback) and `>=8.0` from any previously placed building in same settlement (greedy by `k` order) and `>=6.0` from settlement center; if violated, perturb deterministically via `unit_float(\"rural_building_nudge\", [hash(s.id),k,attempt])*12` at up to `5` attempts; if still invalid the building is dropped (keeps spacing contract) rather than overlapping.
- Footprint and orientation: `footprint = Vector2(lerp(FP_MIN.x,FP_MAX.x, unit_float(...)), lerp(FP_MIN.y,FP_MAX.y, unit_float(...)))` per kind caps; `yaw` is deterministic cardinal `0, PI/2, PI, -PI/2` aligned to nearest road tangent if `dist_to_road <36` else quantized random via `unit_float(\"rural_building_yaw\")`, keeping batching simple and collision axis-aligned.
- Door siting: on footprint edge midpoint (deterministic choice among 4 edges) facing nearest road if `distance_to_road <22` else settlement center; door `pos` = edge center, `yaw` = edge normal angle (cardinal), width `1.0` (barn `1.2`) height `2.1` (barn `2.2`), hinge left/right via `unit_float(\"rural_building_palette\")`; `door_pos`/`door_yaw` stored on building dict and emitted as `door_manifest` `{id: \"rural_door_%s_0\", building_id, position: Vector3(edge_center + terrain_height), yaw: wall_yaw, edge, width, height, hinge, kind: &\"rural_house\"}`; door faces road/settlement verified via dot `>0.2`.
- Gates and urban exclusion: no rural building whose `center.length() < URBAN_INNER_M 350` unless gate barn exception (`kind==\"barn\"` at `URBAN_INNER_M+40` within `140` of a gate, `<=1` per gate, rarely). Flood/water/slope gates: no building where `water_body_at != \"\"` or `is_floodplain` or `distance_to_water <= BANK_W+2` or `terrain_class_at==\"cliff\"` or `slope >= BUILDABLE_MAX_SLOPE_DEG 22` (village_house `<14`); these use real `WorldConstants` thresholds, not duplicated. Never on `is_bridge` deck (rejected via water + road is_bridge proximity). Pure, deterministic for `(seed, world XZ)`, order-independent, handles negative coords, additive — does not mutate `TerrainPlan` heights nor carve `CityPlan` blocks.
- Deterministic and additive: does not mutate `TerrainPlan` heights, does not carve `CityPlan` blocks, `GENERATOR_VERSION` stays 2. Determinism holds for any seed and any query order, including negative coords. Macro-cell non-speckling proven via clustering: buildings follow settlement anchors, not per-chunk speckles; at least `12-36` settlement anchors still hold and rural buildings per chunk bounded (`<=6`) and total distinct chunks `< total buildings`.

### 14.3 Rural chunk manifest and materialization (batched box shells, 192/144 vs 17x17 terrain)

- Per-chunk manifest via `RuralBuildingChunkBuilder.build_manifest(WorldPlan, coord)` for `Rect2(origin, Vector2(64,64))` querying `rural_building_plan.rural_buildings_in(rect)` then filtering to ownership by footprint center inside rect (so streaming never duplicates or tears a building). Keys: `coord, origin, size, rural_buildings (clipped), door_manifests, rural_vertices, rural_triangles, rural_colliders, rural_doors, has_rural, rural_gen_ms` plus `aabbs` and `colors`/`verts`/`normals`/`indices` for materialize. Deterministic byte-identical across shuffled builds.
- Sampling: batched box geometry per building at `terrain.height_at(center)+0.01` with quantized cardinal yaw, vertex-colored palette per kind (wall + roof), one `ArrayMesh` combining all rural boxes for that chunk (no texture import). Each box is axis-aligned after yaw quantization with `footprint.x/z` and `height`; indices per box `12 tris` (closed shell, 24 verts with per-face normals), batched mesh may share vertices but measured as `rural_vertices`/`rural_triangles`. Door aperture left as wall opening; `Door` leaf instanced at `door_pos` with `door_yaw`.
- Collision budget: at most **one rural collider per chunk** (`0` if none) — one aggregated `ConcavePolygonShape3D` (`backface_collision=true`) under `StaticBody3D RuralBody` (`collision_layer 1`, `mask 0`). Door leaves retain their own `RigidBody3D` collision (per-door, counted in `rural_doors` not in shell budget). Warm retains visuals but shell collision disabled (`collision_layer=0`) — ACTIVE-only rural physics, analogous to city/terrain/water/biome/road. Doors at most `6` per village chunk, `3` per hamlet/farmstead chunk. Typical hamlet/farmstead chunk `<=192 verts / <=144 tris` with 1-2 buildings; village core `<=320/240` with up to 6 buildings (bounded, never 2D grid).
- Clipping and seam: buildings belong to exactly one chunk by center; adjacent chunks agree because each building appears only in its owner chunk; no duplicated walls at shared borders; empty field between buildings is traversable terrain/biome. Shared road centerline still within `0.02` at both `+` and `-` borders.
- Urban compatibility: rural manifests generated for all chunks, but inside `URBAN_INNER_M` suppressed (`has_rural=false` if `origin.length() < URBAN_INNER_M` and not gate barn). Gates may host at most `1` roadside barn.
- Hydrology/road compatibility: no rural `aabb` may intersect water/floodplain or lie on road ribbon (`distance_to_road < width*0.5+2.5` rejected at plan time); if road later rerouted near building, building stays and road manifest's hydrology constraint already avoided water; no dirt/road collider and rural shell collider share `collision_layer 1` but do not overlap in world XZ.

### 14.4 Streaming, telemetry, persistence, compatibility

- `WorldConstants` is single source for rural building numerics (vocab, footprints, heights, spacing, setbacks, caps, `192/144`, lift `0.04`). Builders/tests import, never duplicate inline numbers. `WorldSeed` domain separation extends with `&\"rural_building\"` etc. (explicit ordered `Array[StringName]` of 9 domains).
- `WorldPlan` facade owns `RuralBuildingPlan` and forwards `rural_buildings`, `rural_buildings_in`, `nearest_rural_building`, `rural_building_at`, `settlement_buildings`; constructed once per worker thread; pure, no scene mutation; `plan_mutex` guards any `CityPlan` caches exactly as before.
- `ChunkManager` mirrors terrain/water/biome/road pipeline: new counters `_rural_vertices_total/_rural_triangles_total/_rural_colliders_total/_rural_doors_total/_rural_buildings_total/_rural_mat_ms_total`, per-chunk record fields `rural_vertices/rural_triangles/rural_colliders/rural_doors/rural_buildings/rural_manifest/rural_gen_ms/rural_mat_ms/layers_rural`, stats keys and timings `rural_gen_ms/rural_mat_ms`. Thread-safe build holds `holder[\"rural\"]` and `rural_gen_ms` measured inside worker (private `WorldPlan`), exactly as terrain/water/biome/road. Active-ring rule: rural shell collider counted toward `active rural` only when chunk state ACTIVE; at most `9` active rural colliders (3x3). Warm retains `RuralMesh` visuals but disables `RuralBody` collision (`collision_layer=0`) and door leaves follow Door semantics (not counted toward shell). `debug_lines()` as `rural verts|tris|colliders|doors t_rural_gen|t_rural_mat active rural (warm)`.
- Save state: `ChunkManager.save_state()` persists `seed/version/discovery/deltas/facts` only — never generated rural shells, rural building vertices/indices/collision shapes, rural door manifests, road polylines, ribbon vertices/indices/collision shapes nor water/biome geometry. Rural manifests deterministic from `seed+coord`. `WORLD_SCHEMA_VERSION` may be noted but `GENERATOR_VERSION` stays **2** additive outside dense historic core (inside `350` no rural shell except gate barn). No new autoload, project setting, or large asset import beyond rural constants and rural streaming stats.
- Compatibility/migration: `GENERATOR_VERSION` stays **2**, additive outside dense historic core and does not alter city block topology or terrain heights or water level or biome manifests, rural buildings suppressed inside `350` except gate barn. Old saves follow the existing mismatch path (warning + regeneration). Never silently reinterpret old door delta keys. No new large asset import; probe rural shells with vertex-colored boxes + primitive door leaves; textured village building meshes remain later milestones.
- Telemetry and docs: `docs/world/WORLD-CONTRACT.md` §14 documents vocab, per-settlement building counts, footprint/yaw/door siting gates (slope/flood/water/road/city flat), manifest `RuralBuildingChunkBuilder` batched mesh + 1 collider + door leaves per building, `ACTIVE-only` rule, save exclusion, compatibility/migration note (`GENERATOR_VERSION` stays 2, additive outside dense core). `ARCHITECTURE.md`/`DEVELOPMENT.md` document rural module map, streaming telemetry (`t_gen/t_mat/t_terrain_gen/t_terrain_mat/t_water_gen/t_water_mat/t_biome_gen/t_biome_mat/t_road_gen/t_road_mat/t_rural_gen/t_rural_mat`, `active terrain|water|biome|road|rural`), ACTIVE-only city/terrain/water/biome/road/rural collision, new 10th gate `--ruraltest` (`--settlementbuildingtest`/`--ruralfabrictest` aliases), rural vocab and `192/144` (`320/240` max) budget plus settlement-building rule and road setback, HEAD streaming pacing `MAX_MATERIALIZATIONS_PER_FRAME 1` + early `_collect_finished_jobs` + freed-Zombie Variant guard, `tools/run_suite.py` 400 timeout for citytest/roadtest/ruraltest (document 450 guidance), folding C001-C004 deferred docs (ACTIVE-only, water district_hint + bank ribbon, streaming pacing, timeout, shutdown noise guards, windowed proof archival under `.hermes/autopilot/reports/SPEC-C005-windowed.*`).

## 15. Rural Interior & Scavenge slice P4.3 (deterministic partition, furniture proxies and FoodCrate loot inside rural shells)

### 15.1 Vocab and constants (authoritative in `WorldConstants`)

- Furniture vocab `RURAL_FURNITURE_VOCAB`: `bed`, `shelf`, `table`, `stove` (crate pedestal is not furniture).
- Interior wall thickness `RURAL_INTERIOR_WALL_THICKNESS 0.15` (range 0.14-0.16), length fraction `RURAL_INTERIOR_WALL_LENGTH_FRACTION_MIN 0.55` to `MAX 0.85` via `lerp(...)*min(footprint.x,footprint.y)-0.4`, doorway gap `RURAL_INTERIOR_DOORWAY_GAP_M 0.95` (0.90-1.10).
- Caps: `RURAL_FURNITURE_MAX_PER_BUILDING 3`, `RURAL_FURNITURE_MAX_PER_VILLAGE_CHUNK 6` (`CAP_PER_CHUNK 6`), `RURAL_CRATE_MAX_PER_CHUNK 3`, `RURAL_CRATE_MAX_PER_VILLAGE 3`, `RURAL_CRATE_MAX_PER_HAMLET 1`, `RURAL_CRATE_ITEMS_MIN 1`, `RURAL_CRATE_ITEMS_MAX_VILLAGE 4`, `RURAL_CRATE_ITEMS_MAX_HAMLET 2`.
- Budgets: rural shell+interior `MAX_RURAL_VERTS_PER_CHUNK 400` typical `240`, `MAX_RURAL_TRIS_PER_CHUNK 300` typical `180` (raised from 320/240 192/144 to carry interior walls+furniture; justification: village core 6 buildings each 24 verts shell + 0-1 wall 24 + 1-3 furniture 24 each = up to ~432 but typical hamlet 1-2 buildings 1-2 walls+furniture ~240/180, dense village core with 6 buildings rarely co-located in one 64 m chunk, caps enforce 400/300 hard limit with furniture culling), `MAX_RURAL_COLLIDERS_PER_CHUNK 1` (aggregated shell+interior, furniture visual only), `MAX_ACTIVE_RURAL_COLLIDERS 9`, `RURAL_DOOR_COUNT_MAX_PER_CHUNK 6`, `RURAL_OVERLAY_LIFT_M 0.04`.
- Palette: interior walls plaster/brick `ddd0c0/b07a5a/7a5a3a` darker `*0.88`, furniture bed `9e8b6a` shelf `6b5a4a` table `7a6a5a` stove `4a4a4a` (deterministic vertex colors, `StandardMaterial3D vertex_color_use_as_albedo=true`).
- Crate `FoodCrate` 1.0x0.9x1.0 BoxMesh `0.45,0.42,0.3` plus `BoxShape`, contents weighted `ItemDB` `canned_food 40%`, `water_bottle 25%`, `bandage 20%`, `antibiotics 15%` (counts village 1-4 hamlet 1-2).

### 15.2 Generation contract (deterministic interior siting inside existing shells)

- Interior siting uses `WorldSeed.sample_coherent` / `unit_float` with explicit rural interior domains `RURAL_INTERIOR_DOMAINS = [&\"rural_interior\", &\"rural_interior_wall\", &\"rural_interior_wall_gap\", &\"rural_furniture\", &\"rural_crate\", &\"rural_crate_contents\"]` (ordered, seed-separated, `floori` for negative coords, no RNG sharing).
- Per-building interior generation deterministic via `unit_float(\"rural_interior_wall\", [hash(id)])` etc.:
  - Partition wall: if `kind in {village_house, cottage, farmhouse}` and `footprint.x >=7.5` and `footprint.y >=9.0`, add one wall `~70%` (`>0.30`). Wall axis-aligned cardinal, thickness `0.14-0.16`, length `lerp(0.55,0.85)*min-0.4`, placed at `0.38-0.62` along long axis central band, inset `0.5` from exterior, leaving `0.90-1.10` gap at one end (choice left/center/right via `rural_interior_wall_gap`). Wall `aabb` fully inside footprint inset `0.5` and not intersecting door swing `1.0`. Barn/stable/shed never get partition but may get furniture/crate.
  - Furniture: `1-3` per building deterministic (`village_house 2-3`, `cottage/farmhouse 1-2`, `barn/stable 0-1` shelf/table only). Each `{kind, pos, yaw, size}` against interior/exterior wall (4+1 choices), inset `0.15` from wall `0.35` from corners, not overlapping door swing or doorway gap, spacing `>=0.9` (aabb gap). Sizes bed `1.9x0.5x0.9`, shelf `1.2x0.4x1.1`, table `1.0x1.0x0.75`, stove `0.8x0.8x0.9`. At most `6` per village chunk `<=4` hamlet/farmstead.
  - Crate: `village 0-3` per village (each building `35%`), `hamlet 1` per settlement (choice among its buildings), `farmstead/isolated_farm 1` per building. Pos inside larger room (partition) or against far wall opposite door, `>=0.70` from walls `1.20` from door `>=0.90` from furniture/wall. Yaw `building yaw`, id `rural_crate_%s`, contents `1-4` (village) `1-2` (hamlet) weighted `40/25/20/15` via `rural_crate_contents`, at least one key. Never on `is_bridge`, inside inset.
- Gates/urban exclusion unchanged: interiors never outside footprint or inside floodplain/water/cliff. Pure, additive, `GENERATOR_VERSION` stays 2, handles negative coords, quantized cardinal yaw.

### 15.3 Rural chunk manifest and materialization (batched shell+interior+farmiture, FoodCrate leaves)

- Per-chunk manifest via `RuralBuildingChunkBuilder.build_manifest` now enriched: existing keys plus `interior_walls`, `furniture_anchors`, `crate_manifests`, `rural_crates`, `rural_furniture`, `interior_vertices/triangles` (folded into `rural_vertices/triangles` but attestable). Deterministic byte-identical shuffled for `pos/yaw/kind/contents/colors`.
- Palette for interiors as above, furniture small boxes, crate is separate `FoodCrate` node `1.0x0.9x1.0` `BoxMesh` + `BoxShape` via `world/food_crate.gd` with `contents` set.
- Collision: at most **one rural shell+interior collider** (`0` if none) aggregated `Concave` (`RuralBody` `collision_layer 1`, warm `0`), furniture visual only. Door leaves `RigidBody3D` per door (`rural_doors`), crate leaves `FoodCrate` `StaticBody3D` per crate (`rural_crates`) with `InteractableComponent` (not counted toward shell budget, but `planned_colliders` separation; warm `collision_layer 0` and `enabled=false`). Building center ownership no duplication at `+`/`-`/`-Z` borders, shared road centerline `0.02`.
- Budgets: each chunk `verts <=400` `tris <=300` typical hamlet/farmstead `<=240/180`, village core `<=400/300` with up to `6` buildings each `0-1` wall `1-3` furniture, doors `<=6`, crates `<=3` per village `<=1` hamlet, furniture `<=6` `<=4`, `3x3` ACTIVE `<=9` rural colliders `<=9` active crates when ACTIVE (warm disabled), `t_rural_gen/t_rural_mat` within `FRAME_BUDGET_MS 12` (interior target `<=3.5` combined).
- Urban compatibility unchanged: rural+interiors suppressed inside `350` except gate barn single.

### 15.4 Streaming, telemetry, persistence, compatibility

- `WorldConstants` single source for rural interior numerics (vocab, footprints, thickness, caps, 240/180 400/300, lift). Builders/tests import. `WorldSeed` domains extend with 6 ordered interior domains.
- `WorldPlan` facade owns extended `RuralBuildingPlan` and forwards enriched `rural_buildings_in` with `interior`. Pure, `plan_mutex` guards.
- `ChunkManager` mirrors pipeline: counters `_rural_crates_total/_rural_furniture_total`, per-chunk `rural_crates/rural_furniture/crate_manifests/interior_manifest`, measures `rural_gen_ms` now shell+interior+crate (still within 12 ms), `rural_mat_ms` with FoodCrate instantiation. Active-ring: rural shell collider `ACTIVE` only, warm retains `RuralMesh` visuals (including interior walls+furniture) but disables `RuralBody` `collision_layer 0` and crate `StaticBody`/`Interactable` `enabled=false` `collision_layer 0`; at most `9` active rural colliders `9` active crates. Stats `t_rural_gen/t_rural_mat` include interior, debug lines `rural verts|tris|colliders|doors|crates|furniture t_rural_gen|t_rural_mat active rural (warm)`. `MAX_MATERIALIZATIONS_PER_FRAME 1` with early `_collect_finished_jobs(pc)` + freed-Zombie Variant guard (`is_instance_valid`+`!is_queued_for_deletion()`+`is_inside_tree()` before `global_transform`) documented as intentional pacing.
- Save state: `save_state()` persists `seed/version/discovery/deltas` plus `deltas.crates : {crate_id: {item:count}}` sibling to `deltas.doors/damage`; never generated rural shells/interiors/furniture/crate polylines. Deterministic re-derive on load; per-chunk `crate_deltas` re-applied before materialize to keep loot depletion (matching `dead_doors` pattern). `GENERATOR_VERSION` stays 2, additive outside dense core, pure. No new autoload/project setting/large asset beyond interior constants and crate streaming stats.
- Telemetry and docs: `WORLD-CONTRACT.md` §15 documents partition+furniture+crate vocab, siting gates, manifest batched mesh `400/300` + `1` collider + door+FoodCrate leaves, ACTIVE-only shell and crate, save exclusion crate deltas, compatibility migration `GENERATOR_VERSION` stays 2. `ARCHITECTURE.md`/`DEVELOPMENT.md` document rural interior module map, streaming telemetry `t_rural_gen/mat` now interior+crate, `240/180` `400/300` budget justification, furniture `<=6` crates `<=3` ItemDB `40/25/20/15`, ACTIVE-only including crate, interior domains, and folding `330/240->400/300` justification plus `MAX_MATERIALIZATIONS_PER_FRAME 1` + freed-Zombie guard verbatim, `tools/run_suite.py` 400 timeout (450 guidance), `WORLD-CONTRACT` §15, folding C001-C005 deferred docs (windowed proof, ACTIVE-only, water district_hint + bank, streaming pacing, timeout, shutdown noise guards).

## 16. Rural Homestead Renewables slice P4.4 (village wells and wild forage patches streamed around rural shells)

### 16.1 Vocab and constants (authoritative in `WorldConstants`)

- Well vocab `RURAL_WELL` (kind `village_well`), radius `RURAL_WELL_RADIUS 0.9`, height `RURAL_WELL_HEIGHT 1.8`, palette wall `8b7f6e` water `2b3a4a` beam `6b5a4a` (vertex-colored, no texture import).
- Forage vocab `RURAL_FORAGE_VOCAB`: `bush_berry`, `mushroom_cluster`, `herb_patch` (at least two kinds, bush_berry required); colors bush `5a7a3a` mushroom `8a6a4a` herb `6a8a5a`.
- Caps: `RURAL_WELL_MAX_PER_CHUNK 2`, `RURAL_WELL_MAX_PER_VILLAGE 2`, `RURAL_WELL_MAX_PER_HAMLET 1`, `RURAL_WELL_MAX_PER_FARMSTEAD 1`; `RURAL_FORAGE_MAX_PER_CHUNK 4`, `RURAL_FORAGE_MAX_PER_VILLAGE_VICINITY 5`, `RURAL_FORAGE_MAX_PER_HAMLET_VICINITY 3`, `RURAL_FORAGE_VICINITY_M 120`, `RURAL_RESOURCE_VICINITY_M 120`; spacing `RURAL_WELL_BUILDING_GAP_MIN 8.0`, `RURAL_WELL_SPACING_MIN 6.0`, `RURAL_FORAGE_BUILDING_GAP_MIN 6.0`, `RURAL_FORAGE_WELL_GAP_MIN 6.0`, `RURAL_FORAGE_SPACING_MIN 4.0`, road setbacks `RURAL_WELL_ROAD_SETBACK 4.0`, `RURAL_FORAGE_ROAD_SETBACK 2.0`; allowed biomes `RURAL_FORAGE_ALLOW_BIOMES = [arable_field, pasture, pasture_orchard, orchard, deciduous_forest, mixed_upland_forest, wet_meadow]`.
- Budgets: rural shell+interior+well+forage `MAX_RURAL_VERTS_PER_CHUNK 480` typical `280`, `MAX_RURAL_TRIS_PER_CHUNK 360` typical `210` (raised from 400/300 240/180 to carry well 24 verts/12 tris + forage 24/12; justification: village core 6 buildings +2 wells +4 forage still within 480/360, hamlet 1-2 buildings +1 well +2 forage <=280/210), `MAX_RURAL_COLLIDERS_PER_CHUNK 1` (aggregated shell+well), `MAX_ACTIVE_RURAL_COLLIDERS 9`, `RURAL_DOOR_COUNT_MAX_PER_CHUNK 6`. Forage Area3D no collider, Well shares RuralBody collider, forage monitorable ACTIVE-only.
- Well `Well` 0.9x1.8 BoxMesh approximation + water disc, forage bush 0.8x0.6x0.8 BoxMesh (mushroom 0.4x0.3), forage herb 0.6x0.35. Contents weighted `ItemDB` `canned_food 40%`, `water_bottle 25%`, `bandage 20%`, `antibiotics 15%` (single item per patch, well always water_bottle); regrow well 1 day at 04:00 next day (total_minutes >= depleted_at_day*1440+240), forage 2 days (day >= depleted_at_day+2).

### 16.2 Generation contract (deterministic wells/forage siting around settlement anchors)

- Wells per settlement deterministic via `unit_float("rural_well", [hash(anchor.id),k])` mapped village 1-2 (0.6), hamlet 1, farmstead 0-1 (0.45), isolated 0-1 (0.65). Candidate `pos_k = settlement_center + polar_offset` where radius `lerp(0.35,0.65, unit_float("rural_well_radius"))*settlement.radius` angular `unit_float("rural_well_angle")*TAU`, then validated via pure queries: terrain != cliff, slope < BUILDABLE_MAX (<14 village), water_body=="" and not floodplain and distance_to_water > BANK+2 and not urban (<350 except gate barn well <=1 per gate within 140), road >=4.0 not on is_bridge, >=8 from any building aabb, >=6 from other wells, greedy 12 attempts + fallback 12 for hamlet strict 1. Well dict {id:"rural_well_%s_%d", kind &"village_well", center/pos, yaw 0, settlement_id/kind, radius0.9 height1.8 color}.
- Forage per settlement vicinity 2-5 (village 3-5 hamlet2-3 farmstead1-2 isolated1) via `unit_float("rural_forage")` mapped; each candidate `cell_origin + lerp(12,120)` jitter +-3, gated terrain !=cliff slope<14/22 distance> BANK+2 not floodplain not water allowed biomes road>=2 not bridge >=6 building >=6 well >=4 forage, cap 4 per chunk and per village 5/hamlet3. Kind via `rural_forage_kind` weighted 40/25/20/15, contents same weighted ItemDB single item. Deterministic, additive, handles negative coords via floori, seed-separated domains RURAL_RESOURCE_DOMAINS ordered, GENERATOR_VERSION stays 2.

### 16.3 Rural chunk manifest and materialization (batched well+forage, 280/210 vs 17x17 terrain)

- Per-chunk manifest via `RuralBuildingChunkBuilder.build_manifest` now enriched with `well_manifests`/`forage_manifests` + `rural_wells/rural_forage/well_vertices/triangles/forage_vertices/triangles` clipped by center inside rect, caps wells<=2 forage<=4, palette well 8b7f6e water2b3a4a beam6b5a4a bush5a7a3a etc., deterministic byte-identical shuffled for pos/yaw/kind/contents/colors.
- Visual ground: wells cylinder Boxes at terrain+0.01 (24 verts/12 tris each) plus water disc, forage bushes Boxes (24/12) vertex-colored, StandardMaterial vertex_color_use_as_albedo. At most 1 shell+well Concave per chunk aggregated, forage Area3D monitorable only when ACTIVE. Well/forage center ownership no duplication at + and - and -Z borders, shared road centerline 0.02.
- Urban compatibility: rural+well+forage suppressed inside 350 except gate well 1 per gate; hydrology/road gates same as buildings plus well/forage road setbacks.

### 16.4 Streaming, telemetry, persistence, compatibility

- WorldConstants single source for well/forage numerics, builders/tests import never duplicate. WorldSeed RURAL_RESOURCE_DOMAINS ordered.
- WorldPlan facade forwards rural_wells, rural_wells_in, nearest_rural_well, rural_forage_patches, rural_forage_patches_in, nearest_rural_forage (pure).
- ChunkManager mirrors pipeline: new counters _rural_wells_total/_rural_forage_total, per-chunk rural_wells/rural_forage/well_manifests/forage_manifests, measures rural_gen_ms now shell+interior+crate+well+forage (still within FRAME_BUDGET_MS 12, interior+renewable target <=4.0), rural_mat_ms with Well/Forage instantiation. Active-ring: shell+well collider ACTIVE only, forage Area3D monitorable ACTIVE only. Debug_lines rural verts|tris|colliders|doors|buildings|crates|furniture|wells|forage t_rural_gen|t_rural_mat active rural (warm). Save_state persists seed/version/discovery/deltas plus deltas.wells {depleted,depleted_at_day} and deltas.forage {depleted,depleted_at_day,contents} sibling to crates/doors/damage; never generated geometry. Deterministic re-derive on load; per-chunk well/forage deltas re-applied before materialize with GameClock refill 04:00 and regrow 2 days; Variant freed-Zombie guard extended to well/forage path. Folding C001-C006 deferred docs (ACTIVE-only, water district_hint + bank, streaming pacing 1-per-frame + freed-Zombie, timeout 400, shutdown noise, windowed proof).

## 17. Rural Hearth Habitation slice P4.5 (deterministic stove/bed functionalization inside rural shells with unified collider and verified renewable timers)

### 17.1 Vocab and constants (authoritative in `WorldConstants`)

- Hearth vocab `RURAL_HEARTH_VOCAB`: `stove`, `bed` subset of `RURAL_FURNITURE_VOCAB` (reuse, no new placement random). Palette `COL_FURNITURE_STOVE 4a4a4a` / `COL_FURNITURE_BED 9e8b6a` vertex-colored, `StandardMaterial3D vertex_color_use_as_albedo=true`.
- Caps: `RURAL_STOVE_MAX_PER_CHUNK 2`, `RURAL_BED_MAX_PER_CHUNK 2`, `RURAL_HEARTH_MAX_PER_CHUNK 4` (village chunk with 6 buildings each 0-1 stove/bed via furniture cap 6, hamlet 2), `RURAL_HEARTH_MAX_PER_VILLAGE_CHUNK 4`.
- Budgets: rural shell+interior+well+forage+hearth `MAX_RURAL_VERTS_PER_CHUNK 480` typical `280`, `MAX_RURAL_TRIS_PER_CHUNK 360` typical `210` **unchanged** (hearth reuses furniture mesh vertices 24 already counted, 0 extra verts/colliders, so 480/360 dense village 6 buildings+2 wells+4 forage+2 stoves+2 beds still within 480/360, hamlet/farmstead 1-2 buildings+1 well+2 forage+1 hearth <=280/210), `MAX_RURAL_COLLIDERS_PER_CHUNK 1` (aggregated shell+interior+well baked, forage/hearth Area3D no collider), `MAX_ACTIVE_RURAL_COLLIDERS 9` unified peak **54 not 63** (9 city+9 terrain+9 water+9 biome+9 road+9 rural where rural is single shell+well Concave, forage/hearth Area3D monitorable ACTIVE-only, no collider; previously Well StaticBody added +9 wells extra causing 63).
- Functional constants: `STOVE_HUNGER_REDUCTION 40` (via NeedsComponent.eat), `BED_FATIGUE_REDUCTION 40`, `BED_SLEEP_MINUTES 480` (8h to next 06:00), `WELL_REFILL_HOUR 4`, `FORAGE_REGROW_DAYS 2`. Well 0.9x1.8 stone ring, forage 0.8x0.6 etc., ItemDB weighted contents `canned_food 40% water_bottle 25% bandage 20% antibiotics 15%` (well always water_bottle, forage single item), stove consumes 1 canned_food, bed advances GameClock 480.
- Forage kind weights corrected from duplicate bush fallback `0-0.40/0.40-0.65/0.65-0.85/else bush (15% duplicate)` to distinct `bush_berry 45% (0-0.45), mushroom_cluster 30% (0.45-0.75), herb_patch 25% (0.75-1.0)` via `rural_forage_kind` domain, settlement-vicinity contract documented as authoritative (per-landscape-cell 256 m distribution deferred to field-parcel cultivation, no principal gameplay difference).

### 17.2 Generation contract (deterministic hearth reusing furniture anchors)

- Hearth per building deterministic via `_build_hearth(furniture, building)` reusing furniture anchors where `kind==stove` or `kind==bed` (no new random siting pass): for each furniture anchor `f` where `f.kind==stove` emit one stove hearth `{id: "rural_stove_%s" % building.id, building_id, kind: &"stove", pos: f.pos, yaw: f.yaw, size: f.size, settlement_id, interior_pos: f.local_pos, aabb}` and similarly for `bed` -> `rural_bed_%s`. Pos/yaw/size copied from furniture anchor, hence automatically satisfies furniture siting gates (`>=0.9` from other furniture, `>=1.0` from door swing, `0.7` from walls, inside inset `0.5`). If a building has multiple stove/bed entries (rare, at most 1 of each via P4.3 distribution, all emitted but per-chunk hearth caps apply at manifest time). No new domain strictly required because hearth reuses furniture kind `rural_furniture`; if a new `RURAL_HEARTH` domain added its ordered Array must be documented after existing `RURAL_RESOURCE_DOMAINS`. No RNG sharing, no per-query mutable state, handles negative coords via floori.
- Deterministic and additive: does not mutate TerrainPlan heights, does not carve CityPlan blocks, `GENERATOR_VERSION` stays 2. Determinism holds for any seed and any query order, including negative coords. Hearth entries are pure plan data, axis-aligned, id stable, additive outside dense historic core (`URBAN_INNER_M 350` no rural shell except gate barn hearth <=1 per gate).
- Gates and urban exclusion unchanged: no hearth whose `center.length() < URBAN_INNER_M` except gate barn hearth <=1 per gate; hearth never outside `WORLD_HALF_EXTENT`.
- Settlement-vicinity forage contract documented as authoritative: forage generated per settlement vicinity via rad 12-120 + +-3 jitter and allowed biomes/road 2.0 + building 6/well 6/forage 4 caps, not per 256 m landscape cell 0-3 with +1 bias; per-landscape-cell `rural_forage_density` coherent noise deferred to future field-parcel cultivation. Keep budgets and ACTIVE-only, additive.

### 17.3 Rural chunk manifest and materialization (unified hearth & collider bake, 280/210 justification unchanged)

- Per-chunk manifest via `RuralBuildingChunkBuilder.build_manifest(WorldPlan, coord)` now enriched: existing keys plus `hearth_manifests`/`stove_manifests`/`bed_manifests` + `rural_hearths/rural_stoves/rural_beds` + `has_hearth/has_stove/has_bed` + `rural_hearths/rural_stoves/rural_beds` counts, at least `rural_stoves (int)` and `rural_beds (int)` or folded `rural_hearths` dict with counts, and attestable vertices/triangles if any (hearth 0 extra). Deterministic for `(seed, coord)` and byte-identical across shuffled builds for all building/door/interior/crate/well/forage/hearth fields including pos/yaw/kind.
- Palette for hearth: reuse furniture colors `COL_FURNITURE_STOVE 4a4a4a` `COL_FURNITURE_BED 9e8b6a` already in chunk mesh — no new palette, deterministic via existing `WorldConstants`/`RURAL_FURNITURE_VOCAB` and not imported texture. `StandardMaterial3D vertex_color_use_as_albedo=true`.
- Collision budget **fixed**: at most **one rural building+well collider per chunk** (`0` if no building and no well; if `has_rural` false but `has_well` true still `1` collider for well). The single aggregated `ConcavePolygonShape3D` (under `StaticBody3D RuralBody` `collision_layer 1`, `mask 0`, `backface_collision=true`) now includes both shell boxes+interior walls **and** well cylinders **baked into the same Concave** — wells no longer instantiate a separate `StaticBody` `CylinderShape 0.9x1.8`. `Well` nodes become `Area3D` interactables (monitorable ACTIVE-only, collision_layer 0, no CollisionShape contributing to physics) — they share the `RuralBody` `Concave` and must not create a second body. Harness asserts `rural_colliders == (1 if has_collider else 0)` **and** that actual `get_tree().get_nodes_in_group("rural_chunk")` physics body count per chunk `== rural_colliders` (no hidden second `WellBody`). Forage patches remain visual+`Area3D` interactable with no separate collider (forage `Area3D monitorable` `collision_layer 0` kept disabled when warm, still within same chunk if needed — must not create a second collider). Door leaves retain their own `RigidBody3D` collision (per-door, counted in `rural_doors`, not towards shell budget). Hearth stoves/beds each have their own `Area3D` Interactable (`Stove`/`Bed`) with `collision_layer 0` `monitorable` only when `ACTIVE`, no collider counted toward `rural_colliders`; their visuals are the same furniture box already in the mesh (no extra verts). The single `RuralBody` `Concave` is the only physics body for shell+well; hearth/forage are `Area3D`.
- Clipping and seam: buildings/wells/forage/hearths belong to exactly one chunk by center inside rect; adjacent chunks agree because each entry appears only in its owner chunk, shared borders carry no duplication. Empty field between buildings remains traversable terrain/biome/road. Forage kind correction 45/30/25, wells/forage/hearth center ownership at + and - and -Z borders shared road centerline 0.02.
- Budgets: each chunk carries at most **1 rural shell+well collider** (`0` if no building and no well, `1` if either) aggregated shell+interior+well `Concave` (warm `collision_layer=0`) with well baked into same `Concave` (no second `WellBody` — harness asserts `rural_colliders == (1 if has_collider else 0)` **and** that actual physics body count per chunk equals `rural_colliders` by scanning `get_nodes_in_group("rural_chunk")` or `Chunk_MG` stats vs scene tree count == `1` not `2-3`); `rural_vertices <= 480` and `rural_triangles <= 360` per chunk (typical hamlet/farmstead `<=280`/`<=210` with 1-2 buildings + well + 2 forage + 1 hearth, village core `<=480/360` with up to 6 buildings + 2 wells + 4 forage + 2 stoves + 2 beds — hearth shares 24 verts already counted, so no increase), doors `<=6` per village chunk, crates `<=3` per village chunk (`<=1` per hamlet), wells `<=2` per village chunk (`<=1` per hamlet), forage `<=4` per chunk, `furniture <=6` per village chunk (`<=4` hamlet), `stoves <=2` `beds <=2` per village chunk (`<=1` hamlet) `hearth_manifests <=4` per village chunk, and the `3x3` ACTIVE ring carries at most **9 active rural colliders** (`active rural` shell+well count, wells and forage/hearth interactables are `Area3D monitorable` not counted toward `54` peak).

### 17.4 Streaming, telemetry, persistence, compatibility

- `WorldConstants` is single source for rural hearth numerics (vocab, footprints, heights, spacing, setbacks, well radius/height, forage vocab/caps, hearth stove/bed vocab, `280/210` typical `480/360` max, lift, regrow days, `well 1 day 04:00 forage 2 days`, `stove hunger -40` `bed fatigue -40` `sleep 480 min`) and unified collider 54 peak not 63. Builders/tests import, never duplicate inline numbers.
- `WorldPlan` facade owns extended `RuralBuildingPlan` and forwards enriched `rural_buildings`, `rural_buildings_in`, `nearest_rural_building`, `rural_building_at`, `settlement_buildings` plus `rural_wells`/`rural_forage_patches` plus **new `rural_hearths_in`/`nearest_rural_hearth`** pure queries, plus settlement/well/forage/hearth siting (flat `<14`, not floodplain/water/cliff, road setback uniform `4.0` well `2.0` forage, building/well/forage/hearth `aabb` gaps).
- `WorldPlan` remains pure (no scene mutation on query), `CityPlan` deterministic building IDs and `TerrainPlan` 17x17 manifests unchanged for same seed/coord, spawn anchor still found dry at `urban_basin`, multi-storey building samples unchanged — proved by `--citytest` plus `--terrainmaterialtest` plus `--hydrotest` plus `--biometest` plus `--roadtest` each finishing with `finished with 0 failure(s)` and retained overlap/door/stair/facade/material/geologic/road seams assertions (no duplicate city/rural buildings/wells/forage/hearth, no terrain seam regression, hydrology `CX`/`meander`/`width` still `530-710`/`72`/`38-50`, biome contiguity still `>=192 m` run-length, district_hint still present, road seam still `0.02` and `9` resident road chunks, `GENERATOR_VERSION` still `2`).
- `ChunkManager` mirrors previous pipeline plus hearth: new counters `_rural_hearths_total/_rural_stoves_total/_rural_beds_total`, per-chunk record fields `rural_hearths/rural_stoves/rural_beds/hearth_manifests` plus `rural_gen_ms/rural_mat_ms` already exist and must now include hearth generation time within the same `rural_gen_ms` (document that `rural_gen_ms` now measures shell+interior+crate+well+forage+hearth). Active-ring rule unchanged: rural shell+well collider counted toward `active rural` only when chunk `ACTIVE`; at most `9` active rural colliders (`3x3`). Warm retains `RuralMesh` visuals (including wells/forage/hearth visuals) but disables `RuralBody` collision (`collision_layer=0`) and `Well`/`Stove`/`Bed` `Interactable.enabled=false` when warm (`Area3D monitorable=false`), forage `Interactable.enabled=false` when warm. Wire `debug_lines()` to extend `rural verts|tris|colliders|doors|crates|furniture|wells|forage|hearth t_rural_gen|t_rural_mat active rural (warm)` (at least `rural_hearths` must appear in debug lines and `F3` overlay). Include hearth counts in `ChunkManager.debug_lines()` and `F3` overlay `active rural` to include hearth derivations. Budgets enforced including well-baked collider `54` peak and hearth `Area3D` counts; crate/well/forage deltas persisted, hearth stateless; no navmesh/grid.
- Hearth functionalization in chunk manager: `ACTIVE-only` shell+well collider and crate/well/forage/hearth interactability (warm `RuralBody` layer `0`, crates/wells/hearth `enabled==ACTIVE && not empty/depleted` plus stove `enabled==ACTIVE && player has canned_food` prompt logic, forage/stove/bed `monitorable==ACTIVE` && not depleted). Persistence: `ChunkManager.save_state()` persists `seed/version/discovery/deltas` including `deltas.crates : {crate_id: {item:count}}` plus `deltas.wells` and `deltas.forage` sibling to `doors/damage`; **stove/bed are stateless** (no `deltas.hearth`) because cooking/sleep affect `InventoryComponent`/`NeedsComponent`/`GameClock` not world deltas; generated hearth leaves never serialized, re-derived deterministically. Well/forage deltas re-applied before materialize with `GameClock` refill well at `04:00` next day and forage after `2` days — now **proven headless** via deterministic `GameClock.advance` test (see §17.3). Save exclusion of generated hearth (stateless), compatibility/migration note (`GENERATOR_VERSION` stays `2`, additive outside dense core, hearth adds `0` verts/colliders).
- Streaming pacing: `MAX_MATERIALIZATIONS_PER_FRAME 1` with early `_collect_finished_jobs(pc)` + freed-Zombie Variant guard (`is_instance_valid` + `!is_queued_for_deletion()` + `is_inside_tree()` before `global_transform`) documented as intentional pacing fix from `2a423d9` (do not revert; keep `MAX_INFLIGHT 6`). Note unified collider `54` peak vs previous `63` with double-count. `ChunkManager.debug_lines()` and `F3` overlay `active rural` include hearth.
- `docs/world/WORLD-CONTRACT.md` §16 → §17 (or §16 addendum) , `ARCHITECTURE.md`/`DEVELOPMENT.md` streaming/collision notes (`ACTIVE-only` clarification now includes rural hearth `Area3D` `monitorable` `ACTIVE-only`, `280/210` `480/360` unchanged but peak `63->54` fix, hearth stove/bed `ACTIVE-only`, well `Area3D` not `StaticBody`, `t_rural_gen`/`t_rural_mat` `4 ms`, stove cook `Canned Food x1 -> hunger -40` `bed Sleep until dawn 480 min -> fatigue -40`), and folding surviving `C001-C007` deferred documentation findings. Also tighten `debug/rural_test.gd` harness per deferred: strict hamlet `2-3`, real road seam sampling at shared border (replace stub `return true` with `RoadChunkBuilder.build_manifest` `0.02` check), `-Z` probe, spacing via `aabb` edge distance `>=8` and `0.5` gap, road setback uniform `>=4.0`, harness typical `280/210` strict, `generators` `GENERATOR_VERSION 2`.






## 18. Field-Parcel Cultivation slice P5.1 (tilled arable parcels with hedgerow-bounded CropPatch rows)

### 18.1 Vocab and constants (authoritative in `WorldConstants`)
- Field parcel vocab `FIELD_PARCEL_VOCAB`: `wheat`, `barley`, `potato`, `beet` (4); `CROP_VOCAB` reuse same 4, mapped to ItemDB `canned_food 40%` (`wheat`), `water_bottle 25%` (`barley`), `bandage 20%` (`potato`), `antibiotics 15%` (`beet`) weighted via `field_parcel_crop` domain.
- Size `FIELD_PARCEL_SIZE_MIN Vector2(18,14)` `MAX Vector2(64,48)` (area ~400-3000 m2 Bohemian strip), `FIELD_PARCEL_MAX_PER_LANDSCAPE_CELL 3`, `FIELD_PARCEL_MAX_PER_CHUNK 4`, `FIELD_DENSITY_MIN 0.38`, `FIELD_CROP_MAX_PER_CHUNK 4`, `FIELD_PARCEL_ROAD_SETBACK 3.0`, `FIELD_PARCEL_BUILDING_GAP 8.0`, `FIELD_PARCEL_SPACING_MIN 4.0` (`AABB_GAP 4.0`), `FIELD_PARCEL_WELL_FORAGE_GAP 6.0`, `CROP_GROW_DAYS 2`, `CROP_REGROW_DAYS 2`, `FIELD_PARCEL_LIFT_M 0.02` (tilled quad at terrain+0.02, hedgerow at terrain+0.02), `HEDGEROW_HEIGHT 0.6`, `FIELD_PARCEL_HEDGEROW_COLOR 5a7a3a`, `COL_FIELD_WHEAT c2b280` `BARLEY 8faa6a` `POTATO 6e635a` `BEET 6a8a5a`, `MAX_FIELD_VERTS_PER_CHUNK 96` `MAX_FIELD_TRIS_PER_CHUNK 64` typical `64/32` for 2 parcels dense `96/64` for 4 parcels, `FIELD_HEDGEROW_MAX_PER_CHUNK 8` of global `MAX_BIOME_INSTANCES_PER_CHUNK 48`, `FIELD_PARCEL_HEIGHT_VARIANCE_MAX 0.8`.
- Budgets: overlay `81/128` + tilled `96/64` per chunk (typical `64/32`, dense `96/64`), hedgerow `<=8` of `48`, at most `1` biome collider per chunk `9` active, at most `4` field parcels `4` CropPatch `Area3D` per chunk `36` active crops (`9*4`), `FRAME_BUDGET_MS 12`.

### 18.2 Generation contract (landscape-cell coherent 256 m, hedgerow-bounded, road/track/contour aligned)

- Deterministic field parcels per `256 m` landscape cell (`LANDSCAPE_CELL_M`), macro `1024 m` region, via `WorldSeed.sample_coherent` / `unit_float` with new `FIELD_PARCEL_DOMAINS` ordered `[field_parcel, field_parcel_density, field_parcel_crop, field_parcel_yaw]` after existing `RURAL_RESOURCE_DOMAINS`, seed-separated from terrain/hydro/geology/biome/settlement/road/rural_building/rural_interior/rural_resource, `floori` lattice for negative coords, no RNG sharing, no per-query mutable state, `LANDSCAPE_CELL_M 256` parcel lattice: for each landscape cell `(cx,cy)=floor(p/256)`, decide parcel count `0-3` if cell's majority biome is `arable_field/pasture/pasture_orchard/orchard` else `0`, with village-adjacent `+1` bias if cell center within `settlement.radius*1.4` of a `village` anchor. Coherent `field_density` noise at cell center decides `count = int(floor(clamp01(field_density)*3)) + (1 if village_adjacent else 0)` capped `<=3` per cell, plus `field_parcel` `unit_float` for precise `0-3` variety. Density threshold `FIELD_DENSITY_MIN 0.38` below which `0` parcels even in arable.
- Per-parcel `crop_kind` weighted `wheat 35 / barley 30 / potato 20 / beet 15` via `unit_float("field_parcel_crop", [hash(cell), k])` mapped without duplicate; hedgerow boundary is implicit between parcels and at cell border where adjacent cell's biome also arable — shared border carries hedgerow.
- Siting per parcel `k` in cell: `pos = cell_origin + Vector2(lerp(12, LANDSCAPE_CELL_M-12, unit_float(...k,0)), lerp(12, LANDSCAPE_CELL_M-12, unit_float(...k,1))))` then snapped to `aabb` `Vector2(lerp(18,64, unit_float(...k,2)), lerp(14,48, unit_float(...k,3))))` (parcel footprint `18-64 x 14-48`, area `~400-3000 m2` Bohemian strip), inset `2.0` from cell border where hedgerow placed. Validate: parcel `aabb` must be fully inside allowed biome (`biome_at` center `== arable_field` family), `terrain_class != cliff`, `slope < ARABLE_MAX_SLOPE_DEG 12` (pasture/orchard `PASTURE_MAX 14` if that biome), `hydrology.water_body_at == ""` and not `is_floodplain` and `distance_to_water > BANK_W+2`, `road_network.distance_to_road >= 3.0` (parcel may abut rural `track` at `3 m` but not cross primary/secondary `hydrology crossing` — reject if `is_bridge` nearby or `distance_to_road < 1.5` for primary); `terrain.height_at(pos)+0.02` is flat within `0.8` height variance across `aabb` corners else shift jitter attempts `8`. Also `>=8` from any rural building `aabb` and `>=6` from any well/forage `CropPatch` center, spacing `>=4` from other parcel `aabb` edge gap via `aabb_gap`. If violated, try `12` deterministic nudge attempts via `unit_float("field_parcel", [hash(cell),k,attempt])*12` at up to `12` attempts; if still invalid the parcel is dropped (keeps spacing contract) rather than overlapping.
- Orientation `yaw` is `0/PI*0.5` deterministic cardinal aligned to nearest `track` tangent if `dist_to_road < 40` else quantized `0/90` via `unit_float("field_parcel_yaw")`, keeping furrow rows axis-aligned for batching and hedgerow axis-aligned.
- Growth stage deterministic via `GameClock.get_day()` and parcel `planted_day` seeded: `planted_day = int(hash(cell,k) % 30)` plus `GameClock.get_day()` offset? Simpler: parcel's `planted_day` is deterministic `0 + int(unit_float(...)*7)` so growth `days_since_plant = GameClock.get_day() - planted_day` modulo `GROWTH_CYCLE 6` yields `planted/growing/harvestable` gates. For test provability, harness will call `GameClock.advance(2*1440+10)` to make depleted regrow and also verify harvestable `is_grown` gate via `cur_day >= planted_day+2`. `CropPatch` contents map crop_kind to `ItemDB` `wheat->canned_food`, `barley->water_bottle`, `potato->bandage`, `beet->antibiotics` or weighted equivalent but at least `2` distinct crop kinds across 5 seeds.
- Gates and urban exclusion unchanged: no parcel whose `center.length() < URBAN_INNER_M` (`350`); parcels never outside `WORLD_HALF_EXTENT`; `GENERATOR_VERSION` stays `2`.

### 18.3 Field-parcel chunk manifest and materialization (tilled quads + hedgerow MultiMesh + CropPatch Area3D, 96/64 vs 81/128 overlay)

- Per-chunk manifest via `BiomeChunkBuilder.build_manifest(WorldPlan, coord)` now includes `field_parcel_manifests (Array clipped by center inside rect, per-parcel id/center/pos/aabb/biome/crop_kind/size/yaw/planted_day/growth_stage/is_grown/contents)` plus `field_parcels/field_crops/field_vertices/field_triangles/field_hedgerow` counts, tilled quad vertices at `terrain+0.02` with crop-specific vertex colors `c2b280/8faa6a/6e635a/6a8a5a` plus hedgerow `MultiMesh` at parcel borders via field hedgerow Box `2.0x0.6x0.4` vertex-colored `5a7a3a` (2 per parcel capped 8 of 48), at most `1` biome collider per chunk (0/1) plus `<=4` field parcels `<=4` CropPatch `hedgerow <=8` of `48`, overlay `81/128` plus field tilled `<=96/64` per chunk typical `64/32` dense `96/64`, `biome_instances <=48`, `3x3` ACTIVE `<=9` biome colliders monitorable not counted to `54` peak not `63`, `t_biome_gen/mat` within `FRAME_BUDGET_MS 12`.
- Visual ground overlay: vertices at `(x, terrain_height+0.03, z)` plus tilled quads at `terrain+0.02` with crop vertex colors, `StandardMaterial vertex_color_use_as_albedo`. `81/128` budgets. Instanced dressing: field hedgerow `2` per parcel `<=8` of `48`. At most one biome collider per chunk, `ACTIVE-only`, warm retains visuals but disables `BiomeBody` collision.
- Clipping via center inside rect, no duplication at `+`/`-`/`-Z`.

### 18.4 Streaming, telemetry, persistence, compatibility

- `WorldConstants` single source for field parcel numerics (vocab, footprints, thresholds, density, `81/128` overlay `96/64` tilled, lifts, growth days) and `WorldSeed` domain lists extend only if new `FIELD_PARCEL` domains added (explicit ordered `Array[StringName]` after existing `RURAL_RESOURCE_DOMAINS`).
- `WorldPlan` facade owns `BiomePlan` enriched and forwards unchanged `biome_at`, `surface_tint_at`, `biome_density_at`, `water_body_at` plus **new `field_parcels_in`/`nearest_field_parcel`** pure queries. No new autoload or `WorldPlan` file replacement is authorized beyond the parcel forwarding; plan remains pure and `plan_mutex` guards any `CityPlan` caches exactly as before. `WorldPlan._init` constructs seven-or-eight plans from same seed; plans are `RefCounted` pure.
- `ChunkManager` mirrors previous pipeline plus field: new counters `_field_parcels_total/_field_crops_total`, per-chunk `field_parcels/field_crops/field_manifests`, measures `biome_gen_ms` now shell+tilled+hedgerow+CropPatch derivation within same `biome_gen_ms`, `biome_mat_ms` with `CropPatch` instantiation. Active-ring: shell+well collider `ACTIVE` only, warm retains `BiomeMesh` visuals (including tilled+hedgerow) but disables `RuralBody` collision and `CropPatch` `Area3D` `monitorable` only when `ACTIVE`; at most `9` active biome colliders (`3x3`) and `<=4` `CropPatch` per chunk `<=36` active crops (`9*4`). Warm retains `BiomeMesh` visuals (including tilled+hedgerow) but disables `BiomeBody`. Persistence: `save_state()` persists `seed/version/discovery/deltas` including `deltas.field_crops : {crop_id: {depleted:bool, depleted_at_day:int, contents:{item:count}, planted_day:int}}` sibling to `crates/wells/forage/doors/damage`; generated field parcels/crop quads/hedgerow never serialized, re-derived deterministically, deltas re-applied before materialize with `GameClock` regrow `2` days and `is_grown` gate `planted+2` proven via `GameClock.advance` mock.
- Telemetry and docs: `docs/world/WORLD-CONTRACT.md` §18 documents vocab, per-landscape-cell siting (density `0.38` + `village-adjacent +1` bias, `0-3` per `256` cell, footprint `18-64x14-48` `terrain+0.02` `aabb` gaps, road `3.0` building `8` spacing `4` `ARABLE_MAX 12`), landscape-cell contiguity not per-chunk speckles, hedgerow at parcel borders, tilled quad manifest (`96/64` max, `ACTIVE-only` `CropPatch` `Area3D` `monitorable` rule, furrow direction), save exclusion, compatibility/migration note (`GENERATOR_VERSION` stays `2`, additive outside dense core).

## 19. Orchard Row Cultivation slice P5.2 (hedgerow-bounded canopy rows with harvestable FruitPatches and true-mesh hedgerow refinement)

### 19.1 Vocab and constants (authoritative in `WorldConstants`)

- Orchard parcel vocab `ORCHARD_PARCEL_VOCAB`: `apple`, `plum`, `pear`, `cherry` (4) weighted `apple 40 plum 30 pear 15 cherry 15` via `orchard_parcel_fruit` domain; `FRUIT_VOCAB` same 4, mapped to ItemDB distinct fruit items `apple hunger 18 plum 16 pear 14 cherry 12`.
- Size `ORCHARD_PARCEL_SIZE_MIN Vector2(20,16)` `MAX Vector2(68,52)` (area ~320-3500 m2), `ORCHARD_PARCEL_MAX_PER_LANDSCAPE_CELL 2`, `ORCHARD_PARCEL_MAX_PER_CHUNK 3`, `ORCHARD_DENSITY_MIN 0.42`, `ORCHARD_CROP_MAX_PER_CHUNK 3` `FRUIT_MAX_PER_CHUNK 3`, `ORCHARD_PARCEL_ROAD_SETBACK 3.0`, `BUILDING_GAP 8.0`, `SPACING_MIN 4.0` (`AABB_GAP 4.0`), `WELL_FORAGE_GAP 6.0`, `FRUIT_GROW_DAYS 3` `FRUIT_REGROW_DAYS 3`, `ORCHARD_PARCEL_LIFT_M 0.04` (canopy rows at terrain+0.04, hedgerow at terrain+0.04, tilled corrected 0.02->0.04), `ORCHARD_TREE_SPACING 5.0` `ROW_SPACING 5.5` `ORCHARD_MAX_SLOPE 14`, `COL_TRUNK 6b5a4a` `COL_CANOPY 3a7a3a/4a6a4a/6a8a5a/7a5a6a` `ORCHARD_HEDGEROW_MAX_PER_CHUNK 6` `MAX_ORCHARD_INSTANCES 12` of global `MAX_BIOME_INSTANCES 48` (field hedgerow 8 + orchard hedgerow 6 + canopy 12 share 48), `HEDGEROW_TRUE 2.0x0.45-0.75x0.4` `5a7a3a` vertex-colored true-mesh not stretched, `HEDGEROW_TRUE_LENGTH 2.0` `WIDTH 0.4` `HEIGHT_MIN 0.45` `MAX 0.75`.
- Corrected `FIELD_PARCEL_LIFT_M 0.02->0.04` (tilled quads now 0.01 above overlay 0.03 exposing furrows) and hedgerow upgraded to true-mesh `2.0x0.45-0.75x0.4` vertex-colored `5a7a3a` with height jitter 0.45-0.75 (not stretched Box 1x1x1 scaled).
- Budgets: overlay `81/128` + tilled `96/64` at 0.04 per chunk + orchard canopy `12` of `48` plus hedgerow `8` field + `6` orchard of `48`, at most `1` biome collider per chunk `9` active, at most `4` field parcels `3` orchard parcels `4` CropPatch `3` FruitPatch `Area3D` per chunk `36` active crops + `27` active fruit (`9*4` + `9*3`), `FRAME_BUDGET_MS 12`.

### 19.2 Generation contract (landscape-cell coherent 256 m, hedgerow-bounded canopy rows, road/track/contour aligned)

- Deterministic orchard parcels per `256 m` landscape cell (`LANDSCAPE_CELL_M`), macro `1024 m` region, via `WorldSeed.sample_coherent` / `unit_float` with new `ORCHARD_PARCEL_DOMAINS` ordered `[orchard_parcel, orchard_parcel_density, orchard_parcel_fruit, orchard_parcel_yaw]` after existing `FIELD_PARCEL_DOMAINS`, seed-separated, `floori` lattice for negative coords, no RNG sharing, no per-query mutable state, `LANDSCAPE_CELL_M 256` parcel lattice: for each landscape cell `(cx,cy)=floor(p/256)`, decide parcel count `0-2` if cell's majority biome is `orchard/pasture_orchard` else `0`, with village-adjacent `+1` bias if cell center within `settlement.radius*1.35` of a `village` anchor. Coherent `orchard_density` noise at cell center decides `count = int(floor(clamp01(orchard_density)*2)) + (1 if village_adjacent else 0)` capped `<=2` per cell, plus `orchard_parcel` `unit_float` for precise `0-2` variety. Density threshold `ORCHARD_DENSITY_MIN 0.42` below which `0` parcels even in orchard.
- Per-parcel `fruit_kind` weighted `apple 40 / plum 30 / pear 15 / cherry 15` via `unit_float("orchard_parcel_fruit", [hash(cell), k])` mapped without duplicate; hedgerow boundary implicit between parcels and at cell border where adjacent cell's biome also orchard — shared border carries hedgerow true-mesh.
- Siting per parcel `k` in cell: `pos = cell_origin + Vector2(lerp(14,256-14, unit_float(...k,0)), lerp(14,256-14, unit_float(...k,1))))` then snapped to `aabb` `Vector2(lerp(20,68, unit_float(...k,2)), lerp(16,52, unit_float(...k,3))))` (parcel footprint `20-68 x 16-52`, area `~320-3500 m2` Bohemian orchard), inset `2.0` from cell border where hedgerow placed. Validate: parcel `aabb` must be fully inside allowed biome (`biome_at` center `== orchard` family `orchard/pasture_orchard`), `terrain_class != cliff`, `slope < ORCHARD_MAX_SLOPE_DEG 14`, `hydrology.water_body_at == ""` and not `is_floodplain` and `distance_to_water > BANK_W+2`, `road_network.distance_to_road >= 3.0` (parcel may abut rural `track` at `3 m` but not cross primary/secondary — reject if `is_bridge` nearby or `distance_to_road < 1.5` for primary); `terrain.height_at(pos)+0.04` is flat within `0.9` height variance across `aabb` corners else shift jitter attempts `8`. Also `>=8` from any rural building `aabb` and `>=6` from any well/forage `FruitPatch`/`CropPatch` center, spacing `>=4` from other orchard/field parcel `aabb` edge gap via `aabb_gap`. If violated, try `12` deterministic nudge attempts via `unit_float("orchard_parcel", [hash(cell),k,attempt])*12` at up to `12` attempts; if still invalid the parcel is dropped (keeps spacing contract) rather than overlapping.
- Orientation `yaw` is `0/PI*0.5` deterministic cardinal aligned to nearest `track` tangent if `dist_to_road < 40` else quantized `0/90` via `unit_float("orchard_parcel_yaw")`, keeping canopy rows axis-aligned for batching and hedgerow axis-aligned.
- Canopy rows: for each orchard parcel, `tree_rows = 3` if `size.x < 40` else `4` rows along long axis, `trees_per_row = 3` if `size.y < 30` else `4-5` trees per row spaced `ORCHARD_TREE_SPACING 5.0` with jitter `+-0.6` via `orchard_parcel` domain, trunk at `pos + row_offset + col_offset` at `terrain+0.04`, canopy `1.4x1.0x1.4` `3a7a3a` (apple `3a7a3a` plum `4a6a4a` pear `6a8a5a` cherry `7a5a6a` tint via fruit_kind but at least `2` kinds distinct).
- Growth stage deterministic via `GameClock.get_day()` and parcel `planted_day` seeded: `planted_day` is deterministic `0 + int(unit_float(...)*7)` so growth `days_since_plant = GameClock.get_day() - planted_day` yields `growing` until `planted+3` harvestable. `FruitPatch` contents map `apple->apple plum->plum pear->pear cherry->cherry` new `ItemDB` food items `hunger 18/16/14/12` distinct, at least `2` distinct fruit kinds across 5 seeds.
- Gates and urban exclusion unchanged: no parcel whose `center.length() < URBAN_INNER_M` (`350`); parcels never outside `WORLD_HALF_EXTENT`; `GENERATOR_VERSION` stays `2`.

### 19.3 Orchard-parcel chunk manifest and materialization (canopy rows + true-mesh hedgerow + FruitPatch Area3D, orchard canopy 12 vs field tilled 96/64 vs overlay 81/128)

- Per-chunk manifest via `BiomeChunkBuilder.build_manifest(WorldPlan, coord)` now includes `orchard_parcel_manifests (Array clipped by center inside rect, per-parcel id/center/pos/aabb/biome/fruit_kind/size/yaw/planted_day/growth_stage/is_grown/contents/tree_instances/tree_rows/trees_per_row)` plus `orchard_parcels/fruit_patches/orchard_vertices/orchard_triangles/orchard_hedgerow/orchard_instances` counts, canopy MultiMesh rows 3-4 rows 3-5 trees 5.0 spacing trunk 6b5a4a + canopy 3a7a3a/4a6a4a etc at terrain+0.04 plus hedgerow true-mesh at parcel borders via orchard hedgerow Box `2.0x0.45-0.75x0.4` vertex-colored `5a7a3a` (2 per parcel capped 6 of 48) plus existing field hedgerow true-mesh `2.0x0.45-0.75x0.4` (2 per parcel capped 8), at most `1` biome collider per chunk (0/1) plus `<=4` field parcels `<=3` orchard parcels `<=4` CropPatch `<=3` FruitPatch hedgerow `<=8` field `<=6` orchard of `48` plus canopy `<=12` of `48`, overlay `81/128` plus field tilled `<=96/64` at `0.04` plus orchard canopy `12` of `48`, `biome_instances <=48`, `3x3` ACTIVE `<=9` biome colliders monitorable not counted to `54` peak not `63`, `t_biome_gen/mat` within `FRAME_BUDGET_MS 12`.
- Visual ground overlay: vertices at `(x, terrain_height+0.03, z)` plus tilled quads at `terrain+0.04` with crop vertex colors, orchard canopy rows at `terrain+0.04` trunk+canopy MultiMesh vertex-colored, `81/128` overlay budgets. Instanced dressing: field hedgerow `2` per parcel `<=8` of `48` true-mesh, orchard hedgerow `2` per parcel `<=6` of `48` true-mesh, canopy `3-4` rows `3-5` trees `<=12` of `48`. At most one biome collider per chunk, `ACTIVE-only`, warm retains visuals but disables `BiomeBody` collision.
- Clipping via center inside rect, no duplication at `+`/`-`/`-Z`, shared road centerline `0.02`.

### 19.4 Streaming, telemetry, persistence, compatibility

- `WorldConstants` single source for orchard parcel numerics (vocab, footprints, thresholds, density, `81/128` overlay `96/64` tilled `0.04` `12` canopy, lifts, growth days) and `WorldSeed` domain lists extend only if new `ORCHARD_PARCEL` domains added (explicit ordered `Array[StringName]` after existing `FIELD_PARCEL_DOMAINS`).
- `WorldPlan` facade owns `BiomePlan` enriched and forwards unchanged `biome_at`, `surface_tint_at`, `biome_density_at`, `water_body_at` plus **new `orchard_parcels_in`/`nearest_orchard_parcel` and `fruit_patch_for_parcel`** pure queries, plus existing `field_parcels_in`. No new autoload or `WorldPlan` file replacement is authorized beyond the parcel forwarding; plan remains pure and `plan_mutex` guards any `CityPlan` caches exactly as before. `WorldPlan._init` constructs seven-or-eight plans from same seed; plans are `RefCounted` pure.
- `ChunkManager` mirrors previous pipeline plus orchard: new counters `_orchard_parcels_total/_fruit_patches_total/_orchard_instances_total`, per-chunk `orchard_parcels/fruit_patches/orchard_manifests/orchard_instances`, measures `biome_gen_ms` now overlay+tilled 0.04+hedgerow true-mesh+canopy+fruit derivation within same `biome_gen_ms`, `biome_mat_ms` with `FruitPatch` instantiation. Active-ring: shell+well collider `ACTIVE` only, warm retains `BiomeMesh` visuals (including tilled+hedgerow+canopy) but disables `RuralBody` collision and `FruitPatch` `Area3D` `monitorable` only when `ACTIVE`; at most `9` active biome colliders (`3x3`) and `<=4` `CropPatch` per chunk `<=36` active crops (`9*4`) plus `<=3` `FruitPatch` per chunk `<=27` active fruit (`9*3`). Warm retains `BiomeMesh` visuals (including tilled `0.04`+hedgerow true-mesh+canopy visuals) but disables `BiomeBody` (`collision_layer=0`) and `CropPatch`/`FruitPatch` `Area3D` `monitorable=false` + `Interactable.enabled=false` when warm or depleted or not grown, same pattern as rural `Well`/`ForagePatch`/`CropPatch` leaves parented under same `Biome_X_Y`. Document single `BiomeBody` `1` collider sharing overlay+tilled, hedgerow+canopy `MultiMesh` not collider. Persistence: `save_state()` persists `seed/version/discovery/deltas` including `deltas.fruit_patches : {fruit_id: {depleted:bool, depleted_at_day:int, contents:{item:count}, planted_day:int, fruit_kind}}` sibling to `field_crops/crates/wells/forage/doors/damage`; generated orchard parcels/canopy quads/hedgerow true-mesh never serialized, re-derived deterministically, deltas re-applied before materialize with `GameClock` regrow 3 days and is_grown gate `planted+3` proven via `GameClock.advance` mock.
- Telemetry and docs: `docs/world/WORLD-CONTRACT.md` §19 documents vocab, per-landscape-cell siting (density `0.42` + `village-adjacent +1` bias, `0-2` per `256` cell, footprint `20-68x16-52` `terrain+0.04` `aabb` gaps, road `3.0` building `8` spacing `4` `ORCHARD_MAX 14`), landscape-cell contiguity not per-chunk speckles, hedgerow true-mesh at parcel borders `2.0x0.45-0.75x0.4` `5a7a3a`, canopy rows `3-4` rows `3-5` trees `5.0` spacing trunk+canopy at `terrain+0.04`, `FruitPatch` `Area3D` `monitorable` rule, furrow lift correction `0.04` vs overlay `0.03`, save exclusion, compatibility/migration note (`GENERATOR_VERSION` stays `2`, additive outside dense core).
- **Windowed player-facing proof** archived under `.hermes/autopilot/reports/SPEC-C010-windowed.*` (PNG+log) from normal windowed `CITY` run `1200x720` showing `WASD/E` door swing (closed blocks/open clears without `RID` exclusion/swung collidable), stair to roof camera following, `F3` overlay with `active biome` `81/128` plus `t_biome_gen`/`t_biome_mat` and `field_parcels`/`field_crops`/`orchard_parcels`/`fruit_patches` alongside `active road`/`active water`/`active terrain`/`active rural` with `hearth`, then `600-900 m` east along rural road corridor to river valley showing continuous tilled parcels `0.04` wheat/barley furrow bounded true hedgerow `0.45-0.75m` plus orchard canopy rows `3-4` rows `3-5` trees trunk `0.35x1.8 + canopy 1.4x1.0x1.4` at `0.04` `200-600 m` blocks bounded by true hedgerow enclosures visible across `256 m` landscape cells, not speckles, plus `FruitPatch` Harvest apples via `E` `Area3D` without `RID` exclusion; `E` takes one item prompt updates to `Picked clean — regrows in 3 days` and `is_depleted` disables `monitorable` until `GameClock 2/3d` plus `Growing` gate; Rural shells `2-6` low-rise with working doors set back `7.0/5.0/3.5` plus bridge deck `+0.35` plus `Stove`/`Bed` hearth plus `Well`/`Forage` true hedgerow remain without seam cracks at `+` and `-` and `-Z`. PNG+log under `.hermes/autopilot/reports/SPEC-C010-windowed.*` referenced; `ARCHITECTURE.md`/`DEVELOPMENT.md` updated for `ACTIVE-only` `FruitPatch`, `81/128+96/64 0.04+canopy 12`, streaming pacing `1-per-frame` + freed-Zombie guard, `WORLD-CONTRACT` `§19`, hedgerow dedup.


## 20. Rural Workbench Economy slice P5.3 (village barn mill/press with distinct grain harvest and crafted bread/cider streamed ACTIVE-only)

### 20.1 Vocab and constants (authoritative in `WorldConstants`)

- Workbench vocab `RURAL_WORKBENCH`: per-village barn/stable `1` with hamlet `0-1` 50% via `rural_workbench_hamlet_roll`, `farmstead` `0`, reuse furniture anchor `shelf/table` inset `0.5` `0.9`/`1.0` gates, `8` spacing between workbenches, `terrain+0.04`, settlement-coherent not per-chunk speckles, workbench `Box 1.2x0.9x0.6` `7a6a5a` at `terrain+0.04`, `Workbench Area3D` `monitorable` only when `ACTIVE` (warm `monitorable=false` + `Interactable.enabled=false`), `CropPatch` distinct `wheat_grain/barley_grain` mapping, `ItemDB` new `wheat_grain/barley_grain/flour/bread/cider` `hunger 12/10/14/42` `thirst 38` `hunger 8`, save exclusion, compatibility/migration note (`GENERATOR_VERSION` stays `2`, additive outside dense core).
- Caps: `RURAL_WORKBENCH_MAX_PER_CHUNK 2`, `RURAL_WORKBENCH_MAX_PER_VILLAGE 1`, `RURAL_WORKBENCH_MAX_PER_HAMLET 1`, `RURAL_WORKBENCH_MAX_PER_FARMSTEAD 0`, `RURAL_WORKBENCH_SPACING_MIN 8.0`, `RURAL_WORKBENCH_BUILDING_GAP_MIN 1.0`, `RURAL_WORKBENCH_SIZE Vector3(1.2,0.9,0.6)`, `COL_WORKBENCH 7a6a5a`, `WORKBENCH_LIFT_M 0.04`, `RURAL_WORKBENCH_WELL_GAP_MIN 6.0`, `RURAL_WORKBENCH_FORAGE_GAP_MIN 6.0`, `RURAL_WORKBENCH_ROAD_SETBACK 3.0`.
- Budgets: rural `MAX_RURAL_VERTS_PER_CHUNK 480` typical `280`, `MAX_RURAL_TRIS_PER_CHUNK 360` typical `210` **unchanged** (workbench reuses furniture mesh 0 extra verts, or 24/12 but still within 480/360, hamlet/farmstead 1-2 buildings+1 well+2 forage+1 hearth+0-1 workbench <=280/210, village 4-6 buildings+2 wells+4 forage+2 stoves+2 beds+1 workbench still within 480/360), `MAX_RURAL_COLLIDERS_PER_CHUNK 1` (shell+well), `MAX_ACTIVE_RURAL_COLLIDERS 9` unified peak **54 not 63**, `Workbench Area3D` `monitorable` ACTIVE-only not counted to 54, `MAX_RURAL_WORKBENCH_MAX_PER_CHUNK 2` per chunk `<=18` active workbenches (9*2) monitorable ACTIVE-only.
- Distinct grain: `CROP_VOCAB` now maps `wheat->wheat_grain hunger 12`, `barley->barley_grain hunger 10`, `potato->bandage`, `beet->antibiotics` (medical until broader split), `CROP_GROW_DAYS 2` `CROP_REGROW_DAYS 2`, `ItemDB` `wheat_grain 12 barley_grain 10 flour 14 bread 42 cider thirst 38 hunger 8` distinct from `canned_food 40` `water_bottle 45` `apple 18` etc, `Workbench` recipes `Mill flour (Wheat Grain x2 -> Flour x1)` `Bake bread (Flour x1 -> Bread x1)` `Press cider (Apple x2 -> Cider x1)` via `InventoryComponent` remove/add stateless, prompt priority `Bake > Press > Mill` vs `needs`.

### 20.2 Generation contract (per-settlement barn workbench reusing furniture anchor)

- Deterministic workbench per settlement anchor: for each settlement `s` where `s.kind == village` select exactly `1` workbench hosted in its `barn/stable` building (choose barn/stable with smallest `id_hash` among that settlement's barn/stable list; if settlement has no barn/stable, no workbench). For `hamlet`, `farmstead`, `isolated_farm`: `hamlet` gets `0-1` workbench with `50%` chance via `unit_float("rural_workbench_hamlet_roll", [hash(settlement.id)]) > 0.5` if hamlet has a barn/stable; `farmstead`/`isolated_farm` get `0` this slice. Village workbench always present when barn/stable exists (strict `1` per village). This yields per-`1024 m` macro cell `0-2` workbenches clustered by settlement spacing `700/420`, not per-`64 m` speckles.
- Per-workbench siting: reuse the host barn/stable building's **furniture anchor** (hearth pattern) — select the building's furniture entry where `kind==shelf` or `table` if exists, else use building center inset `0.7` from wall toward interior, validated `aabb` fully inside building `aabb` inset `0.5` and `>=0.9` from other furniture `aabb` gap and `>=1.0` from door swing `1.0`, `terrain.height_at(pos)+0.04` flat within `0.8` variance across `aabb` corners else shift jitter attempts `4` via `unit_float("rural_workbench", [hash(building.id), attempt]) * 1.2`. Orientation `yaw` copied from building `yaw` (cardinal). Size `Vector3(1.2, 0.9, 0.6)` box at `terrain+0.04` (or reuse furniture `size` if reusing anchor, keeping visuals within existing rural mesh). Validate: workbench `pos` must be fully inside allowed biome not `cliff`, `terrain_class != cliff`, `slope < BUILDABLE_MAX` (`<14` for village_house host but barn/stable `<22`), `hydrology.water_body_at == ""` and not `is_floodplain` and `distance_to_water > BANK_W+2`, `road_network.distance_to_road >= 3.0` check via building already gates, `>=8` from other workbench `pos` (`8` via `aabb_gap`/`distance`), `>=6` from well/forage, `>=8` from rural buildings `aabb_gap` and `>=3.0` from road via building gate, never floodplain/water/`cliff`/`urban_basin` `URBAN_INNER_M 350` (`350` no barn except gate barn hearth `<=1` per gate, but workbench strict `0` inside `350` unless gate barn is hamlet `50%` — gate barn workbench suppressed inside `350`). If violated, workbench is dropped rather than overlapping — but village barn spacing already ensures `1` per village passes.
- Output `id` stable `rural_workbench_<settlement.id>` (hamlet `rural_workbench_<settlement.id>_<k>` if ever `>1`), `pos` world `Vector2`, `pos3` `Vector3(pos.x, terrain.height_at(pos)+0.04, pos.y)`, `yaw`, `building_id`, `settlement_id`, `settlement_kind`, `building_kind`, `aabb` `Rect2` for chunk clipping. Pure handles negative coords.
- **Distinct grain mapping**: `CropPatch` contents mapping extended in `world/crop_patch.gd` `_default_contents` to return `wheat->wheat_grain` `barley->barley_grain` distinct food items (keep `potato->bandage` `beet->antibiotics` as `CROP_VOCAB` medical until broader split). `world/fruit_patch.gd` unchanged (`apple/plum` etc). `ItemDB` must contain new items (see §4).

### 20.3 Rural workbench chunk manifest and materialization (unified Workbench & collider bake, 280/210 justification unchanged)

- Per-chunk manifest via `RuralBuildingChunkBuilder.build_manifest(WorldPlan, coord)` now enriched: existing keys plus `workbench_manifests`/`rural_workbenches` + `has_workbench` + `rural_workbenches` counts, at least `rural_workbenches (int)` and workbench `id/pos/aabb/yaw/building_id/settlement_id` fields, and attestable vertices/triangles if any (workbench 0 extra verts if reusing furniture mesh). Deterministic for `(seed, coord)` and byte-identical across shuffled builds for all building/door/interior/crate/well/forage/hearth/workbench fields including pos/yaw/kind/building_id/settlement_id. Sampling: for the chunk rect `Rect2(origin, Vector2(64,64))`, query `rural_workbenches_in(rect)` clipped, then filter to ownership by `center` inside rect; for each owned workbench include its dict plus derived `Workbench` `{id: "workbench_%s" % building_id, pos, position: Vector3(pos.x, terrain.height_at(pos)+0.04, pos.y), yaw: building.yaw}`. Workbench size `1.2x0.9x0.6` box at `terrain+0.04`, `MultiMesh` not needed (single `Area3D` per workbench). No new collider for workbench itself; it shares `RuralBody` `Concave` (like hearth) or has `Area3D` only. Indices per workbench `0` verts for workbench (reuses furniture mesh) but `workbench_count` counted toward stats.
- Palette for workbench: vertex-colored `COL_WORKBENCH 7a6a5a` (or `6b5a4a` if reusing furniture shelf color) `StandardMaterial3D vertex_color_use_as_albedo=true` at `terrain+0.04` without z-fighting, `BoxMesh` `1.2x0.9x0.6` if new mesh else reused `shelf/table` box already in chunk mesh. Keep within existing rural mesh budget `480/360` `280/210` — workbench reuses furniture anchor so `0` extra verts/tris, or if new mesh add `24/12` but still within `480/360` because village core `6` buildings `144` verts + well `24` + forage `48` + hearth `0` + workbench `24` = `240` < `480`; hamlet `1-2` buildings `48` + well `24` + forage `24` + hearth `0` + workbench `24` = `120` < `280`. So `480/360` `280/210` unchanged.
- Collision budget: at most **one rural shell+well collider per chunk** remains (`0` if no building/well, `1` if either) — workbench `Area3D` is `Area3D` interactable (`monitorable` `ACTIVE-only`, `collision_layer 0`) with no `Concave`/`BoxShape` counted toward `rural_colliders` or `biome_colliders` or `54` peak; workbench remains `MultiMesh` visual not collider if new mesh else reused. Harness must assert `rural_colliders == (1 if has_collider else 0)` and that `rural_workbenches` count per chunk `<=2` with `workbench <=2` `ACTIVE-only` plus `stoves<=2` `beds<=2` `hearth<=4` `wells<=2` `forage<=4` `doors<=6` `crates<=3` `furniture<=6` and `3x3 ACTIVE` workbenches carry no extra collider (peak stays `54`).

### 20.4 Streaming, telemetry, persistence, compatibility

- `WorldConstants` is single source for rural workbench numerics (vocab, footprints, heights, spacing, setbacks, `8` workbench, `6` well/forage, `8` building, `3` road, lift `0.04`, regrow days, `well 1 day 04:00 forage 2 days` `stove 40` `bed 40` `sleep 480` `well refill 04:00` `forage 2d` `well 1` `forage 2-5` `well 1-2/village` `1/hamlet` `1-2` `forage 2-5` `1/hamlet` `1-2` `forage 2-5` `well 1-2/village` etc, unified collider 54 peak not 63). Builders/tests import, never duplicate inline numbers.
- `WorldPlan` facade owns extended `RuralBuildingPlan` and forwards enriched `rural_buildings`, `rural_buildings_in`, `nearest_rural_building`, `rural_building_at`, `settlement_buildings` plus `rural_wells`/`rural_forage_patches` plus `rural_hearths_in`/`nearest_rural_hearth` plus **new `rural_workbenches_in`/`nearest_rural_workbench`** pure queries, plus settlement/well/forage/hearth/workbench siting (flat `<14`, not floodplain/water/cliff, road setback uniform `4.0` well `2.0` forage, hearth `0.5` `0.9` `1.0`, workbench `0.5` `0.9` `1.0` `8` spacing, `6` well/forage, `8` building, `3` road).
- `WorldPlan` remains pure (no scene mutation on query), `CityPlan` deterministic building IDs and `TerrainPlan` 17x17 manifests unchanged for same seed/coord, spawn anchor still found dry at `urban_basin`, multi-storey building samples unchanged — proved by `--citytest` plus `--terrainmaterialtest` plus `--hydrotest` plus `--biometest` plus `--roadtest` each finishing with `finished with 0 failure(s)` and retained overlap/door/stair/facade/material/geologic/road seams assertions (no duplicate city/rural buildings/wells/forage/hearth/workbench/field/orchard parcels, no terrain seam regression, hydrology `CX`/`meander`/`width` still `530-710`/`72`/`38-50`, biome contiguity still `>=192 m` run-length, district_hint still present, road seam still `0.02` and `9` resident road chunks, `GENERATOR_VERSION` still `2`).
- `ChunkManager` mirrors previous pipeline plus workbench: new counters `_rural_workbenches_total`, per-chunk record fields `rural_workbenches/rural_workbenches_active/workbench_manifests` plus `rural_gen_ms/rural_mat_ms` already exist and must now include workbench generation time within the same `rural_gen_ms` (document that `rural_gen_ms` now measures shell+interior+crate+well+forage+hearth+workbench). Active-ring rule unchanged: rural shell+well collider counted toward `active rural` only when chunk `ACTIVE`; at most `9` active rural colliders (`3x3`). Warm retains `RuralMesh` visuals (including wells/forage/hearth/workbench visuals) but disables `RuralBody` collision (`collision_layer=0`) and `Well`/`Stove`/`Bed`/`Workbench` `Interactable.enabled=false` when warm (`Area3D monitorable=false`), forage `Interactable.enabled=false` when warm. Wire `debug_lines()` to extend `rural verts|tris|colliders|doors|buildings|crates|furniture|wells|forage|hearth|stoves|beds|workbenches t_rural_gen|t_rural_mat active rural (warm)` (at least `rural_workbenches` must appear in debug lines and `F3` overlay). Include workbench counts in `ChunkManager.debug_lines()` and `F3` overlay `active rural` to include workbench derivations. Budgets enforced including well-baked collider `54` peak and workbench `Area3D` counts; crate/well/forage deltas persisted, workbench stateless; no navmesh/grid.
- Workbench functionalization in chunk manager: `ACTIVE-only` shell+well collider and crate/well/forage/hearth/workbench interactability (warm `RuralBody` layer `0`, crates/wells/hearth/workbench `enabled==ACTIVE && not empty/depleted` plus stove `enabled==ACTIVE && player has canned_food` prompt logic, forage/stove/bed/workbench `monitorable==ACTIVE` && not depleted). Persistence: `ChunkManager.save_state()` persists `seed/version/discovery/deltas` including `deltas.crates : {crate_id: {item:count}}` plus `deltas.wells` and `deltas.forage` and `deltas.field_crops : {crop_id: {depleted,depleted_at_day,contents,planted_day,crop_kind}}` with distinct `wheat_grain/barley_grain` plus `deltas.fruit_patches` sibling to `doors/damage`; **workbench are stateless** (no `deltas.workbench`) because crafting affects `InventoryComponent`/`NeedsComponent`/`GameClock` not world deltas; generated workbench leaves never serialized, re-derived deterministically. Well/forage/field_crops/fruit_patches deltas re-applied before materialize with `GameClock` refill well at `04:00` next day and forage after `2` days and crop `2` days and fruit `3` days — now **proven headless** via deterministic `GameClock.advance` test. Save exclusion of generated workbench (stateless), compatibility/migration note (`GENERATOR_VERSION` stays `2`, additive outside dense core, workbench adds `0` verts/colliders).
- Streaming pacing: `MAX_MATERIALIZATIONS_PER_FRAME 1` with early `_collect_finished_jobs(pc)` + freed-Zombie Variant guard (`is_instance_valid` + `!is_queued_for_deletion()` + `is_inside_tree()` before `global_transform`) documented as intentional pacing fix from `2a423d9` (do not revert; keep `MAX_INFLIGHT 6`). Note unified collider `54` peak vs previous `63` with double-count. `ChunkManager.debug_lines()` and `F3` overlay `active rural` include workbench.

## 21. Rural Granary & Provisioning Cache slice P5.4 (village barn granary chest persistent flour/bread storage streamed ACTIVE-only)

### 21.1 Vocab and constants (authoritative in `WorldConstants`)

- Granary vocab `RURAL_GRANARY`: per-village barn/stable `1` with hamlet `0-1` 30% via `rural_granary_hamlet_roll`, `farmstead` `0`, reuse second furniture anchor `shelf/table` inset `0.5` `0.9`/`1.0` gates, `8` spacing between granaries, `6` well/forage, `1.0` workbench, `8` building, `3` road, `terrain+0.04`, settlement-coherent not per-chunk speckles, granary `Box 1.2x0.6x0.8` `6b4a3a` at `terrain+0.04`, `GranaryChest Area3D` `monitorable` only when `ACTIVE` (warm `monitorable=false` + `Interactable.enabled=false`), `capacity 8` any `KIND_FOOD`, `ItemDB` new `wheat_grain/barley_grain/flour/bread/cider` already distinct, save exclusion, compatibility/migration note (`GENERATOR_VERSION` stays `2`, additive outside dense core).
- Caps: `RURAL_GRANARY_MAX_PER_CHUNK 2`, `RURAL_GRANARY_MAX_PER_VILLAGE 1`, `RURAL_GRANARY_MAX_PER_HAMLET 1`, `RURAL_GRANARY_MAX_PER_FARMSTEAD 0`, `RURAL_GRANARY_SPACING_MIN 8.0`, `RURAL_GRANARY_BUILDING_GAP_MIN 8.0`, `RURAL_GRANARY_SIZE Vector3(1.2,0.6,0.8)`, `COL_GRANARY 6b4a3a`, `GRANARY_LIFT_M 0.04`, `RURAL_GRANARY_CAPACITY 8`, `RURAL_GRANARY_WELL_GAP_MIN 6.0`, `RURAL_GRANARY_FORAGE_GAP_MIN 6.0`, `RURAL_GRANARY_WORKBENCH_GAP_MIN 1.0`, `RURAL_GRANARY_ROAD_SETBACK 3.0`, `RURAL_GRANARY_FURNITURE_GAP_MIN 0.9`, `RURAL_GRANARY_DOOR_SWING_GAP 1.0`.
- Budgets: rural `MAX_RURAL_VERTS_PER_CHUNK 480` typical `280`, `MAX_RURAL_TRIS_PER_CHUNK 360` typical `210` **unchanged** (granary reuses furniture mesh 0 extra verts, or 24/12 but still within 480/360, hamlet/farmstead 1-2 buildings+1 well+2 forage+1 hearth+0-1 workbench+0-1 granary <=280/210, village 4-6 buildings+2 wells+4 forage+2 stoves+2 beds+2 workbenches+2 granaries still within 480/360 =264 <480), `MAX_RURAL_COLLIDERS_PER_CHUNK 1` (shell+well), `MAX_ACTIVE_RURAL_COLLIDERS 9` unified peak **54 not 63**, `GranaryChest Area3D` `monitorable` ACTIVE-only not counted to 54, `MAX_RURAL_GRANARY_MAX_PER_CHUNK 2` per chunk `<=18` active granaries (9*2) monitorable ACTIVE-only, hardened workbench `aabb_gap <8 -> false` without intersects gate and `wb_pos.length()<350`.
- Distinct grain already: `wheat_grain 12 barley_grain 10 flour 14 bread 42 cider thirst 38` distinct, `CropPatch` distinct mapping unchanged.

### 21.2 Generation contract (per-settlement barn granary reusing second furniture anchor, hardened workbench)

- Deterministic granary per settlement anchor: for each settlement `s` where `s.kind == village` select exactly `1` granary hosted in its `barn/stable` building (choose barn/stable with second smallest `id_hash` among that settlement's barn/stable list if >=2 barns, second smallest; if 1 barn, same barn different anchor). For `hamlet`: `0-1` granary with `30%` chance via `unit_float("rural_granary_hamlet_roll", [hash(settlement.id)]) >0.70` if hamlet has barn/stable; `farmstead`/`isolated_farm` `0`. Village granary always present when barn exists (strict `1` per village). This yields per-`1024 m` macro cell `0-2` granaries clustered by settlement spacing `700/420`, not per-`64 m` speckles. Granary count independent from workbench (village 1 each, hamlet 50% vs 30%).
- Per-granary siting: reuse host barn/stable building's **second furniture anchor** (granary pattern) — select building's furniture entry where `kind==table` or `shelf` not occupied by workbench if exists, else use building center inset `0.7` from opposite wall toward interior, validated `aabb` fully inside building `aabb` inset `0.5` and `>=0.9` from other furniture/workbench `aabb` gap and `>=1.0` from door swing `1.0`, `terrain.height_at(pos)+0.04` flat within `0.8` variance across `aabb` corners else shift jitter attempts `4` via `unit_float("rural_granary", [hash(building.id), attempt]) *1.2`. Orientation `yaw` copied from building `yaw` (cardinal). Size `Vector3(1.2,0.6,0.8)` chest at `terrain+0.04` (or reuse furniture `size` if reusing anchor, keeping visuals within existing rural mesh). Validate: granary `pos` must be fully inside allowed biome not `cliff`, `terrain_class != cliff`, `slope < BUILDABLE_MAX` (`<22` for barn/stable), `hydrology.water_body_at == ""` and not `is_floodplain` and `distance_to_water > BANK_W+2`, `road_network.distance_to_road >=3.0` check via building already gates, `>=8` from other granary `pos` (`8` via `aabb_gap`/`distance`), `>=6` from well/forage, `>=8` from rural buildings `aabb_gap` and `>=1.0` from workbench, `>=3.0` from road via building gate, never floodplain/water/`cliff`/`urban_basin` `URBAN_INNER_M 350`. If violated, granary dropped rather than overlapping — but village barn spacing already ensures `1` per village passes. Hardened workbench siting: `if _aabb_gap(granary_aabb, other_building_aabb) <8.0: return false` without intersects gate, and urban via `wb_pos.length()<350` not host.center, documented.
- Output `id` stable `rural_granary_<settlement.id>` (hamlet `rural_granary_<settlement.id>_<k>` if ever `>1`), `pos` world `Vector2`, `pos3` `Vector3(pos.x, terrain.height_at(pos)+0.04, pos.y)`, `yaw`, `building_id`, `settlement_id`, `settlement_kind`, `building_kind`, `aabb` `Rect2` for chunk clipping, `capacity 8`. Pure handles negative coords. **Granary contents persistence**: chest holds `Dictionary` `{item_id:count}` initially empty `{}` (capacity `8` total items, not stacks, sum counts `<=8`). Interact deposits/withdraws one item per `E` via `InventoryComponent`.

### 21.3 Rural granary chunk manifest and materialization (unified Granary & collider bake, 280/210 justification unchanged)

- Per-chunk manifest via `RuralBuildingChunkBuilder.build_manifest(WorldPlan, coord)` now enriched: existing keys plus `granary_manifests`/`rural_granaries` + `has_granary` + `rural_granaries` counts, at least `rural_granaries (int)` and granary `id/pos/aabb/yaw/building_id/settlement_id` fields, and attestable vertices/triangles if any (granary 0 extra verts if reusing furniture mesh). Deterministic for `(seed, coord)` and byte-identical across shuffled builds for all building/door/interior/crate/well/forage/hearth/workbench/granary fields including pos/yaw/kind/building_id/settlement_id. Sampling: for the chunk rect `Rect2(origin, Vector2(64,64))`, query `rural_granaries_in(rect)` clipped, then filter to ownership by `center` inside rect; for each owned granary include its dict plus derived `Granary` `{id: "granary_%s" % building_id, pos, position: Vector3(pos.x, terrain.height_at(pos)+0.04, pos.y), yaw: building.yaw}`. Granary size `1.2x0.6x0.8` chest at `terrain+0.04`, `MultiMesh` not needed (single `Area3D` per granary). No new collider for granary itself; it shares `RuralBody` `Concave` (like hearth/workbench) or has `Area3D` only. Indices per granary `0` verts for granary (reuses furniture mesh) but `granary_count` counted toward stats.
- Palette for granary: vertex-colored `COL_GRANARY 6b4a3a` (or `6b5a4a` if reusing furniture shelf/table color) `StandardMaterial3D vertex_color_use_as_albedo=true` at `terrain+0.04` without z-fighting, `BoxMesh` `1.2x0.6x0.8` if new mesh else reused `shelf/table` box already in chunk mesh. Keep within existing rural mesh budget `480/360` `280/210` — granary reuses furniture anchor so `0` extra verts/tris, or if new mesh add `24/12` but still within `480/360` because village core `6` buildings `144` verts + well `24` + forage `48` + hearth `0` + workbench `24` + granary `24` = `264` < `480`; hamlet `1-2` buildings `48` + well `24` + forage `24` + hearth `0` + workbench `24` + granary `24` = `144` < `280`. So `480/360` `280/210` unchanged.
- Collision budget: at most **one rural shell+well collider per chunk** remains (`0` if no building/well, `1` if either) — granary `Area3D` is `Area3D` interactable (`monitorable` `ACTIVE-only`, `collision_layer 0`) with no `Concave`/`BoxShape` counted toward `rural_colliders` or `biome_colliders` or `54` peak; granary remains `MultiMesh` visual not collider if new mesh else reused. Harness must assert `rural_colliders == (1 if has_collider else 0)` and that `rural_granaries` count per chunk `<=2` with `granary <=2` `ACTIVE-only` plus `workbench <=2` `wells<=2` `forage<=4` `furniture<=6` `stoves<=2` `beds<=2` `hearth<=4` `granaries<=2` (hamlet `1`), doors `<=6` crates `<=3` wells `<=2` forage `<=4` of `48` plus canopy `12` if orchard, and the `3x3` ACTIVE ring carries at most **9 active rural colliders** (`active rural` shell count, `GranaryChest`/`Workbench`/`CropPatch`/`FruitPatch Area3D` `monitorable` not counted toward `54` peak) and at most `18` active granaries (`9*2`) `monitorable` plus `18` active workbenches hardened (`9*2`) `monitorable`.

### 21.4 Streaming, telemetry, persistence, compatibility

- `WorldConstants` is single source for rural granary numerics (vocab, footprints, heights, spacing, setbacks, `8` granary, `6` well/forage, `1.0` workbench, `8` building, `3` road, lift `0.04`, regrow days, `well 1 day 04:00 forage 2 days` `stove 40` `bed 40` `sleep 480` etc, unified collider 54 peak not 63). Builders/tests import, never duplicate inline numbers.
- `WorldPlan` facade owns extended `RuralBuildingPlan` and forwards enriched `rural_buildings`, `rural_buildings_in`, `nearest_rural_building`, `rural_building_at`, `settlement_buildings` plus `rural_wells`/`rural_forage_patches` plus `rural_hearths_in`/`nearest_rural_hearth` plus `rural_workbenches_in`/`nearest_rural_workbench` plus **new `rural_granaries_in`/`nearest_rural_granary`** pure queries, plus settlement/well/forage/hearth/workbench/granary siting (flat `<14`, not floodplain/water/cliff, road setback uniform `4.0` well `2.0` forage, hearth `0.5` `0.9` `1.0`, workbench `0.5` `0.9` `1.0` `8` spacing, granary `0.5` `0.9` `1.0` `8` spacing, `6` well/forage, `8` building, `3` road, `1.0` workbench, `8` other granary).
- `WorldPlan` remains pure (no scene mutation on query), `CityPlan` deterministic building IDs and `TerrainPlan` 17x17 manifests unchanged for same seed/coord, spawn anchor still found dry at `urban_basin`, multi-storey building samples unchanged — proved by `--citytest` plus `--terrainmaterialtest` plus `--hydrotest` plus `--biometest` plus `--roadtest` each finishing with `finished with 0 failure(s)` and retained overlap/door/stair/facade/material/geologic/road seams assertions (no duplicate city/rural buildings/wells/forage/hearth/workbench/granary/field/orchard parcels, no terrain seam regression, hydrology `CX`/`meander`/`width` still `530-710`/`72`/`38-50`, biome contiguity still `>=192 m` run-length, district_hint still present, road seam still `0.02` and `9` resident road chunks, `GENERATOR_VERSION` still `2`).
- `ChunkManager` mirrors previous pipeline plus granary: new counters `_rural_granaries_total`, per-chunk record fields `rural_granaries/rural_granaries_active/granary_manifests` plus `rural_gen_ms/rural_mat_ms` already exist and must now include granary generation time within the same `rural_gen_ms` (document that `rural_gen_ms` now measures shell+interior+crate+well+forage+hearth+workbench+granary). Active-ring rule unchanged: rural shell+well collider counted toward `active rural` only when chunk `ACTIVE`; at most `9` active rural colliders (`3x3`). Warm retains `RuralMesh` visuals (including wells/forage/hearth/workbench/granary visuals) but disables `RuralBody` collision (`collision_layer=0`) and `Well`/`Stove`/`Bed`/`Workbench`/`GranaryChest` `Interactable.enabled=false` when warm (`Area3D monitorable=false`), forage `Interactable.enabled=false` when warm. Wire `debug_lines()` to extend `rural verts|tris|colliders|doors|buildings|crates|furniture|wells|forage|hearth|stoves|beds|workbenches|granaries t_rural_gen|t_rural_mat active rural (warm)` (at least `rural_granaries` must appear in debug lines and `F3` overlay). Include granary counts in `ChunkManager.debug_lines()` and `F3` overlay `active rural` to include granary derivations. Budgets enforced including well-baked collider `54` peak and workbench/granary `Area3D` counts; crate/well/forage/field_crops/fruit/granary deltas persisted, workbench stateless; no navmesh/grid.

