# Player clothing and animation — execution / Hermes handoff

Date: 2026-09-11. Canonical checkout: `C:/Vibe Code project/Godot Project/ring-bell`. Direct user assignment authorizes planning followed by implementation: female player, fair skin, hijab, gamis, physically simulated clothing. This supersedes the unrelated cave task for this session only. Do not change VISION or the strategic roadmap.

## Recovery and ownership

Read this file, `VISION.md`, `ARCHITECTURE.md`, `DEVELOPMENT.md`, then Git status and the listed sources. HEAD at inspection: `fe89ad6`. Numerous world-generation files and captures were already dirty; preserve them. The builder lock names PID 38560 dated August 31; that PID was absent at inspection. Do not launch another builder or overwrite unrelated work. No external messages are needed. Record exact test outcomes below; a pending test is not a pass. Resume from actual diff, not assumptions in this plan.

## Inspected implementation

`actors/survivor/survivor.gd::_build_visual` (around line 185) selects female for the player and builds `HumanoidModel`. `actors/humanoid_model.gd::build_human` produces primitive meshes: short female legs ending above ground, bare hair, short skirt. `components/animation/skeleton_factory.gd::attach_model` transfers limb meshes into BoneAttachment3D nodes and transfers skirt to hips. Model root scale does not transfer with those meshes. Preserve existing ten-bone topology and the 19 animation clips in `components/animation/locomotion_library.gd`; `CharacterLocomotion` owns animation. Legacy animator is disabled when the skeleton is present.

`actors/skirt_cloth.gd` is an existing Verlet solver, not native SoftBody3D. It uses local particles without previous attachment transforms, so translation/rotation produces no physical inertia. Vertical rest distance ignores skirt flare; horizontal constraints omit the last row; normals use an incorrect seam neighbor. The collider is a static cylinder and local floor, not animated legs or world geometry. These are concrete repair targets.

## Implementation sequence

1. Add a player-only modest outfit configuration; NPC appearance remains configured independently. Use fair warm skin, muted full-length gamis with matching long sleeves, contrasting hijab. Keep hands and face visible; cover hair, neck, legs. Correct player leg/boot length to existing hip-to-floor measurements. Retain primitive art direction but use curved cloth surfaces instead of boxes for the veil.
2. Repair cloth topology and simulation: rest-position-derived structural and diagonal/shear constraints, all ring rows including hem, pinned boundary inverse masses, wrapped indices and triangle-derived normals. Keep bounded particle counts and iterations. Transport both position histories through the previous/current global attachment transforms to preserve world inertia. Reset on large teleports, clamp large time steps, use time-aware damping. Gravity must be transformed from world space. Add pose-derived body capsule collision and world collision queries excluding the actor's own body. Preserve freeze behavior for corpses. Cloth remains visual and cannot push the player or alter gameplay collision.
3. Hijab: fitted opaque head covering with open face plus simulated shoulder drape. Attach to existing upper-body bone through the existing mesh attachment route. A second cloth configuration can reuse the solver with its own dimensions. Pinned fabric is intentionally fitted; free fabric must move under physics. No claim of full garment self-collision or finite-element simulation.
4. Add an isolated regression scene/script that uses actual Survivor/SkeletonFactory and existing animation clips. Verify player outfit, full leg length, stable finite particles, exact pins, inertia after movement/turn, floor/body clearance, teleport recovery, frozen simulation, all existing animation states. Include a lit rendered preview and save front/side/back/action PNGs. Avoid touching dirty `world/main.gd`: launch the isolated scene directly or use existing animation test entry points.
5. Run `python tools/run_suite.py --import 120`, `--animationtest 120`, and focused cloth test (through wrapper with a scene argument if practical). Run actual renderer preview; inspect images. Existing `--animcapture` provides additional integration evidence. Godot is at `C:/Vibe Code project/Godot Project/Godot_v4.7.2-stable_win64.exe`. Headless is not visual verification. Browser preview can display generated evidence but cannot substitute for actual Godot physics/rendering. Save evidence under `captures/player-clothing/`.

## Acceptance and limitations

Player visibly wears hijab and ankle-length gamis, has fair skin, no visible hair or bare legs in standing/walking. Free skirt and veil respond to acceleration and rotation, settle rather than explode, and respect tested body/floor contacts. Existing locomotion suite remains green. Verify extreme crouch/slide/climb poses and disclose any remaining intersections; do not call incomplete physics finished. Measure runtime cost on this machine and report particle counts. Do not weaken gameplay or tests. Do not run expensive unrelated city suites unless this work changes their dependencies.

## Execution record

Implemented player-only `modest` configuration in `Survivor`; fair skin `f2d9ca`, fitted hijab hood/collar, simulated shoulder drape, curved gamis bodice, ankle-length skirt, covered full-length legs, simulated sleeves. The existing procedural art direction and ten-bone rig remain.

Changed sources: `actors/humanoid_model.gd`, `actors/skirt_cloth.gd`, `actors/survivor/survivor.gd`. Added `debug/player_clothing_preview.gd` and `.tscn`; `tools/run_suite.py` routes `--clothingtest` to this isolated scene. `debug/animation_test.gd` watchdog raised from 80 to 180 simulation seconds after observed timeout in later traversal assertions; assertions remain unchanged.

Cloth uses 324 particles total: 128 skirt, 96 veil, 50 per sleeve. Six constraint passes; current/previous global transforms preserve inertia; structural and crossed diagonal edges cover every row; cached skeleton-driven torso/leg/arm capsules project free particles outside the body; world sweeps use collision mask 1 and exclude actor RID. Pins: skirt waist, veil neckline, sleeves shoulder and cuff. Large translation resets history. Freeze is supported. Normals now follow outward triangle winding.

Verification command: `python tools/run_suite.py --clothingtest 120 --rendered`. Latest run: `ClothingTest finished with 0 failure(s)`, 34 checks including seven animation phases, fixed pins including cuffs, finite bounded positions, world-floor clearance, movement response, teleport recovery and freeze. Four-garment mean physics cost in final loaded run: 5.800 ms over 30 steps; this is a single-machine measurement under concurrent test load, not a city performance guarantee. Earlier 3.090 ms was before cuff repair; do not use it as final performance evidence.

Actual Forward+ renderer: NVIDIA RTX 5060 Laptop GPU. Fourteen PNGs under `captures/player-clothing/`. Inspected standing, running, slide and climb captures. Fixed waist gap, neck coverage, inward normals and disappearing sleeve cuffs found during visual iteration. Browser gallery `captures/player-clothing/index.html` displays those actual frames, not a separate browser physics simulation. Initial preview runs had missing animation-library prefix/deferred initialization bugs; those were fixed and are not final evidence. Final preview uses `locomotion/` clips and samples Slide/ClimbUp at 0.35 seconds.

Final import passed: `[Import] boot OK`, exit 0. Full animation suite first exceeded 120 wall seconds, then reached its original 80 simulation-second watchdog without assertion failures. Extended-watchdog rendered run PASSED in 263 seconds, exit 0, `[AnimationTest] finished with 0 failure(s)`. Shutdown still reports 170 leaked ObjectDB instances and 13 resources in this existing harness; do not represent shutdown as warning-free.

Known scope limits: position-based visual cloth is not native SoftBody3D, has no fabric self-collision or cloth-to-cloth contact. Fitted hood, collar and bodice are bone-driven; loose drape, skirt and sleeves are simulated. Capsule contacts approximate anatomy and particle rays approximate world contact, so this does not establish universal triangle-level collision freedom against arbitrary thin obstacles. No world-generation, movement capsule, saved-game schema or animation clip changed. Further Hermes work must target demonstrated issues, preserve unrelated concurrent edits, and rerun focused rendered evidence after garment changes.
