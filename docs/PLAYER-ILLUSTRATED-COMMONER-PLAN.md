# Illustrated Victorian commoner â€” implementation and Hermes handoff

## Assignment and recovery

2026-09-11, direct user request: improve the actual 3D player beyond tubes, boxes and balls; somewhat realistic illustrated art direction; Victorian-steampunk commoner clothing, neither glamorous nor ragged. Preserve previous requirements: woman, fair skin, hijab, full-length gamis, physical loose clothing. User explicitly authorizes plan first, then execution. This file is written before code changes. Resume from Git and this record if context expires; no extra approval needed for this bounded work.

Canonical checkout `C:/Vibe Code project/Godot Project/ring-bell`. Previous character commit `055ba5e` on `copilot/worldgen-fix`. Many unrelated world-generation edits and reports remain dirty and have changed concurrently. Never stage/reset/rewrite those. Do not publish unrelated branch commits to master. Existing strategic/cave assignment is unrelated; leave it intact. Read VISION, ARCHITECTURE, DEVELOPMENT and previous `docs/PLAYER-CLOTHING-PLAN.md` for contracts. Only one agent is implementing this request.

## Actual inspected sources and defects

`actors/humanoid_model.gd::build_human` currently creates a cylindrical bodice, spherical hood, capsule face panel, box hands/boots and four SkirtCloth nodes. `Survivor` passes `modest=true` for the player. This provides coverage and physical motion but has no authored anatomy, garment tailoring, facial expression, seams, or occupational identity. Merely adding accessories to these primitives will not satisfy this request.

`components/animation/skeleton_factory.gd::attach_model` moves direct MeshInstance3D children from model pivots to existing bone attachments. New art must put every attached mesh directly under the correct pivot; arbitrary nested detail nodes will otherwise be left behind. Hips skirt is found by SkirtCloth script path and attached to hips with zero offset. Skeleton has ten bones; +Z is model-facing, while Survivor can rotate the visual root. Keep gameplay capsule, skeleton topology, animation library, and physics controller unchanged.

`actors/skirt_cloth.gd` uses a 324-particle four-garment PBD/Verlet implementation, six constraint passes, bone capsules, world ray sweeps and pinned cuffs. It supports only circular linear ring profiles and uniform color. Extend rest-profile and material inputs without replacing its verified inertia/contact/freeze implementation. Defaults must retain NPC behavior. Mesh UV/colors must use stable rest coordinates, never swimming world coordinates. Collision remains approximate and lacks self-collision; do not claim otherwise.

## Art specification

An ordinary working woman: muted petrol/charcoal wool gamis, shaped but modest fitted upper garment, high collar, practical long gathered sleeves with cuffs, paneled full skirt, quiet hem facing, warm oatmeal/taupe wrapped hijab. Small dark-brown leather utility belt and side pouch, a few aged brass buttons/clasp and a short watch chain communicate Victorian-steampunk use. No goggles glued to the face, gear spam, corset cleavage, expensive silk, lace extravagance, torn rags or aristocratic bustle.

Human proportions need a jaw/chin, cheek and brow structure, modeled nose, almond eyes, restrained eyebrows and lips. Fair skin uses warm/cool vertex tones; face remains visible inside the scarf opening. Hands have tapered palm/finger silhouette and thumb rather than cubes. Boots have a shaped instep/toe/sole. Slightly simplified surfaces and controlled matte shading should read like a three-dimensional illustration at both portrait and game distance.

## Concrete implementation

1. Add `actors/illustrated_commoner.gd`, a deterministic authored mesh builder. `HumanoidModel.build_human` routes modest player configuration to it. Return exactly the same root metadata and limb pivots expected by SkeletonFactory/CharacterLocomotion. Use ArrayMesh surfaces from shaped cross-section profiles, custom facial patches and ribbons; no primitive SphereMesh/CylinderMesh/BoxMesh visible on this player path. Ordinary NPC and zombie paths remain available unchanged.
2. Implement reusable mesh functions inside the new builder: joined oval sections with tapered radii and center offsets, smooth normals, rest UVs and restrained vertex shading; curved ribbons for seams/straps/brows; facial patch with shaped boundary and explicit nose/lip/eye placement. Author the scarf as a hood surrounding an open face, not a ball behind another ball. Build curved panel seams and small functional hardware into the silhouette. Keep individual meshes directly attached to pivots.
3. Extend SkirtCloth with optional oval proportions, gathered/pleated rest shapes, per-row profile, and vertex-color trim. Skirt pleats and hem facing must move with simulated vertices. Sleeve profile has shoulder fullness, narrower forearm and cuff. Veil has a shaped drape. Preserve four simulations and existing tests; measure updated vertex count/cost. Rest constraints derive from final shaped mesh.
4. Improve `debug/player_clothing_preview.gd` photography: close full-body front/three-quarter/back plus a face portrait. Derive camera axes from actual visual root transform, because old filenames reverse front/back under Survivor's yaw. Neutral lit background, sufficient detail resolution. Save to new `captures/player-commoner/` so previous evidence remains. Test the actual Survivor integration, not a disconnected art mockup. Add checks for authored ArrayMesh-only player geometry, facial/material metadata and garment topology as appropriate. Keep numerical cloth checks and run actual animation states.
5. Run `python tools/run_suite.py --import 120`; `python tools/run_suite.py --clothingtest 150 --rendered`; inspect full-body, portrait and motion PNGs, fix visual defects, rerun focused test for actual changes. Run `python tools/run_suite.py --animationtest 300 --rendered` after final model structure is stable. Existing animation watchdog is 180 seconds; do not reduce assertions or misreport timeouts. Existing suite can emit shutdown resource-leak warnings; distinguish these from assertion outcomes. Browser gallery displays actual Godot evidence, not an alternate simulated model.

## Acceptance and handoff record

Required: recognizable human facial structure and fingers/boots, tailored silhouette with articulated cloth folds and visible construction, restrained working-class Victorian-steampunk styling, covered hair/neck/legs, retained four physical garment simulations, existing motion/traversal tests passing. Compare against prior screenshot rather than claiming fidelity just from source changes. Inspect from front, side and back at full-body and portrait distances. No single rendering or synthetic test proves freedom from all intersections.

Pending: implementation, visual review, numerical gates, final scoped commit. Update this section with exact paths, commands, pass/fail, remaining risks and commit when done. If interrupted, next agent first reads current diff and logs before redoing work. Do not mark unrun gates passed.


## User steering — revised outfit and articulated rig (authoritative)

User continued before completion: realistic adult proportions; coat, gloves, backpack; replace gamis with syar'i top and oversized leggings (implemented as roomy trousers, not skin-tight leggings); French khimar; red neck veil/scarf with a hanging back tail; dynamic animation with bending elbows and knees. This replaces the earlier full-length skirt outfit specification. Preserve illustrated commoner styling, fair skin and modest coverage. Implement immediately after this plan update.

New bounded technical work: optional player-only articulated skeleton, preserving default NPC/zombie rig. Add `l_calf/r_calf` at anatomical knees 0.42m below hips, reparent existing `l_shin/r_shin` ankle markers under these at another 0.42m; append `l_forearm/r_forearm` at 0.27m below shoulder. Keep ankle names for existing foot metrics. Map new calf/forearm model pivots in SkeletonFactory. Generate optional elbow/knee tracks for all 19 clips; add matching procedural bends to live locomotion, reset ankle positions to rest so no bone separation. Hang/IK uses extended elbows to preserve the existing reach contract. Verify default rig unchanged and actual player pivots/segment lengths and bends, not just bone existence.

Art changes in IllustratedCommoner: reduce head group to approximately adult seven-head silhouette and shoulder width; split roomy trouser geometry at knees and gloves at forearms. Replace skirt with shorter loose coat/syar'i hem. Enlarge khimar shoulder/chest coverage; add red neck wrap and an independently simulated open rectangular scarf tail behind a practical leather/canvas backpack with actual straps. Extend cloth with optional open-panel topology and an animated cuff anchor bone. Sleeve cuffs follow the forearm endpoint; free fabric spans the articulated elbow, with upper/forearm collider segments. All defaults preserve prior NPC cloth behavior. Add backpack collision proxy for hanging fabric. No gameplay movement/capsule changes.

Prior intermediate run: authored geometry and cloth checks passed; full animation suite passed before this steering, with existing shutdown resource warnings. These are NOT final revised-rig validation. Update captures and run the focused and full relevant tests again after the changes.


## Implemented revised state (read this before earlier superseded design)

`actors/illustrated_commoner.gd` builds all player-visible body/clothing/details from authored ArrayMesh surfaces. The player is routed through `HumanoidModel.build_human` when `modest=true`. The earlier full gamis concept is superseded: current outfit is a long loose dark petrol coat/top over roomy muted trousers, French-style khimar with a fitted coverage layer and simulated outer drape, red neck scarf with a connected hanging back tail, gloves, shaped boots, practical backpack/straps/flap/buckle and belt pouch. Brass buttons/chain are restrained. Head group scaled to 0.82 around its anatomical center; shoulder pivots match the new rig. Painted vertex tones and a deterministic woven texture supply matte illustration shading. No external assets/downloaded models.

New `actors/garment_joint.gd` deforms a continuous knee fabric bridge by both thigh and calf transforms. This is skeletal garment deformation, not a separate cloth solver. It closes rigid segment gaps exposed by deep knee flexion. Upper/lower trousers, gloves and boots attach to the correct pivots; sleeve cuffs use the forearm's endpoint as an animated cloth pin.

Rig: `SkeletonFactory.build_survivor_skeleton(articulated := false)` preserves the default ten-bone NPC/zombie rig. Survivor passes `is_player()`. Player has 14 bones: existing ankle markers `l_shin/r_shin` reparented through new knee bones `l_calf/r_calf` with 0.42m upper/lower leg segments; forearms at 0.27m below shoulders. Shoulder x is +/-0.215m. BoneAttachment mapping includes the four new pivots. Crucial initialization repairs: hip clothing position is set before adding it to the live tree; Survivor sets initial visual yaw before building garments. Otherwise cloth interpreted spawn attachment changes as movement and flipped upward.

`LocomotionLibrary.build_library(articulated := false)` adds knee/elbow rotation tracks to all 19 clips only for the player. Crouch/slide override original rigid-leg thigh angles with forward thighs and backward knee flexion. Existing default clips unchanged. `CharacterLocomotion` supplies corresponding live procedural elbow/knee motion, resets ankle translations to anatomical rest, preserves extended-elbow hang reach, and adjusts visual hips in grounded states to keep the lower ankle at its 0.02m contact datum. Root and gameplay capsule do not move in this adjustment. The pose is more articulated, not a claim of full biomechanical foot IK or motion capture.

Cloth: five independent simulations (two sleeves, khimar drape, red tail, coat hem), 591 total particles. Optional shaped ring profiles, gathered rest lengths, bending constraints, stable UV/vertex trim, animated forearm cuff pins and open-panel topology extend the previous solver. New player body proxies include articulated upper/lower arms and legs, upper chest and backpack. Final known benchmark under concurrent regression load: 10.473 ms per five-garment physics step averaged over 30 calls; not a general city frame-time guarantee. Defaults retain original NPC structural passes and body proxy dimensions. Loose fabric is simulated; fitted layers/boots/gloves/backpack use bone deformation. No cloth self-collision, cloth-to-cloth contact, or exact triangle-based world collision is claimed.

## Evidence and exact resumption steps

Final focused command so far: `python tools/run_suite.py --clothingtest 150 --rendered` -> `ClothingTest finished with 0 failure(s)`, 55 PASS assertions, log `tools/out_clothingtest.txt`. Actual Forward+ renderer on RTX 5060 Laptop GPU. Tests check authored player meshes, 14 bones, knee/elbow segment lengths, seven pose phases, bend maxima over full locomotion cycles (a straight-leg instant must not falsely fail a motion test), live controller elbow drive/attached ankles, pinned boundaries, finite/bounded cloth, floor clearance, movement response, teleport recovery and freeze. Preview pauses animation briefly before pin/capture inspection so asynchronous animation frames do not invalidate the comparison. This does not weaken the physical pin invariant.

Fifteen latest images: `captures/player-commoner/Idle-front.png`, `Idle-back.png`, corresponding front/back Walk/Run/Sprint/CrouchIdle/Slide/ClimbUp, and `Portrait.png`. Front/back camera uses actual visual-root basis, unlike earlier reversed filenames. Renderer images were inspected for face visibility, proportion, cuffs, knee gaps, scarf coverage, cloth inversion and crouch grounding. Specific defects found and repaired: clockwise winding, overlarge legacy torso proxy, spawn transform impulse, rigid knee gaps, floating scarf connection, wrong crouch bend direction. Existing shutdown resource-leak warnings in the broad animation harness remain separate from gameplay assertions.

Full revised-rig animation run at 300 seconds timed out late in traversal chain with no assertion failure; it is NOT a pass. Rerun `python tools/run_suite.py --animationtest 480 --rendered` is pending. Before marking complete, inspect its terminal marker and final `--import 120` already passed. Preserve exact final logs under `captures/player-commoner/`. Any timeout stays unverified. Do not rerun unrelated city generation suites: no world-generation changes belong to this task.

Source changes belonging to this task: `actors/humanoid_model.gd`, `actors/illustrated_commoner.gd` and UID, `actors/garment_joint.gd`, `actors/skirt_cloth.gd`, `actors/survivor/survivor.gd`, `components/animation/skeleton_factory.gd`, `components/animation/locomotion_library.gd`, `components/animation/character_locomotion.gd`, `debug/player_clothing_preview.gd`, this plan. No additional model rig libraries or test assertions were removed. Unrelated world-generation files continue changing concurrently; do not stage them. Commit only these verified files after final gates. Current changes are not yet committed or pushed.
