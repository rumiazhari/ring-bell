docs/world/BUILDING-CONTRACT.md:130:`validate_build`'s registration gate + the `--buildingcontracttest` harness.
docs/world/BUILDING-CONTRACT.md:158:`--buildingcontracttest` (debug/building_contract_test.gd):
docs/world/BUILDING-CONTRACT.md:165:Regression gates: `--citytest`, `--cityruntime`, `--ruraltest`, `--smoke`.
docs/world/WORLD-CONTRACT.md:300:- `WorldPlan` remains pure (no scene mutation on query), `CityPlan` deterministic building IDs and `TerrainPlan` 17x17 manifests unchanged for same seed/coord, spawn anchor still found dry at `urban_basin`, multi-storey building samples unchanged — proved by `--citytest` plus `--terrainmaterialtest` plus `--hydrotest` plus `--biometest` plus `--roadtest` each finishing with `finished with 0 failure(s)` and retained overlap/door/stair/facade/material/geologic/road seams assertions (no duplicate city/rural buildings/wells/forage/hearth, no terrain seam regression, hydrology `CX`/`meander`/`width` still `530-710`/`72`/`38-50`, biome contiguity still `>=192 m` run-length, district_hint still present, road seam still `0.02` and `9` resident road chunks, `GENERATOR_VERSION` still `2`).
docs/world/WORLD-CONTRACT.md:400:- `WorldPlan` remains pure (no scene mutation on query), `CityPlan` deterministic building IDs and `TerrainPlan` 17x17 manifests unchanged for same seed/coord, spawn anchor still found dry at `urban_basin`, multi-storey building samples unchanged — proved by `--citytest` plus `--terrainmaterialtest` plus `--hydrotest` plus `--biometest` plus `--roadtest` each finishing with `finished with 0 failure(s)` and retained overlap/door/stair/facade/material/geologic/road seams assertions (no duplicate city/rural buildings/wells/forage/hearth/workbench/field/orchard parcels, no terrain seam regression, hydrology `CX`/`meander`/`width` still `530-710`/`72`/`38-50`, biome contiguity still `>=192 m` run-length, district_hint still present, road seam still `0.02` and `9` resident road chunks, `GENERATOR_VERSION` still `2`).
docs/world/WORLD-CONTRACT.md:430:- `WorldPlan` remains pure (no scene mutation on query), `CityPlan` deterministic building IDs and `TerrainPlan` 17x17 manifests unchanged for same seed/coord, spawn anchor still found dry at `urban_basin`, multi-storey building samples unchanged — proved by `--citytest` plus `--terrainmaterialtest` plus `--hydrotest` plus `--biometest` plus `--roadtest` each finishing with `finished with 0 failure(s)` and retained overlap/door/stair/facade/material/geologic/road seams assertions (no duplicate city/rural buildings/wells/forage/hearth/workbench/granary/field/orchard parcels, no terrain seam regression, hydrology `CX`/`meander`/`width` still `530-710`/`72`/`38-50`, biome contiguity still `>=192 m` run-length, district_hint still present, road seam still `0.02` and `9` resident road chunks, `GENERATOR_VERSION` still `2`).
docs/world/WORLD-CONTRACT.md:489:- Save state: `save_state()` excludes generated biome/industrial geometry (only deltas sibling pattern `deltas.crates|wells|forage|field_crops|fruit_patches|granaries|cave_discovered`), deterministic re-derive on load, `GENERATOR_VERSION` stays `2` additive, `WorldPlan` pure, `CityPlan` IDs / `Terrain 17x17` / hydrology `CX` etc unchanged proved by `--citytest + --terrainmaterialtest + --hydrotest + --biometest + --roadtest + --ruraltest + --cavetest` each `0` failures with retained seams.
docs/world/WORLD-CONTRACT.md:541:- Save state: save_state() excludes generated city interior geometry (only deltas.interior_looted via WorldState.has_flag(interior_looted) sibling to doors/damage, or stateless; generated BoxMesh/InteriorStation vertices/indices/Area3D never serialized). Deterministic re-derive on load with per-chunk deltas re-applied before materialize via InteriorStation.load_state + WorldState flag. GENERATOR_VERSION stays 2 additive inside URBAN_INNER 350 but city interior is within 350 — additive inside 350 that does not alter parcel topology stays 2 with audit note. CityPlan IDs / Terrain 17x17 / hydrology CX etc unchanged proved by --citytest + --terrainmaterialtest + --hydrotest + --biometest + --roadtest + --ruraltest + --cavetest + --verticaltest each 0 failures with retained seams.
docs/world/WORLD-CONTRACT.md:563:- ChunkManager reuses city interior counters; no new ACTIVE-only group needed for asset visuals (MeshInstance, not Area3D). Optionally asset_resolves_total counter, but debug_lines() at least shows city interior stats or asset resolves via print("[AssetPipeline] asset wall_2m resolve exists true fallback false") and t_city_mat within FRAME_BUDGET_MS 12. Pacing MAX_MATERIALIZATIONS_PER_FRAME 1 + freed-Zombie guard extended to asset MeshInstance (is_instance_valid + is_queued_for_deletion + is_inside_tree before global_transform). Save state: save_state() excludes generated asset geometry (only deltas sibling pattern interior_looted etc., no asset deltas), deterministic re-derive on load with AssetCatalog resolve re-probed before materialize (file probe not persisted), GENERATOR_VERSION stays 2 additive, WorldPlan pure, CityPlan IDs / Terrain 17x17 / hydrology CX 530-710 / biometest etc. unchanged proved by --citytest + --terrainmaterialtest + --hydrotest + --biometest + --roadtest + --ruraltest + --cavetest + --verticaltest each 0 failures with retained seams.
docs/world/WORLD-CONTRACT.md:610:- `ChunkManager` mirrors cave/vertical pipeline: counters `_cave_vertices_total/_triangles_total/_entrances_total/_chambers_total`, `_cave_active_colliders` stays 0, `_cave_colliders_total` 0, `debug_lines()` as `cave verts|tris|colliders|entrances|chambers t_cave_gen|t_cave_mat active cave (warm)` (new `chambers` column), per-chunk record fields `cave_vertices/cave_triangles/cave_colliders/cave_entrances/cave_chambers/cave_manifest/cave_gen_ms/cave_mat_ms`, stats keys and timings `cave_gen_ms/cave_mat_ms` already exist and now include chamber derivation within same `cave_gen_ms` (document that `cave_gen_ms` now measures entrance+chamber). Active-ring rule: cave portal and chamber portal counted toward `active cave` only when chunk `ACTIVE`; at most `3` active caves (sparse) in `3x3` (entrance+chamber co-located, so active cave counts chambers same as entrances). Warm retains `CaveMesh` visual (entrance+chamber) but disables `CavePortal`/`CaveChamberPortal` `monitorable` via `_set_caves_enabled`. `save_state()` excludes generated cave/chamber geometry, only `deltas.cave_discovered` (and optionally empty `deltas.cave_chambers_discovered` persists, but chamber stateless), deterministic re-derive on load with per-chunk deltas re-applied before materialize keeps discovered flag. `GENERATOR_VERSION` stays `2` additive (chamber overlay, no parcel topology change, no new voxels outside quarry upland). Compatibility: `CityPlan` IDs / Terrain 17x17 / hydrology CX etc unchanged proved by `--citytest + --terrainmaterialtest + --hydrotest + --biometest + --roadtest + --ruraltest + --cavetest + --verticaltest` each `0 failures` with retained seams. No new autoload, project setting, or large asset import is authorized beyond chamber constants and cave streaming stats already described.
docs/world/WORLD-CONTRACT.md:636:**Harness:** `--buildingcontracttest` — malformed-spec rejection matrix (missing entrance, off-edge doors, unreachable floors, stairs that don't fit, fake classifications, tiny footprints, grounding violations, disconnected/overlapping rooms, tampered evidence, bypass registration) + real-world conformance: CityPlan chunks (5×5) and migrated rural contract houses all validate with 0 violations, budgets hold.
docs/world/WORLD-CONTRACT.md:638:**Status (this slice):** city pipeline contract-stamped + validated with no geometry regression (`--citytest`); ALL rural house-family buildings (cottage/village_house/farmhouse, 1- and 2-storey with ladder circulation) assemble via the universal grammar with real door/window apertures, slabs, gabled roofs, interiors; barn/stable/shed stay on the legacy rural art path (tracked pending migration in BUILDING-CONTRACT.md).
