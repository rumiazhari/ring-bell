# Review C003 R01 — Czech Rural Mosaic Closeout (P3.1-RURAL-MOSAIC) — Builder t_00b4c949 run 29

Reviewed: builder task `t_00b4c949` run `29` (review-requested at 2026-08-29 15:47), commit `dbbe18ffcc8a07beb50f2f539c7878043ab094fe` (`fix(P3.1): closeout R01 thresholds and door settle + docs`) on top of `023070d` (P3.1 biome optimize) and `e32f0aa` (P2.2 hydrology). Kanban review task `t_e5e76183` (run 30, `review_of_task t_00b4c949 run 29`). This report satisfies `t_e5e76183`.
Spec: `.hermes/autopilot/specs/SPEC-C003-R01.md` SHA-256 `a5ed9d6cdc901fb88e937741707a8faac356a175a020199f218198329ae91641` (verified `sha256sum` matches `AUTOPILOT_STATE.json` current.spec_sha256).
State: `AUTOPILOT_STATE.json` phase `reviewing` cycle `3` revision_round `1` milestone `P3.1-RURAL-MOSAIC`, enabled `true`, controller `ring-bell-autopilot-v2`, board `ring-bell-v2`, reviewer sole architect `lunaringbell` (`muse-spark-1.2-contributor` via `opencode-go` reasoning `max`, no production code/tests/scenes/assets/project settings edited).
Diff base: `023070d..dbbe18f` — 5 production files, 84 insertions 20 deletions (`debug/terrain_material_test.gd` thresholds, `debug/city_runtime_test.gd` door settle, `ARCHITECTURE.md`, `DEVELOPMENT.md`, `docs/world/WORLD-CONTRACT.md` §12). Plus ~35 modified + ~31 untracked dirty pre-existing (addons/godot_ai 26 files, AGENTS.md, AUTOPILOT_POLICY.md, tests/test_ring_bell_autopilot_v2.py, etc) preserved per policy, no reset/clean, no history rewrite, no file deletions (junk/ kept).
Workspace: `C:/Vibe Code project/Godot Project/ring-bell`, branch `master` ahead 27 behind origin, Godot `C:/Vibe Code project/Godot Project/Godot_v4.7.2-stable_win64.exe` (4.7.2 stable), Windows 11, `tools/run_suite.py` headless, marker `finished with N failure(s)` is pass gate (exit 3221225477 with marker is pass per spec).

## Verdict: REVISE (principal, bounded, round 02/02)

The R01 closeout is half-correct. Save thresholds and docs are now green and the underlying rural mosaic is still principally correct (deterministic 9x9 biome/geology, macro 256 m contiguity, 81/128 and <=48/12/6 ACTIVE-only 9 colliders, `t_biome_gen/t_biome_mat` telemetry, GEN2 2 preserved, hydro CX/width untouched, walkthrough honest 5-storey). One required gate remains red: `--cityruntime` `door closes via API` still flakes 1/2 despite the 3.0s + 3 physics frames patch, violating `SPEC-C003-R01` AC2. A second bounded harness-only revision (SPEC-C003-R02) mirroring walkthrough's proven step-back retry is warranted rather than `accept_with_deferred`. Max 2 revisions; this was round 01, next will be final round 02.

## What was inspected (direct reads, not handoff prose alone)

- `debug/terrain_material_test.gd` diff +11: thresholds `save delta reasonable` `<50000` -> `<250000`, `save size not inflated` `< save_before+30000` -> `< save_before+150000`, comment citing door-state snapshot cost and `MAX_INFLIGHT 6`/`0.1` streaming tuning. Strict checks retained: `save has no terrain field` (no `terrain_vertices` key in save_state), `save records have no terrain payload` (no `terrain`/`terrain_vertices`/`terrain_gen_ms` in record), `save existing records unchanged`. No other assertion weakened, no FAIL downgraded.
- `debug/city_runtime_test.gd` diff +40: door selection now closest exterior to player (deterministic, fallback to any door), open wait `1.2` -> `2.0` s, close wait `1.2` -> `3.0` s + `await physics_frame`*3 + conditional retry `0.5` + 2 frames if `is_open` still true. Strict checks retained: `closed leaf blocks doorway` (collider==leaf), `open leaf reaches swing angle +/-6deg`, `open doorway clear without RID exclusion` (exclude only player RID), `open leaf still collidable at swung position` (collider==leaf). No RID exclusion added, no leaf rotation faked.
- `ARCHITECTURE.md` diff 4 lines: `ACTIVE-only collision` now lists `BiomeBody` and `36 peak (9 city+9 terrain+9 water+9 biome)`, budgets now `Biome: 9x9 81/128 + MultiMesh <=48/12/6`, `t_biome_gen/t_biome_mat` in telemetry, `district_hint` + bank ribbon vertex-color choice. Already in working tree at R00, now committed.
- `DEVELOPMENT.md` diff 28 lines: `CITY` world-mode row now `+ biome: 9x9 81/128, at most 1 biome collider... 9 active biome max plus MultiMesh <=48/12/6`, streaming telemetry paragraph now `and biome verts|tris|colliders|instances t_biome_gen/t_biome_mat`, collision budget `9 city+9 terrain+<=9 water+<=9 biome =36 peak`, headless matrix now 10 gates with `#5 --biometest 300` + alias `--biomematerialtest` and renumbered `#6 cityruntime .. #10 soak`, contract section `What --biometest verifies` added, manual walkthrough §2 F3 now `active biome + t_biome_gen/t_biome_mat`, §3 east walk now `biome tint field->wet meadow->floodplain->teal 4a7a94 bank 9m floodplain 26m plus hedgerow/tree proxies`.
- `docs/world/WORLD-CONTRACT.md` diff 21 lines: §12 Biome & Geology (12.1 vocab constants, 12.2 generation micro/macro contiguity, 12.3 manifest 9x9 vs 17x17 justification lift 0.03 caps 48/12/6 MAX 48, 12.4 streaming telemetry save exclusion GEN2 2). Bank ribbon choice documented as vertex-color 1.5 m transition (81/128) deferred until trench carve. Already in working tree at R00, now committed.
- `world/streaming/chunk_manager.gd` unchanged at R01 (MAX_INFLIGHT 6, STREAM_UPDATE_INTERVAL 0.1, shared WorldPlan per worker, holder `biome`+`biome_gen_ms`, `debug_lines` `t_biome_gen/t_biome_mat`, save_state `records only`). Verified HEAD still 6/0.1.
- `world/buildings/door.gd` unchanged — leaf `RigidBody3D` + `HingeJoint3D`, `DRIVE_TICKS_LIMIT 90` (~1.5s), `STALL_TICKS 18` (~0.3s), `_bounce_open()` on blocked close, `is_open()` == `state==OPEN`. Walkthrough close uses step-back retry to avoid bounce.

Git truth: `git diff 023070d..dbbe18f --stat` 5 files; `git show dbbe18f` message includes SPEC-C003-R01 SHA; `git status` still 35 modified +31 untracked dirty pre-existing (addons/godot_ai + character + worktrees) — preserved, no absorption, no deletions, junk/ kept.

## Test evidence (reviewer independent re-runs from canonical root via tools/run_suite.py, Windows 11, Godot 4.7.2)

Marker `finished with 0 failure(s)` is pass gate (3221225477 with marker accepted per spec). Logs `tools/out_*.txt`. Elapsed on reviewer host (builder reported similar).

- `python tools/run_suite.py --terrainmaterialtest 300` → `[TerrainMaterialTest] finished with 0 failure(s)` exit 0, reviewer 188s (builder 219s). Tail shows PASS resolution 17, heights 289, verts 289, tris 512, colliders 1, shared edge +/-, 256m seam +/-, cliff rock, manifest equality reversed, alt seed variation, CityPlan unchanged, urban inner flat 0, outer varies, materialize verts/colliders, idempotent, seam heights/normals match, active terrain <=9 measured verts/tris/coll matches, negative <=9, async terrain_gen_ms >0, outer streamed collider, stale rejected, save existing unchanged, save has no terrain field PASS, save records have no terrain payload PASS, save delta reasonable PASS (now <250000, length ~201760 vs before 99019, delta +102k <150k), save size not inflated PASS, mesh extent, collision extent, etc. — PASS (covers R01 AC1). Previously at R00 2 failures <50000/+30000; now 0.
- `python tools/run_suite.py --cityruntime 360` first run → `[CityRuntime] finished with 1 failure(s)` exit 1, reviewer 177s. PASS chunk manager exists, 3x3 ACTIVE, player spawned, wall blocks ray, zombies spawned, doors exist, closed leaf blocks, door opens via API, open leaf angle, open doorway clear without RID, swung collidable, but FAIL `door closes via API   ()`. far ring, unload/return, deterministic ids, stair probe, camera lens/sector, interior <=9m, destruction survival — all other PASS. Log shows leaf debug after open but not after close (door stayed OPEN).
- `python tools/run_suite.py --cityruntime 360` second run (same code, no change) → `[CityRuntime] PASS door closes via API` then other PASS, `finished with 0 failure(s)` — i.e. 1/2 deterministic. Builder handoff reported identical pattern: 1 pass (2.6+0.6) then 1 flake (2.5+0.5), final 3.0s not re-proven. Reviewer confirms the 3.0s+3 frames patch does not make the gate deterministic (flake rate ~50% on reviewer host, 6 inflight load).
- `python tools/run_suite.py --biometest 300` → `[BiomeTest] finished with 0 failure(s)` exit 322... reviewer ~70s (not timed precisely, tail captured). PASS same-seed shuffled/negative, different seed >=3/9, contiguous >=192m no speckling, vocab subset, 5-seed geographic validity, manifest equality shuffled, shared-edge +X/-X/+Z >=7/9, collider <=1 verts 81 tris <=128, instances <=48/12/6 3x3 ACTIVE <=9, >=9 resident, debug t_biome_gen/mat, unload 480m and regenerate identical, save excludes biome, GEN2 2, CityPlan IDs stable, Terrain 17x17, hydro CX 530-710 — PASS (covers AC of P3.1, unchanged from R00 reviewer 85s).
- `python tools/run_suite.py --hydrotest 300` → `[HydroTest] finished with 0 failure(s)` reviewer ~60s PASS (hydro determinism, CX 530-710 meander 72+18 width 38-50 bank 9 floodplain 26, seam 0.02, 81/128 active <=9, t_water_*). — PASS.
- `python tools/run_suite.py --citytest 300` → `[CityTest] finished with 0 failure(s)` reviewer ~150s PASS (plan determinism, overlap, stair opposite-edge, facade, no duplicate). — PASS.
- `python tools/run_suite.py --walkthrough 360` → `[Walkthrough] finished with 0 failure(s)` reviewer 50s PASS (closed blocks, E open, door->stairwell without teleport, climb all 5 storeys to deck y 16.25 camera tracks, descend, walk out, door reports CLOSED then blocks again). Walkthrough's close logic (4x0.9s step-back retry) is the proof that the door CAN close deterministically when the player steps back — cityruntime's stub does not.
- `python tools/run_suite.py --havoctest 240` → `[Havoc] finished with 0 failure(s)` reviewer 20s PASS — PASS.
- `python tools/run_suite.py --smoke 180` → `[SmokeTest] finished with 0 failure(s)` reviewer 25s PASS (population/quest/combat/save). — PASS.
- `python tools/run_suite.py --import 120` previously PASS (R00 reviewer 2s), not re-run but parsing is stable.

Overall matrix at `dbbe18f`: 7/8 gameplay gates green (biometest, hydro, city, terrainmaterial, walkthrough, havoc, smoke) + 1/8 intermittent red (cityruntime). R01 required 8/8 green per `finished with 0 failure(s)`.

Builder handoff run 29 reported terrainmaterialtest 0 failures proven 219s (matches reviewer 188s), cityruntime hardened but two-consecutive not re-proven due to budget (matches reviewer 1/2), full 8-gate matrix plus import not re-run due to budget assumed green from `023070d` baseline (reviewer now fills gap: assumptions hold except cityruntime).

## Acceptance criteria vs spec (4, SPEC-C003-R01)

1. `--terrainmaterialtest` 0 failures (two save-delta checks now <250k/+150k; `save has no terrain field`, `save records have no terrain payload`, `save existing records unchanged` still pass; 289/512 seam and ACTIVE budgets unchanged) — PASS (reviewer 188s 0 failures, 2601 verts 4608 tris active, save delta PASS at 201760 vs 99019).
2. `--cityruntime` 0 failures (closed leaf blocks, open clears without RID, swung collidable, door closes via API deterministically after physics drain, far ring and destruction survival intact; 360 allowed if 300 exceeds) — FAIL as measured: reviewer 177s FAIL `door closes via API`, second run PASS, i.e. not deterministic. Walkthrough same door E close passes with step-back, so design is honest but harness immediate check without player clearance flakes under 6 inflight load even with 3.0s+3 frames. The 3.0s patch moves the marker from always-FAIL to 1/2 but does not close the gate.
3. Full 8-gate matrix 0 failures each: `--import` plus `--biometest`, `--hydrotest`, `--citytest`, `--terrainmaterialtest`, `--cityruntime` (360 allowed), `--walkthrough`, `--havoctest`, `--smoke` — FAIL as measured: 7/8 green, cityruntime intermittent. If host still needs 360, a single 360 pass is accepted but this revision still fails at least 1 of 2 360 runs, so the matrix criterion is not met.
4. `DEVELOPMENT.md` (and `ARCHITECTURE.md`/`docs/world/WORLD-CONTRACT.md` §12) are updated for ACTIVE-only biome and `t_biome_gen/t_biome_mat` alongside water/terrain, with `+ biome` streaming paragraph and `--biometest` contract, folding C001/C002 deferred docs. Windowed 600-900 m east walk with biome tint + hedgerow/tree proxies remains deferred — PASS: all three docs committed at `dbbe18f` with required paragraphs (modes, telemetry, budgets 81/128 48/12/6, 36 peak, district_hint, bank ribbon, biometest contract, walkthrough steps). Windowed PNG/log for C003 still synthetic `SPEC-C002-windowed.png/log` (29 KB PIL) — correctly deferred per R01 spec (not a blocker for this revision).

Overall: R01's save-threshold and docs closeout are correct and green, but the door-close harness still violates the explicit 0-failure gate. Per review discipline, this is principal (explicit criterion), not deferrable.

## Performance / persistence / compatibility

- Preserve 64 m, ACTIVE 1 (3x3=9) WARM 2 UNLOAD 3, MAX_INFLIGHT 6 (raised from 2) STREAM_UPDATE_INTERVAL 0.1 (was 0.2) — intentional to keep 300s budget with biome+water+terrain, private WorldPlan per worker holder `biome_gen_ms`, BoxShape single collider per biome chunk, 81/128 overlay lift 0.03, 36 peak active colliders (9 city+9 terrain+9 water+9 biome) within FRAME_BUDGET_MS 12; `t_biome_gen` worker + `t_biome_mat` main telemetered via debug_lines alongside `t_water_*`. Reviewer tail confirms `t_biome_gen` present.
- Thresholds raised narrowly 50k->250k absolute and +30k->+150k without masking leak: `has_terrain false` checks retained; injecting fake `terrain_vertices` still fails payload check; current save JSON ~100k before far move / ~201k after ~75 records is legitimate discovery growth (door states per resident chunk) not a leak — leak would be >500k. GEN2 stays 2 additive outside 350 core, no trench, no new autoload/asset (vertex-colored overlay + primitive Box proxies only).
- Saves `ChunkManager.save_state()` → `{records: {"x,y": {discovered,deltas}}}` only — verified `save has no terrain field` PASS; biome excluded from save_state PASS; `CityPlan`/`WorldPlan` deterministic IDs before/after hydrology identical, hydrology CX/width unchanged. Old saves reinterpret path unchanged.
- Compatibility: hydrology and biome additive outside URBAN_INNER 350 (CX 530-710 >600, biome urban_basin 0 instances inside 350, forest outside water/floodplain), no terrain trench carve through historic core, no building footprint overlap. Water manifest `district_hint` `urban_basin|rural_plateau|river_valley` via radial+distance_to_water, bank ribbon vertex-color 1.5 m transition without extra geometry as budgeted choice keeping 81/128/1 — deferred truthfully.
- City runtime budget: reviewer 177-188s cityruntime at 360 (spec allows 360 if 300 exceeds), still within revised budget but the 1 failure is principal per AC2. Walkthrough 50s honest traversal.

## Out of scope — correctly not done (and correctly not broadened in R01)

No hierarchical road/rail graph, settlement/village placement, trench/dike carve through 350, city block water avoidance, street bridge mesh, lake/reservoir beyond hydrology placeholder, swimming/buoyancy/boat, vertical survivor network/caves, full shader water/biome textures, art/toon shader rework, broad building refactor, collision disabling/teleport/skips, per-chunk hard-coded biome hacks, waypoint skipping, or test relaxation beyond the two save caps (which were explicitly authorized) — all respected. R01 did not absorb unrelated dirty files or rewrite history.

## Conflicts / hotspots / risks

- No spec vs repository principal conflict. Biome lift 0.03, BoxShape single collider, ACTIVE-only physics are intentional budgeted optimizations documented as such; warm visuals without BiomeBody is same pattern as city/terrain/water — not a violation.
- Hotspot flagged for orchestrator: `world/streaming/chunk_manager.gd` collision hotspot (city+terrain+water+biome layers, touched 3 cycles) plus 26 dirty `addons/godot_ai` files ahead of origin — advise decomposition before further streaming work lands there. Not blocking.
- Principal risk driving this revision: cityruntime door-close immediate check without player clearance races the leaf's HingeJoint solver under 6-inflght load. Door.gd's `_bounce_open()` on blocked close (DRIVE_TICKS_LIMIT 90) explains the flip to OPEN when the capsule pins the leaf. Walkthrough's step-back retry is the proven fix that R01 missed by only waiting.
- Minor residual to defer (not revision blockers): authentic windowed CITY run to river valley with biome tint+proxies still synthetic PIL placeholder from C002; ObjectDB `!is_inside_tree()` still emitted 1× per harness at shutdown (guarded in main.gd where feasible but probes still emit) — accepted per spec; save threshold now 250k/+150k is narrowly chosen to pass canonical seed (~100/201k) while still catching >500k leak — no further widening needed.

## Why revise not accept_with_deferred

All core gameplay pillars are closed (exploration via contiguous forest/field belts, agency via field vs forest vs floodplain cover/resource, Czech identity via mosaic, performance via 81/128 and 36 peak) and R01's save/docs pillars are now closed. But acceptance is gated by the 3-7 independent criteria and 1-8 required tests; AC2 explicitly requires 0 failures on `cityruntime` judged by marker, and AC3 requires the full matrix green. The door-close failure is not cosmetic: it blocks the honest-physics gate (door close is part of traversal integrity, same leaf that walkthrough proves). Per `AUTOPILOT_POLICY.md` minor findings are deferred, principal blockers get bounded revision (max 2). A third direct revision would be recovery cycle; this is R01→R02, the final allowed. Opening a second revision for step-back retry keeps the quality bar intact and gives the builder a deterministic, bounded fix without broadening the slice. Windowed proof remains deferred — headless builder cannot honestly produce it, and headless walkthrough+seam tests already prove continuity.

## Deferred findings carried forward (for next related design, not this revision)

Carry the 6 prior deferred plus this cycle's windowed polish (already in AUTOPILOT_STATE.json deferred_findings):

- Archive one authentic normal windowed CITY run (1200x720, not --shot) showing WASD/E door swing (closed blocks/open clears without RID exclusion/swung collidable), stair climb to roof with camera following, F3 overlay with active biome + `t_biome_gen`/`t_biome_mat` alongside active water/terrain, then 600-900 m east walk to river valley showing continuous biome tint field->wet meadow->floodplain->teal water 4a7a94 bank 9 m floodplain 26 m plus hedgerow/tree proxies without seam cracks/flicker. PNG+log under `.hermes/autopilot/reports/SPEC-C003-windowed.*` (or `junk/` if large) referenced in handoff; `ARCHITECTURE.md`/`DEVELOPMENT.md` updated for ACTIVE-only biome (36 peak) and `t_biome_*` plus water `district_hint` + bank ribbon vertex-color decision, folding C001/C002. Current `SPEC-C002-windowed.png/log` remain synthetic PIL placeholders and do not satisfy.
- Reduce headless shutdown noise where feasible without masking failures (smoke/walkthrough/havoc still emit ObjectDB !is_inside_tree and Windows 3221225477 with marker — guard remaining global_transform reads after queue_free and quiet marker-present exit reporting).
- Water manifest contract polish (district hint present, bank ribbon vertex-color choice documented — truthfully deferred until trench carve).

## Rollback

Keep `e32f0aa` checkpoint (P2.2 hydrology, 8 gates green at 50k cap no biome), `023070d` P3.1 candidate (biome correct but terrainmaterial 2 fails + cityruntime 1 fail), and `dbbe18f` R01 (terrainmaterial 0 but cityruntime 1/2). If R02 fails, revert or quarantine only its harness commit, leave artifacts under `junk/` not deleted, restore controller via Kanban outcome with exact failure logs. Rollback must reproduce `dbbe18f` baseline: `--hydrotest`, `--citytest`, `--terrainmaterialtest` (with new 250k cap), `--biometest`, `--walkthrough`, `--havoctest`, `--smoke` each green; `cityruntime` still flakes 1/2; no windowed PNG new.

Reviewed by: Luna (architect-autopilot, sole architect/reviewer, independent — no production code/tests/scenes/assets/project settings edited). Model `muse-spark-1.2-contributor` via `opencode-go` reasoning `max`, `gpt-5.6-luna`. Nonblocking review link `review_of_task t_00b4c949 run 29` (Kanban review `t_e5e76183` run 30). Bounded revision spec `SPEC-C003-R02.md` SHA-256 `0691a749cedb87b2af1f277425e7716bcc18e4828131dbd5ed6cc8f94b9dab34` written for controller routing.
