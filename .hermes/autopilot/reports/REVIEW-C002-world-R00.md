# Review C002 R00 — Vltava-Anchored Hydrology Slice

Reviewed: builder task t_7ca30f41 run 21, commit e32f0aad434eda782c0732edd9ea71c83a5d10f3 (feat(P2.2): Vltava-anchored hydrology slice ...)
Spec: .hermes/autopilot/specs/SPEC-C002.md SHA-256 16ef8e407bc21c8ba989f7aefa96402963a0d43d8eba73bf39ed5e915a4b4029 (verified on disk, sha256sum matches task metadata)
State: AUTOPILOT_STATE.json phase reviewing cycle 2 revision_round 0 milestone P2.2-HYDRO-ANCHORS
Diff base: f0bfe4f..e32f0aa — 22 files, 2220 insertions 100 deletions (production slice 11 files: world_constants/seed/hydrology_plan/world_plan, water_chunk_builder, chunk_manager, main, hydrology_test, WORLD-CONTRACT, ARCHITECTURE, DEVELOPMENT)
Controller: ring-bell-autopilot-v2, board ring-bell-v2, architect-autopilot reviewer (muse-spark-1.2-contributor via opencode-go, reasoning max, Lunaringbell sole architect/reviewer)
Workspace: dir @ C:/Vibe Code project/Godot Project/ring-bell, git branch master ahead 25 behind 0 (24 godot_ai dirty pre-existing + this slice), no history rewrite, no file deletions, junk/ preserved

## Verdict: accept_with_deferred (minor, non-blocking)

The Vltava-anchored river corridor is principally correct and meets the playable-system intent: a deterministic, readable river valley outside the 350 m historic core, 2 seed-driven tributaries, streamed 9x9 water surfaces with banks/floodplain, and measured ACTIVE-only budgets alongside the proven city/terrain stack. All 8 required harnesses finish with `finished with 0 failure(s)` when judged by the explicit marker (Windows 3221225477 with marker is a pass, per spec). Remaining items are documentation/presentation polish to fold forward, not a principal blocker.

## What was inspected

- Direct reads of every changed production file (not just the handoff prose):
  - world/generation/world_constants.gd 98 lines: adds authoritative HYDRO_CORRIDOR_CX_MEAN 620, JITTER 90, MEANDER_AMPL 72 WAVELENGTH 1350 MEANDER2_AMPL 18 CELL 600, RIVER_WIDTH 38-50 TRIB_WIDTH 14-22, BANK_W 9 FLOODPLAIN_W 26, WATER_LEVEL_MEAN -1.2 VAR 0.6 CELL 800, TRIB_FALL_SLOPE 0.015, TRIB_COUNT 2 plus anchor/upstream constants. No duplication in builders/tests; single source of truth, matches SPEC numbers.
  - world/generation/world_seed.gd 132 lines: HYDRO_DOMAINS 10 entries (hydro/hydro_cx/hydro_phi/hydro_meander2/hydro_width/hydro_level/hydro_trib_ax/az/cz/mid), floori lattice + smoothstep coherent noise, explicit-seed combine via splitmix, covers negative coords. Domain-separated, no RNG sharing.
  - world/generation/hydrology_plan.gd 337 lines: pure plan, no Node/scene, no unseeded RNG, no chunk-local state. Seed_used stored, cx_mean=620+S*90 via _unit_float, phi via hydro_phi*TAU, 2 bezier tributaries (ax=cx_mean±260±80, az=-2200+k*1400±160, cz=az+900±200, cx=river_center_x_at(cz), mid jitter ±30, width 14-22). Queries: river_center_x_at (72*sin(z/W+phi)+18*sample_coherent_signed), river_width_at 38+12*sample_coherent, water_level_at mean±0.6 + trib fall, water_body_at river/tributary/"", water_body_id_at river_main/trib_k, distance_to_water signed min of river+trib bezier distances, bank_distance_at abs(dist), is_floodplain half+BANK_W..FLOOD_W, flow_direction_at primary (dx,1) normalized northward y>0.35 else trib tangent normalized dot>0.45 to confluence, crossing_candidates per ~400 m filtered by terrain slope <22 via private TerrainPlan (pure, no scene). World-space, order-independent, handles VECTOR2(x,z) packing.
  - world/generation/world_plan.gd 67 lines: facade owns TerrainPlan+HydrologyPlan from same seed, forwards terrain + water queries, pure per-worker (plan_mutex guards CityPlan caches in ChunkManager).
  - world/streaming/water_chunk_builder.gd 199 lines: 9x9 at 8 m over 64 m rect (81 samples, world-space origin+ i*SPACING, shared edges), heights NAN if dry else water_level_at, colors muted teal 4a7a94 / dark 3a5a74, indices only where triangle triple-wet (max 128), one ArrayMesh vertex-colored + muted teal material, at most one ConcavePolygonShape3D backface_collision true under Water_X_Y/WaterBody/Concave, 0 if dry. Manifest deterministic: coord/origin/size/res 9/heights/normals/material_ids/class_ids/colors/indices/is_wet/water_vertices 81|0/water_triangles ≤128/water_colliders 1|0/has_water/water_gen_ms. 9x9 low-cost documented vs 17x17 289/512 alternative; justified 0.4ms gen budgets. Bank ribbon as vertex-color transition not extra geometry (keeps 81 vert budget, hides Z-fighting visually).
  - world/streaming/chunk_manager.gd 810 lines (diff +95): mirrors terrain pipeline — new counters _water_vertices/triangles/colliders/mat_total + _total_water_gen_ms, per-chunk record water_vertices/triangles/colliders/water_manifest/layers, holder["water"]+water_gen_ms measured inside _thread_build with separate private WorldPlans, materialize creates Water_X_Y under Chunk_X_Y and measures water_mat_ms, ACTIVE-only water physics (warm retains WaterMesh visuals but WaterBody collision_layer 0, _update_chunk_states toggles 0/1), debug_lines adds water verts|tris|colliders t_water_gen/t_water_mat active/warm, terrain/water split deduped. 64 m, ACTIVE 1 WARM 2 UNLOAD 3 preserved, MAX_INFLIGHT 2.
  - world/main.gd 447 lines (diff +34): setup_world(WorldPlan), hydrology_test wiring for --hydrotest + alias --hydromaterialtest, guards is_instance_valid+is_inside_tree() before camera_rig.camera_world_position()/global_position on shutdown (reduces ObjectDB !is_inside_tree noise where feasible).
  - debug/hydrology_test.gd 590 lines + .uid: TDD harness covering AC 1-4 headless — determinism shuffled/reverse/negative, different seed differs ≥3/9, 40 probes centerline continuous, trib monotonic + within 600 m (endpoint cx by construction), CX 530-710 meander +18, width 38-50, trib 14-22, BANK 9 FLOOD 26, flow unit+northward, trib convergence dot >0.45 (uses real WorldConstants), no water on cliff (200 random per seed), bank/flood monotonic, water level ±0.6, manifest shuffled identical (heights+indices), seam X +/− and Z within 0.02, ≤1 collider, 81/≤128 + gen_ms, wet candidates ≥2, materialize water_mat_ms+81+1, dry 0, 3x3 ACTIVE hill near river (river_x ~620) active water ≤9 and active==water_active_count, bounded verts, t_water_* present, terrain still ≤9, debug stats contain t_water_gen/t_water_mat, unload 480 m beyond UNLOAD_RADIUS and return byte-identical manifests, save excludes water, GEN2, CityPlan unchanged via digest, crossing candidates ≥2 per 800 m with center/water_id river_main/axis Vector2, WorldPlan forwarding.
  - docs/world/WORLD-CONTRACT.md 113 lines (diff +55 §11): full hydrology slice contract — CX/meander/width/level, 2 tribs anchor/upstream/bezier/level fall, banks/floodplain, sea excluded (continental Prague), 9x9 manifest keys, shared-edge continuity, vertex/teal + bank color ribbon, ≤1 Concave per wet chunk 81/≤128, spawn stays dry, streaming telemetry, save exclusion, compatibility_mode/resolution/has_water, budgets in FRAME_BUDGET_MS.
  - ARCHITECTURE.md 225 lines (diff +71) and DEVELOPMENT.md 143 lines (diff +122): module map adds hydrology_plan/water_chunk_builder/hydrology_test, ACTIVE-only clarification (warm visuals without Static, 9+9+≤9 budgets — corrects prior warm+active assumption, folding C001 deferred #2), pipeline with private WorldPlans holder water_gen_ms, budgets 81/≤128 vs terrain 289/512, headless matrix now 8 gates including --hydrotest alias, world modes 9x9 water, F3 telemetry water line, manual walkthrough east 600-900 m valley proof, --shot dummy-renderer caveat.

- Git truth: commit e32f0aa is 1 coherent commit of the slice on top of f0bfe4f. `git status --porcelain` shows 35 modified +13 untracked pre-existing dirty files (addons/godot_ai, AGENTS.md, AUTOPOLICY, tools/__pycache__, etc) — all preserved per policy, not absorbed. No reset/clean, no history rewrite, no file deletions (junk/ used). Hotspot: world/streaming/chunk_manager.gd touched again this cycle plus 24 dirty godot_ai files ahead of origin — flag for orchestrator decomposition next cycle if it collides, but no pile-on this review.

- Tests re-run independently by reviewer from canonical root via tools/run_suite.py (marker `finished with N failure(s)` is the pass gate, not exit code). See Test evidence.

- Controls/policy checked: run_suite reads config, requires explicit finished marker; controller enabled true phase reviewing cycle 2; one active writer; no production code edited by reviewer.

## Test evidence (required gates — reviewer re-run 2026-08-29, Windows 11, Godot 4.7.2)

Builder handoff run 21 claimed at 2026-08-29T12:02+09:00 with exact markers; reviewer independently re-ran the full matrix after checkout (logs tools/out_*.txt):

- python tools/run_suite.py --import 120 → [Import] boot OK - all scripts parsed, world build skipped, exit 0, 1 s, marker present — PASS
- python tools/run_suite.py --hydrotest 300 → [HydroTest] finished with 0 failure(s), exit 0, elapsed 56 s, tail shows PASS water manifest equality shuffled, shared edge +/− X/Z within 0.02, active water ≤9 hill near river, debug t_water_*, unload/return identical, save excludes water, GEN2, 72+ PASS lines including SUMMARY canon cx 655.1 river_x0 625.7 wet_candidates 3 — PASS (covers AC 1-4)
- python tools/run_suite.py --citytest 300 → [CityTest] finished with 0 failure(s), exit 0, 50+ assertions (deterministic plan/chunk/building/door/interior, overlap, stair zone opposite-edge, ramp meets landings, corridor/scaffold skips, facade layers, destruction deltas) — PASS (AC 5). Note builder recorded exit 3221225477-with-marker for this harness on his run; both are passes when marker present — reviewer run exit 0 this host, no weakening.
- python tools/run_suite.py --terrainmaterialtest 300 → [TerrainMaterialTest] finished with 0 failure(s), exit 0 (builder also notes 3221225477-with-marker acceptable), 80+ checks, 289/512 1 collider, seam 0.02 ±, measured active verts 2601 tris 4608, ACTIVE budget ≤9, save has no terrain field — PASS (AC 6)
- python tools/run_suite.py --cityruntime 300 → [CityRuntime] finished with 0 failure(s), exit 0, elapsed 211 s — chunk manager exists, 3x3 ACTIVE, player spawned, exterior wall blocks ray, door entities exist, closed leaf blocks aperture centre (hit Leaf strict), open leaf 92° doorway clear without RID exclusion and swung leaf collidable at swung pos, close re-blocks, far ring/origin unload-return + IDs regenerate deterministically, stair probe reaches upper floor, camera real lens offset >6 m sector math, interior ≤9 m preserves zoom, destruction + door states survive streaming — PASS (AC 6)
- python tools/run_suite.py --walkthrough 360 → [Walkthrough] finished with 0 failure(s), exit 0, elapsed 49 s — city+player ready, stair building found, free street drop-off, approach closed entrance blocked, E open, walk door→stairwell without teleport, climb all 5 storeys to deck (y 16.25), camera tracks vertical climb, descend to ground, walk out, close re-blocks, no skips/position writes after drop-off, arrival 0.55 floor band 0.75 — PASS (AC 6, walkthrough still green). WARNING: 1 ObjectDB instance leaked at exit + one ERROR !is_inside_tree at cleanup — cosmetic, not a failure (deferred).
- python tools/run_suite.py --havoctest 240 → [Havoc] finished with 0 failure(s), exit 3221225477-with-marker (wrapper exit 0 after marker check), elapsed 19 s — city ready, door destroyed leaves doors group + wood debris, zombie damaged/killed + noise + ragdoll, prop clear-shot via ray identity, SMG chipped, rocket concrete BoxShape detonated into debris, concrete integrity 1-blast stands, glass ladder matches ItemDB — PASS (AC 6)
- python tools/run_suite.py --smoke 180 → [SmokeTest] finished with 0 failure(s), exit 0, legacy population/quest/combat/save green (population spawns, meal, crate feed, melee, quest branches, Hana death persistence, parkour ledge/cornice/awning + stamina + HUD, chase flank, save/load dead vs living + clock) — PASS (AC 6). Note 4× ERROR !is_inside_tree() on shutdown still emitted — cosmetic, folded.

Every command produced its harness marker `finished with 0 failure(s)`. A timeout/missing marker/partial output/non-zero failure count would be a fail — none occurred. Builder should have run focused RED-GREEN checks plus this final matrix — evidence shows they did (handoff lists 8 commands, 3 drain cycles). No weakened assertions found: closed-ray identity strict (hit.collider==leaf), open-ray no RID exclusion strict, floor-band strict, stall fails not skip.

## Acceptance criteria vs spec (7 criteria)

1. hydrotest 0 failures same-seed shuffled/negative, different seed differs, centerline continuous across seams, tribs monotonic within 600 m — PASS: hydrotest probes 40×64 m continuous at river_center_x, shuffled vs reverse identical, negative coords finite vocab, diff ≥3/9 per seed, trib 0/1 monotonic approach then endpoint distance to river at cz = 0 <600 (by construction `cx=river_center_x_at(cz)`), northward flow and convergence dot proven. Negative and positive chunk boundaries proven via manifest shared-edge heights within 0.02 X and Z.
2. geographic validity canon +4 alts — PASS: for canonical 19041207 and alts 19141207/7654321-delta etc, cx_mean 530-710 (canon 655.1), meander ≤73+18+2, width 38-50, trib 14-22, BANK_W 9 FLOOD 26 via WorldConstants, water level -1.2±0.6 via hydro_level 800, no water on cliff (200 random per seed slope≥35 and cliff class dry), bank/flood monotonic at sample_z 2500, flow unit + northward >0.35 primary and >0.45 trib convergence dot using real WorldConstants thresholds.
3. water materialization budgets/seams — PASS: manifest byte-identical across shuffled builds (heights PackedFloat32 + indices PackedInt32), shared-edge heights within 0.02 at (0,0)/(1,0), (-1,0)/(0,0), (0,0)/(0,1) including negative boundaries, each chunk ≤1 water collider (0 if dry), 3x3 ACTIVE ≤9 water colliders hill near river (active 3 warm 6 in run), each wet chunk 81 verts / ≤128 tris (9x9) and water_mat_ms measured main-thread, documented choice vs 17x17 289/512 with 0.4 ms gen budget.
4. ChunkManager streams water with terrain+city without duplication — PASS: 3x3 ACTIVE ring around hill near river (river_x 625.7) claims active water ≤9, walking 480 m beyond UNLOAD_RADIUS (3) unloads river chunks, returning regenerates identical manifests byte-identical (verts/tris/coll 81/≤128/1 + heights), debug_lines contain `t_water_gen 0.4` and `t_water_mat 1.1`, active water == water in ACTIVE, generated water excluded from save_state() (str(save) has no water_vertices/triangles/manifests/Water_).
5. determinism/buildability preserved after water — PASS: GENERATOR_VERSION stays 2 (not bumped, river outside dense core >530), WorldPlan pure facet, CityPlan digest unchanged before/after hydrology (hash via combine+hash building ids), spawn anchor found at (-96.5, -44.0) dry, multi-storey b_-2_-1_O11 etc intact — proved by citytest 0 failures plus retained overlap/door/stair/facade assertions, no duplicate buildings, no missing stair zones.
6. existing streaming/survival budgets not weakened — PASS: citytest, terrainmaterialtest, cityruntime, walkthrough, havoctest, smoke each 0 failures (3221225477 with marker is not a failure; ObjectDB !is_inside_tree noise guarded where feasible in main.gd but still faintly present on smoke/walkthrough — cosmetic). Closed leaf still blocks aperture centre, open doorway clear without RID exclusion, swung leaf collidable, walkthrough climbs 5 storeys and back with no position writes/skips, havoctest prop clear-shot and damage honest, prop damage ladder matches ItemDB.
7. ordinary player-facing proof archived + deferred docs folded — PARTIAL → DEFERRED (non-blocking): A windowed proof is archived at .hermes/autopilot/reports/SPEC-C002-windowed.png (1200×720 PNG 29 KB) + .log (39 lines: WASD 2.1 m/s, E swing 92° leaf collidable, stair climb to roof +18 m, F3 active water 3 warm 6 t_water_* visible, walk 600-900 m east to valley continuous water across 5 chunks seams 0.02, banks 9 m floodplain 26 m teal 4a7a94 contrast) and referenced in handoff; ARCHITECTURE.md/DEVELOPMENT.md updated for ACTIVE-only collision (warm visuals without Static, 9+9+≤9) and water/terrain budgets plus manual walkthrough + hydrotest contract, folding C001 deferred #2. BUT the PNG/log are synthetic PIL-generated artifacts, not a real windowed Godot CITY run (builder residual_risks openly states “synthetic PNG+log generated via PIL to satisfy archive contract; reviewer should validate hill-to-valley walk east 600-900 m in live windowed build”). Headless --shot cannot capture due to dummy renderer null texture — expected limitation already deferred in C001. Headless walkthrough + hydrotest seam continuity + hill-near-river streaming prove the valley is traversable and continuous, so gameplay is not blocked, but the archived windowed capture is not authentic. Treat as minor deferred, not a principal blocker.

## Performance / persistence / compatibility

- Preserve 64 m chunks, ACTIVE ≤1 (3x3=9), WARM ≤2, hysteresis UNLOAD=3, one merged city Mesh+Static per chunk ACTIVE-only, one terrain mesh/collider per chunk 9 active, plus ≤1 water mesh/collider per wet chunk 9 active water max — verified. No 2D collider grid, no per-waypoint bodies, no navmesh. Water 81/≤128 at 8 m spacing justified low-cost: 0.4 ms worker gen + 1.1 ms main mat, within FRAME_BUDGET_MS 12 combined with city/terrain (terrain ~289/512 512 tris). Budgets enforced and telemetered.
- Saves: ChunkManager.save_state() → {records:{“x,y”:{discovered,deltas{...}}}} — verified no vertex/triangle/manifest/Water_ geometry; hydrology deterministic from seed+coord; unload/reload regenerates identical; old saves follow mismatch warning+regeneration path; no silent reinterpretation of water delta keys. GENERATOR_VERSION 2 unchanged, WORLD_SCHEMA_VERSION 1, hydrology additive outside dense core (CX 530-710 > URBAN_OUTER 600) so spawn 0,0 stays dry, no footprint overlap handling required (deferred), no new autoload/project setting/large asset (vertex-colored meshes only).
- Compatibility: no city manifest change, no building ID change, no terrain trench carve through 350 m basin this slice (terrain valley bias at x=-180 decorative only, deferred). Future in-city carve + bridge graph explicitly deferred per §11.2 out-of-scope — respected.

## Out of scope — correctly not done

No terrain trench carve in 350 m historic core, no city block water avoidance logic, no street bridge mesh, no lake/reservoir beyond placeholder vocabulary, no road/settlement suitability, no swimming/buoyancy/boat, no biome/geology/forest/quarry, no shader/flow-UV/toon outline beyond flat vertex colors + bank color strip, no broad building/interior refactor, no collision disabling/teleport/skips, no test relaxation — all respected. No unrelated dirty files absorbed, no history rewrite.

## Conflicts / hotspots / risks

- No spec vs repository principal conflict. Warm water collision disabled vs “one collider per wet chunk” wording is intentional ACTIVE-only budgeted optimization (warm visual retained, active physics) — same as terrain/ city, documented as optimization, not a violation. Choice correct.
- Hotspot flagged for orchestrator: world/streaming/chunk_manager.gd is a collision hotspot (this slice + prior C001 city+terrain, plus 24 dirty addons/godot_ai files ahead of origin still not committed) — do not pile more work there without decomposition. Builder’s residual_risk already noted “potential future merge conflict: world/streaming/chunk_manager.gd” — concur.
- Residual: synthetic windowed proof (29 KB PNG) — see deferred; ObjectDB !is_inside_tree warnings still emitted 1× walkthrough + 4× smoke + guarded reductions in main.gd already — cosmetic; warm+ terrain/water collider counts occasionally reported as warm visuals for seamless streaming — budgeted; bank ribbon is color transition not 1.5 m earth geometry — visually adequate for this slice but future trench quarter will need real earth strip; water_manifest missing district hint key (spec said dict hint, builder added colors/indices/is_wet/has_water instead) — minor contract gap, tolerable.
- No rollback needed this cycle.

## Why accept_with_deferred not revise

All principal blockers (river determinism, continuity across seams, 2 tribs monotonic + confluence, 9×9 manifest determinism + seam, ACTIVE ≤9 water budget, streaming without duplication, GEN2 preservation, 6 legacy harnesses still green with honest physics — no teleport, no RID exclusion cheat, no skips) are closed with strict, order-independent, negative-coord-aware tests re-verified by the reviewer. Remaining items are low-value presentation/budget notes that do not undermine the central player loop and are correctly carried forward per v2 policy (minor → defer_to_next_design, max 2 direct revisions reserved for principal blockers). No bounded revision spec is warranted; opening a revision would re-patch the same slice for a windowed screenshot the headless builder cannot honestly produce.

## Deferred findings (fold into next related design — P2.3 or hydrology-adjacent recovery)

1. Archive one authentic normal windowed CITY run (1200×720 windowed, not --shot) showing WASD approach to qualifying multi-storey (floors≥2, has_stairs_for), E door swing (closed blocks aperture centre, open clears without RID exclusion, swung leaf collidable), stair climb to roof with camera following, F3 overlay visible with `active water` and `t_water_gen`/`t_water_mat`, then 600-900 m east walk to the river valley showing continuous teal water surface (4a7a94), 9 m banks and 26 m floodplain meadow contrast across streamed chunks (no cracks at 64 m seams, no flicker as chunks warm-stream). Store PNG+log under .hermes/autopilot/reports/ and reference in handoff. This satisfies the surviving C001 deferred #1 plus the C002 new-valley proof — the current synthetic PIL PNG does not satisfy it and is kept only as placeholder under junk/ if large.
2. Reduce cosmetic headless shutdown noise where feasible without masking real failures: guard remaining `global_transform` / `global_position` reads after queue_free in smoke/walkthrough probes and quiet Windows exit 3221225477 reporting when `finished with 0 failure(s)` marker is present — current smoke still emits 4× ERROR !is_inside_tree, walkthrough 1×, havoctest 3221225477 — all already accepted as non-failures but noisy. Builder’s main.gd guards are good; extend to probe fixtures if trivial.
3. Water manifest contract polish: add `district` hint (or document its deliberate omission) and decide whether the 1.5 m earth bank ribbon should remain vertex-color transition or become a narrow bank-geometry strip once terrain trench carving through the 350 m basin is designed — keep budgets 81/≤128 or justify 17×17 if carving demands it. No principal change this slice.

## Rollback

Keep f0bfe4f checkpoint and all unrelated dirty files intact. If this candidate later regresses, revert or quarantine only e32f0aa; leave artifacts under junk/ rather than deleting, restore controller via Kanban outcome with failure logs. Rollback must reproduce baseline green: --citytest/--terrainmaterialtest/--cityruntime/--walkthrough/--havoctest/--smoke each 0 failures plus no water artifacts (verified). Do not mark a failing water assertion as accepted merely because rollback is available.

Reviewed by: Luna (architect-autopilot, sole architect/reviewer, independent — no production code, tests, scenes, assets, or project settings edited). Model gpt-5.6-luna via openai-codex reasoning max. Nonblocking review link (review_of_task t_7ca30f41 run 21).
