# AUTOPILOT TASK — The ONLY current implementation assignment

**Task:** Deterministic underground chamber proxy — 5×5×3 vault at cave entrance -2m streamed per chunk (G10-M1 bounded slice)

**Grand Plan:** Generation 10 — M1 Underground chamber proxy

**Player-facing goal:** Cave entrances already mark quarry-linked portals 5a4a3a at terrain+0.01 — now stepping through "Enter cave" reveals a believable 5×5×3 vault 2 m below the surface at `entrance_pos + Vector3(0,-2,0)` with charcoal-grey walls 4a3a2a, giving the first traversable underground volume without breaking streaming, budgets, or determinism.

## Context

Repository HEAD 6f704b8 streams city interiors (G9-M1: residential ground floor 3-4 rooms, 0.18 wall 0.95 opening, 320/240 per chunk additive to 1500/2480, ACTIVE-only) plus asset pipeline (G9-M2: `art/asset_catalog.gd` + `wall_2m.glb` 920 bytes fallback a8a090 caps 4 0 collider) plus society hamlet worker 06-18 at workbench/granary/field within 90 (SocietyPlan pure, NPCBrain WORK 0.78-0.88 hunger 70 fatigue 70 speed 2.2 arrive 1.8, 7 workers, 54 peak) plus terrain 17×17, water 9×9, biome 9×9 + field/orchard tilled, road ribbon, rural fabric with wells/forage/hearth/workbench/granary, cave portal 3.6×3.6×2.2 5a4a3a 24/12 0 collider Area3D "Enter cave" per 256 cell quarry limestone/slope≥28/cliff spacing 32, vertical roof_bridge 8b7f6e 24/12 0 collider, industrial corridor 7a6a6a/slag. All geometry vertex-colored with fallback asset, ChunkManager streams city+terrain+water+biome+road+rural+cave+vertical+industrial+city interior+society with ACTIVE/WARM/COLD MAX_MATERIALIZATIONS_PER_FRAME 1 early _collect_finished_jobs freed-Zombie guard telemetry. VISION Arc D/E requires functional underground space beyond portal, not just Box portal. CavePlan currently yields 0-1 entrance per 256 cell deterministically via `WorldSeed.cave_entrance` spacing 32 road≥4 water>11 slope<22 urban≥350. This task opens the underground pipe with smallest verifiable slice: chamber vault at entrance -2m as true volume, no shafts/collapse/flood yet.

## Scope

**Implement:**

1. `world/generation/world_constants.gd` authoritative numerics for cave chamber (no duplicate inline numbers elsewhere): `CAVE_CHAMBER_VOCAB [&"cave_chamber"]`, `CAVE_CHAMBER_SIZE Vector3(5,5,3)` (x 5, z 5, y 3), `CAVE_CHAMBER_OFFSET Vector3(0,-2,0)` (entrance_pos + offset is chamber center), `CAVE_CHAMBER_COLOR Color("4a3a2a")` + `CAVE_CHAMBER_DARK Color("3a2a1a")` optional, `CAVE_CHAMBER_LIFT_M -2.0` (or offset documented), `MAX_CAVE_ENTRANCES_PER_CHUNK 1`, `MAX_CAVE_CHAMBERS_PER_CHUNK 1`, `MAX_CAVE_VERTS_PER_CHUNK 48` (24/12 entrance +24/12 chamber), `MAX_CAVE_TRIS_PER_CHUNK 24`, `CAVE_CHAMBER_SPACING 32.0` reuse cave spacing, `CAVE_CHAMBER_ROAD_SETBACK 4.0` etc reuse gates, no new autoload; constants tell policy.

2. `world/generation/world_seed.gd` — add `CAVE_CHAMBER_DOMAINS` ordered `[&"cave_chamber"]` (or single domain if additive) seed-separated from terrain/hydro/geology/biome/settlement/road/rural_building/cave_entrance etc., handles negative coords via floori, no RNG sharing. Document reuse if NewPlan reuses existing domain but prefer new ordered domain for purity.

3. `world/generation/cave_plan.gd` pure extension — deterministic per-entrance chamber:
   - For each `cave_entrance` anchor already generated (0-1 per 256 cell limestone/slope≥28/cliff spacing 32 road≥4 water 11 slope<22 urban≥350), generate 0-1 chamber where entrance exists, at `chamber_pos = entrance_pos + Vector3(CAVE_CHAMBER_OFFSET.x, -2.0, CAVE_CHAMBER_OFFSET.z)` more precisely `Vector3(0,-2,0)` from entrance center, so center is `entrance_center + Vector3(0,-2,0)` and vault is `5×5×3` (x×z×y) with `y = terrain_y(entrance) -2 + 1.5` center? Simplify: chamber center = entrance_pos + Vector3(0,-2,0), size 5×5×3, color 4a3a2a, provides stable `chamber_id: "cave_chamber_<entrance_id>"`, `entrance_id`, `pos: Vector3` (x,y,z), `center: Vector2` (xz for chunk ownership), `aabb: Rect2` (xz), `size`, `color`.
   - Siting gates reuse entrance gates exactly (quarry-suitable limestone etc., already validated), no new gate beyond entrance existence, suppressed inside `URBAN_INNER_M 350` already via entrance, handled via same 256 cell and spacing 32 greedy by id lexicographic (so chamber inherits entrance spacing, no extra spacing needed beyond entrance's 32, but chamber also respects 32 from other chambers via same id ordering).
   - API pure: `cave_chambers() -> Array[Dictionary]`, `cave_chambers_in(rect) -> Array[Dictionary]` clipped by center inside rect, `chamber_for_entrance(entrance_id) -> Dictionary`, `nearest_chamber(p) -> Dictionary`. Must be byte-identical shuffled including negative coords, different seed materially differs (≥2/9 probes differ + ≥30% placements differ OR at least 2 distinct chamber assignments in 5-seed matrix differ), center ownership not needed beyond entrance's ownership, no duplicate chamber_id, `GENERATOR_VERSION` stays 2 additive (chamber is pure volume overlay, no parcel topology change).
   - No physics, no scene mutation, no persistence of chamber position beyond plan (position is via chunk materialization).
   - Keep existing `cave_entrances()` / `cave_entrances_in()` contracts untouched; add `cave_chambers*` helpers.

4. `world/streaming/underground_chunk_builder.gd` extension — batch chamber per chunk:
   - For each chunk rect `Rect2(coord * 64, 64)`, query `cave_plan.cave_entrances_in(rect)` and `cave_plan.cave_chambers_in(rect)` via WorldPlan facade refs (or directly via CavePlan instance), center ownership gives no duplication at + and - and -Z borders (building center ownership pattern, same as cave entrance).
   - Materialization per chunk: entrance Box 3.6×3.6×2.2 at terrain+0.01 color 5a4a3a Area3D "Enter cave" ACTIVE-only as before (24/12 0 collider), plus if chamber exists for that entrance, chamber Box 5×5×3 at `entrance_pos + Vector3(0,-2,0)` color 4a3a2a with 24/12 additional but capped 48/24 per chunk 0 collider Area3D "Explore chamber" or same Area3D portal remains, vault visual only under terrain, batched vertex-colored, caps `MAX_CAVE_VERTS/TRIS_PER_CHUNK 48/24` per chunk (entrance 24/12 + chamber 24/12), at most 1 entrance +1 chamber per chunk, at most 0 collider (Area3D only) so unified 54 peak not 63 (cave Area3D not counted, verifier scans `get_nodes_in_group("cave_chunk")` body count 0 vs `cave_colliders` 0).
   - Shared-edge: no duplication at shared borders via center ownership, same as entrance; seams 0.02 not applicable to cave vault but manifest byte-identical shuffled.
   - Streaming pacing: no new pacing, reuse `MAX_MATERIALIZATIONS_PER_FRAME 1` + early `_collect_finished_jobs(pc)` + freed-Zombie guard extended to CaveChamber Area3D (`is_instance_valid` + `!is_queued_for_deletion()` + `is_inside_tree()` before `global_transform`).

5. `world/generation/world_plan.gd` facade owns enriched `CavePlan` and forwards pure queries `cave_chambers()`, `cave_chambers_in(rect)`, `nearest_cave_chamber(p)`, `cave_chamber_for_entrance(id)` (private instance per worker thread, plan_mutex guards CityPlan caches exactly as before). Wired via `set_world_refs` for consistency. `GENERATOR_VERSION` stays 2 additive (chamber is pure volume overlay, no parcel topology change).

6. `world/streaming/chunk_manager.gd` — extend cave telemetry to include chambers: counters `_cave_vertices_total/_triangles_total/_entrances_total/_chambers_total`, `_cave_active_colliders` stays 0, `_cave_colliders_total` 0, debug_lines `cave verts|tris|colliders|entrances|chambers` + `t_cave_gen|t_cave_mat`, pacing `MAX_MATERIALIZATIONS_PER_FRAME 1` + freed-Zombie guard verbatim, chunk groups `cave_chunk` already, `save_state()` excludes generated cave/chamber geometry, only `deltas.cave_discovered` (and optionally `deltas.cave_chambers_discovered` empty) persisted, deterministic re-derive on load with per-chunk deltas re-applied before materialize keeps discovered flag, unified 54 peak not 63.

7. Update `docs/world/WORLD-CONTRACT.md` new §28 with cave chamber contract (vocab, siting gates, manifest, budgets 48/24 0 collider ACTIVE-only, save exclusion, GENERATOR_VERSION stays 2 additive, no project setting), and `ARCHITECTURE.md`/`DEVELOPMENT.md` module map + telemetry + gate docs for `--cavetest` extension.

**Do NOT implement (out of scope):** shafts/tunnels/collapse/flood/ore veins/mineshaft graph beyond single 5×5×3 vault at entrance -2m, ladder/bridge second type, industrial warehouse/slag beyond existing palette, toon shader, village workers beyond hamlet 1, city retail upper floors, terrain carve, new autoload/project setting, collision mesh merging.

## Acceptance Criteria (prove independently, not one aggregate fallback)

1. **Determinism & siting** — `CavePlan` pure: same-seed `cave_entrances()` and `cave_chambers()` and `cave_chambers_in(rect)` byte-identical shuffled including negative coords, different seed materially differs (≥2/9 probes differ + ≥30% placements differ OR at least 2 distinct chamber assignments in 5-seed matrix differ), each entrance gets 0-1 chamber where entrance exists (never without entrance) at `entrance_pos + Vector3(0,-2,0)` size 5×5×3, spacing ≥32 from other chambers (inherits entrance spacing), never inside `URBAN_INNER_M 350` (suppressed via entrance), never inside water/floodplain/cliff (via entrance gates), no duplicate chamber_id, `GENERATOR_VERSION` stays 2 additive (chamber overlay, no parcel topology change).

2. **Materialization budgets & seams** — manifests byte-identical shuffled, each chunk `≤48 verts / ≤24 tris` (24/12 entrance +24/12 chamber) 0 collider (Area3D only), no duplication at shared borders (center ownership), shared-edge not cracked at + and - and -Z, at least 1 resident cave chunk with chamber around quarry transect (via `get_nodes_in_group("cave_chunk")` has vault, not just entrance), unified 54 peak not 63 (cave Area3D/MultiMesh not counted, verifier scans `get_nodes_in_group("cave_chunk")` body count 0 vs `cave_colliders` 0), `t_cave_gen/mat` within `FRAME_BUDGET_MS 12` (chamber slice ≤3ms added), `cave_instances` not applicable but verts/tris within 48/24.

3. **Streaming & telemetry** — ChunkManager streams cave entrances+chambers with city+terrain+water+biome+road+rural+vertical+industrial+city interior+society without duplication: 3×3 ACTIVE around quarry near cave claims `active cave ≤3` (sparse) with at least 1 chamber active when central, walking 480 m beyond `UNLOAD_RADIUS` unloads cave chunks and returning regenerates identical manifests (center/pos/yaw/kind/chamber pos/size), `debug_lines()` contains `t_cave_gen|t_cave_mat` and `cave verts|tris|colliders|entrances|chambers`, pacing 1-per-frame + freed-Zombie guard remains 0 failures, no new collider (chamber 0 collider, 54 peak intact).

4. **Persistence & compatibility** — `save_state()` excludes generated cave/chamber geometry (only `deltas.cave_discovered` and optionally empty `deltas.cave_chambers_discovered` persists, deterministic re-derive on load), deterministic re-derive on load with per-chunk deltas re-applied before materialize keeps discovered flag byte-identical after load, `GENERATOR_VERSION` stays 2 additive, `WorldPlan` pure, `CityPlan` IDs / Terrain 17×17 / hydrology CX 530-710 / biometest etc. unchanged proved by `--citytest + --terrainmaterialtest + --hydrotest + --biometest + --roadtest + --ruraltest + --cavetest + --verticaltest` each 0 failures with retained seams, rural/cave/vertical/industrial still monitorable, closed door still blocks/open clears, asset fallback still a8a090.

5. **Existing budgets not weakened** — `--citytest, --terrainmaterialtest, --hydrotest, --biometest, --roadtest, --ruraltest, --cavetest, --verticaltest, --cityruntime, --walkthrough, --havoctest, --smoke, --import` each `finished with 0 failure(s)` (3221225477 with marker not failure; guards extended to CaveChamber if needed), walkthrough climbs 5 storeys no teleports, asset MeshInstance does not block door/window, 54 active peak intact (cave chamber 0 collider), `t_vertical_gen` still within patched <125, `t_cave_gen` within <6, no inline duplicate numbers beyond WorldConstants.

6. **Player-facing proof archived** — one normal windowed run log+PNG under `.hermes/autopilot/reports/SPEC-CAVE-CHAMBER-windowed.*` (1200×720, not --shot dummy) shows F3 overlay `cave verts|tris|colliders|entrances|chambers` alongside active city/rural/road/water/terrain/biome/vertical/industrial/society, then near quarry limestone upland slope≥28 with cave portal Box 3.6×3.6×2.2 5a4a3a "Enter cave" prompt at 1.8m and immediately visible vault Box 5×5×3 4a3a2a at -2m below terrain (chamber prompt "Explore chamber" or portal remains) without seam cracks at 64m borders, no teleport, unified 54 peak. Log line `cave chamber <chamber_id> at <pos> entrance <entrance_id> distance -2.0 size 5x5x3` and `cave entrances 1 chambers 1` in debug, referenced in `WORLD-CONTRACT §28`, `ARCHITECTURE.md`, `DEVELOPMENT.md`.

## Required Tests / Evidence

- `python tools/run_suite.py --cavetest 400` extended with `_test_cave_chamber` (determinism shuffled incl negative, different seed differs, 5-seed matrix distinct, per-entrance 0-1 chamber at -2 size 5×5×3, never urban 350, spacing ≥32, budgets 48/24 0 collider, 1 resident cave chunk with chamber, unified 54 peak, 0 failures) — PLUS `python tools/run_suite.py --citytest 400`, `--terrainmaterialtest 300`, `--hydrotest 300`, `--biometest 550`, `--roadtest 400`, `--ruraltest 400`, `--verticaltest 400`, `--cityruntime 300`, `--walkthrough 360`, `--havoctest 240`, `--smoke 180`, `--import 120` each 0 failures (extended timeout 550 for biometest acknowledged on this HW; if HW truncated, document that 500-600s is guidance and show at least 53 PASS up to manifest stage, but cavetest/chamber must reach finished with 0 failures).
- Determinism harness: prints `CaveTest finished with 0 failure(s)` plus shared-edge byte-identical, chamber size/offset verified.
- Windowed proof PNG+log (normal windowed, not --shot dummy, 1200×720) archived under `.hermes/autopilot/reports/` with visible chamber vault and F3 overlay.
- Manual traversal still holds: 600-900m east along rural road corridor to river valley shows continuous shells+bridge+hedgerow and through open door partition 0.95 and near quarry cave portal 5a4a3a with chamber 4a3a2a at -2m.

## Out of Scope Forbids

- No shafts/tunnels/collapse/flood/ore veins beyond single 5×5×3 vault at entrance -2m; no ladder bridge; no industrial warehouse/slag beyond palette; no toon_outline.gdshader; no village workers beyond hamlet 1; no city retail upper floors; no terrain trench carve; no multi-chamber per entrance; no new autoload; no project.godot import re-init; no collision mesh merging.

## Delivery

Builder overwrites `BUILD_RESULT.md` (and `.hermes/autopilot/BUILD_RESULT.md`) after commit+push to `origin/master`, records task hash (SHA256 of this file), HEADs, changed files, tests with lines `finished with 0 failure(s)` or honest failure with blocker, player-facing verification, limitations, completion belief.
